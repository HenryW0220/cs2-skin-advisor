# 特征假设库（HYPOTHESES）

> 这份文档管一件事：**外部概念（股市技术分析、市场微观结构、行为金融、你的操盘剧本）进入这个项目的通道**。
>
> 规矩是一句话：**概念只能当假设来源，不能当先验。** 写进这里不代表它成立，只代表它值得测；从这里毕业进 `lib/signals`/`lib/rules` 必须过第五节的门槛，一步不减。
>
> **已证伪的条目和已验证的同等重要，永远不删。** 它们防的是几个月后有人（包括 AI）再想一遍同样的直觉、再花一遍同样的时间。
>
> 版本：v2（2026-08，补入候选池 + 8-03 之后的复检结果）。相关文档：验证方法见 `REPORT-*.md`；现状见 `HANDOFF.md`；路线图见 `PLAN.md`。

---

## 0. 为什么要有这份文档

项目里已经有一正一反两个案例，把"概念该怎么用"讲清楚了。

**正面 —— 洗盘。** 概念来自项目所有者口述的操盘剧本。但 `scripts/analyze-playbook-stages.mjs` 是先用分段线性拟合 + BIC 无监督变点检测切段、k-means 聚类完**才**对号入座命名的，全程不用剧本先验（PLAN.md 风险 5 点名要防的"按剧本切数据再用数据证明剧本"的循环论证）。结果 `drawdown48h` 成了三份独立报告里唯一稳定复现的信号。**概念的价值是提供"该往哪看"，严谨来自验证时把概念关在门外。**

**反面 —— 成交量异动。** 这本身就是一个被灌输进来的股市概念："量能异动"是标准技术分析词汇，它进规则引擎不是因为验证过，是因为它标准。结果是 `price_snapshots.volume`（在售挂单数量，**存量**）被喂进一个"最新一期 ≥ 前 7 期均值 2 倍"的、按**成交量（流量）**设计的判据。实测 325 个饰品这个比值中位数 **1.000**、最大 **1.22**、达标 **0 个**。后果级联且完全静默：这一项永不贡献分数 → SELL（≤−40）只剩"RSI 超买 + 趋势走弱"一条路 → 而这两个状态几乎互斥（实测 0/325 同时满足）→ SELL 永不出现 → 模拟盘 230 笔一笔平不掉 → 用来验证规则引擎的实验拿不到任何卖出侧样本（踩坑 43）。

**教训：导入词汇的同时默认导入了判断，但没有导入前提条件。** 股市的"成交量"是流量、有真实撮合数据；CS2 只有挂单存量。词一样，东西不是一个东西。

**所以第三节每一条都带「前提条件」栏。写不出这一栏的概念，不许进候选池。**

---

## 1. 状态定义

| 状态 | 含义 |
|---|---|
| 🟢 **已上线** | 过了第五节全部门槛，在生产代码里参与决策 |
| 🔵 **已验证未上线** | 统计上立得住，但样本量不足以校准阈值，或上线副作用没评估 |
| 🟡 **待验证** | 有明确可测形式和数据路径，还没跑 |
| ⚪ **数据不足** | 形式明确，但当前数据结构下测不了 |
| 🔴 **已证伪** | 测过，不成立。**不要再试** |
| ⚫ **不适用** | CS2 市场结构上不存在对应物 |

---

## 2. 已在项目里的条目

### 2.1 🟢 已上线

| 概念 | CS2 里的形式 | 证据 | 位置 |
|---|---|---|---|
| 波动率放大 | `vol24h` 24 小时波动率 | 按饰品 **67/67**、AUC 中位 **0.819**（池化 0.710），p≈0 | `lib/signals/manipulation-score.ts` |
| 异常涨跌幅 | `absReturn24h` / `move24h` | **67/67**、中位 **0.760**（池化 0.657），p≈0 | 同上 |
| 偏离均线 | `maDev` | **66/67**、中位 **0.744**（池化 0.639），p≈0 | 同上 |
| 洗盘 / 甩轿子 | 深回撤 >15% 接急拉（`drawdown48h`） | 无监督聚类独立得出（n=81，回撤中位 17.4%）；C1/C2 和 T+7 两版基线里都是**最大正权重特征**；三份独立报告互相印证 | `lib/signals/washout.ts` |
| 追涨风险 / **短期反转** | 24h 涨幅 >15% | 未来 7 天平均 **−10.74%**、**70.8% 为负**；动量基准两次测试 −28.8% / −33.1% | `lib/signals/momentum-chase.ts` |
| 同收藏品联动 | 上级异动 → 下级炼金料跟涨 | 机制已证实（喧嚣杀戮 + USP-S 次时代 2026-05-22T04:00 同一小时异动） | `lib/anomaly-scan.ts` |
| 超额收益 / 基准归因 | "未来 7 天收益 − 当天全市场中位数" | v2 全部阈值由此反推（714 饰品 / 99 天）；>30% 档超额中位 **−18.69%**、**71.4% 为负** | `lib/rules/sell-rule-v2.ts` |

> ⚠️ **`vol24h` 是波动率不是成交量**。名字跟 2.4 已证伪的 `volumeRatio` 长得像，两者毫无关系。

### 2.2 🟢 已上线但**没有验证依据**（诚实登记，优先补测）

这一类是这份文档最该存在的理由：它们已经在影响你看到的建议，但从来没有过回测。

| 概念 | 现状 | 问题 |
|---|---|---|
| **RSI 超买/超卖** | v1 规则引擎主要打分来源（`+30` 超卖单独 52 个、`−30` 超买单独 10 个） | **无任何回测支撑**。v2 正是按"阈值必须能从回测反推"把它挡在门外的。模拟盘 136/153 笔开仓是 score=30，等于"RSI 超卖"这一个信号——**现在的模拟盘实际上是个单因子抄底策略**，而 T+7 又让它没法止损 |
| **均线趋势（MA7/MA30）** | 另一主要来源（`−25` 趋势走弱 52 个、`+15` 趋势走强 17 个） | 同上 |

**待办**：用 `build-sell-rule-baseline.mjs` 同一套超额口径回算 RSI 各档和趋势各档。**性价比最高的待验证项**——数据、脚本、口径全现成，而结论直接决定 v1 还剩多少东西，也顺带回答"`ENTRY_MIN_SCORE` 要不要 30→40"（若 RSI 本身没信号，提到 40 只是换个噪音门槛）。

### 2.3 🔵 已验证未上线

| 概念 | 形式 | 证据 | 卡在哪 |
|---|---|---|---|
| 板块联动（价格） | `coMove` / `coMove24h` | 剔除退化样本后 **32/32**、中位 **0.613** / **0.749**，p≈0 | 没进 `manipulation-score.ts`。**历史坑**：跟踪四次的 0.500→0.520 是假象，34 个印花/探员 collection 为空、特征恒 0、全并列时 AUC 机械等于 0.5 |
| 板块联动（分组扩展） | 印花按赛事胶囊、探员按组织 | **25/26**、中位 **0.651**，p≈4×10⁻⁷ | 已写库 42 行（`collection_source='derived'`），**观察期只入库不推送** |
| 需求侧挂单厚度（吸货） | `bidRatio` = 求购数 / 自身 168h 均值 | **15/16**、p=**0.0003**、中位 **0.747**，过 Bonferroni | 只有 16 个饰品、两周数据。7-20 才入库，历史永远补不了。4-6 周后重跑 |
| 订单簿失衡（相对） | `bidAskRatioRel` = 厚度比 / 自身基线 | **14/16**、p=**0.0021**、中位 **0.710** | 同上 |

### 2.4 🔴 已证伪 —— 不要再试

| 概念 | 直觉说法 | 实测 | 备注 |
|---|---|---|---|
| 成交量异动 | "量能放大 = 有人在动" | `volumeRatio` **29/67、p=0.889**；复检阴性对照 **p=0.7428**；比值中位 **1.000**、最大 **1.22**、达标 **0/325** | 根因：存量 vs 流量。SteamDT/C5 都不返回成交笔数，K 线不带量——**当前数据源下没有可测形式**，不是阈值调不对。`lib/signals/volume.ts` 已整个删除 |
| 价差收窄 = 吸货 | "有人在下方堆买单，价差会收窄" | `bidSpread` **9/16、p=0.402**、中位 0.540 | 池化时看起来有差别（0.0690 vs 0.0572），假象。**但它在 3.1 里有第二次生命——当成本不当信号** |
| 挂单簿绝对厚度 | "买盘厚 = 有人在收" | `bidAskRatio` **10/16、p=0.227**；`bidCount` **11/16、p=0.105** | **两者池化 AUC 反而最高（0.652 / 0.571）**——量纲被"贵价/廉价品厚度天差地别"污染，池化把选择效应当信号 |
| 单小时尖峰 | "瞬时异动 = 有人扫货" | `absReturn1h` **57/66、p≈0 过 Bonferroni，但各饰品 AUC 中位数只有 0.531** | **2026-08 复检结论：维持证伪，但依据升级**——不是"弱"，是**统计显著、效应量太小**。生产在用的三个是 0.744~0.819。大概率是 `absReturn24h`（0.760）的冗余投影而非独立信号 |
| 按信号强度筛噪音 | "z 越大越可疑" | z `6-7` 68%、`8-10` 76%（几乎无差别），**z≥15 只有 31%** | 按强度砍会误伤 133 条真操盘。干净的维度是**触发价格**：`<¥5` **0%（121 条无一命中）**、`¥20-100` **99%** |

### 2.5 🟡 待验证（已在路线上）

| 概念 | 可测形式 | 需要什么 | 优先级 |
|---|---|---|---|
| **外部事件 vs 操盘可分** | 三类 `bidRatio` 中位数实测 **操盘 1.0824 > 平时 0.9802 > 外部事件 0.8022**——外部事件期间求购盘反而**更薄**，方向是反的 | 只有 **48 条**外部事件小时样本，凑不出按饰品检验 | **最高**。直接打在 C1/C2 上不了线的核心死结上 |
| **领先-滞后（Lead-Lag）** | coMove 是 24h 窗口内**共动**、不带方向；业务地基说的是**有方向的链条**。可测形式：同分组饰品对做互相关，找最大相关的滞后期 τ，检验 τ 是否系统性 >0 | 现成数据，只读脚本 | **高**。τ 本身就是可交易的提前量 |
| **Wyckoff 吸货/派发** | 吸货 = 需求侧变厚（已部分印证）；**派发 = 供给侧变厚**，用在售数单独测 | 在售侧价位分布**完全没有** | **高**。精确指向卡最久的两个指纹 |
| RSI / 均线回算 | 见 2.2 | 全现成 | **高**（成本最低） |

### 2.6 ⚪ 数据不足

吸货期（跟纯横盘分不开）、会员进场（看不出子阶段）、出货期（跟普通回调分不开，**D1 已绕开它**——v2 用追涨风险 + 洗盘否决）、真实成交量（无数据源）、在售侧挂单深度（只有总数无价位分布）。

### 2.7 ⚫ 不适用

| 概念 | 为什么 |
|---|---|
| **对冲 / 做空** | 无做空、无衍生品、无借券，仓位层面对冲**不可执行**。但它值钱的另一半——**基准归因**——已独立摸到并在用（见 2.1 末行）。**所有收益类评估必须走超额口径**，不扣基准的话普跌行情里什么都"卖对了"（7-30 归因那次就是这么被误导的） |
| **老鼠仓检测** | 已经在了：`buy_price>0` = 凭消息买入；PLAN.md E2 的"加观察池行为本身就是信号 + 多个高分用户短窗口聚集"就是标准形式 |
| 融资融券 / 龙虎榜 / 大单流入 | 无对应数据 |

---

## 3. 候选池：还没进项目的金融概念

**这一节是 v2 补的主体。** 每条必须能填「前提条件」栏——写不出来的说明还没想清楚它在 CS2 里意味着什么，不许进。

### 3.1 市场微观结构（价值最高，因为这是项目唯一有增量数据的方向）

**为什么这一族优先**：价格特征已经榨得差不多了（三个地基特征 AUC 0.74~0.82，再加一个价格衍生量大概率是冗余投影，`absReturn1h` 就是例子）。而 7-20 起入库的求购数据是**唯一一路新的、结构不同的信息**——挂单簿的需求侧天然带着"谁在买"的信息，价格数据永远回答不了这个。第一次验证就拿到了项目至今唯一统计上站得住的非价格特征，赔率明显高于继续在价格上打转。

| 概念 | 原始形式 | CS2 里的可测形式 | ⚠️ 前提条件 | 数据 | 优先级 |
|---|---|---|---|---|---|
| **虚假挂单 / 层挂**（Spoofing, Layering） | 挂大单制造供需假象，成交前撤单 | 求购数尖峰后塌陷、**而价格几乎没动**。指纹：`bidCount` 单期涨幅大 + 次期回落大 + 同期 `\|return\|` 小 | 假设撤单发生在小时采样能捕捉的尺度上。**如果庄家秒级挂撤，小时快照完全看不见**——测出负结果时必须能区分"没这回事"和"采样太粗" | 已在攒（7-20 起） | **最高** |
| **配对交易 / 一价定律**（Pairs Trading） | 两个高相关标的价差偏离历史均值 → 均值回归 | **同一皮肤不同磨损档位的价格比值**。崭新/略磨/久经的比值应长期稳定，偏离 = 有人在单独做某个档位 | 需要两个档位都有足够流动性；破损/战痕成交稀薄会给出噪音比值（7-26 扩容时正因此只留三个主流档） | **完全现成**——观察池 7-26 已按"同一皮肤的不同磨损档位一起跟踪"扩容 241 条 | **最高** |
| **非流动性指标**（Amihud ILLIQ） | \|收益率\| / 成交额，衡量单位交易量造成多大价格冲击 | 无成交量，用 `\|return\| / bidCount` 做代理 | **代理是否成立没验证过**，这是它跟成交量那次最像的地方。必须先单独证明代理量跟"真实难成交程度"相关（可用买卖价差、报价更新频率交叉验证） | 已在攒 | 高 |
| **订单簿弹性**（Resiliency） | 大单吃掉挂单后，簿子恢复厚度的速度 | 求购数骤降后回到基线需要几小时 | 需要小时级连续观测，中间不能断档（崩溃三天那种缺口会污染） | 已在攒 | 中 |
| **冰山单**（Iceberg） | 大单拆成小单隐藏真实意图 | 求购数长期温和高于基线、无尖峰、价格缓慢抬升——**这正是"吸货期"的微观结构版本** | 跟"自然需求回暖"形态上可能分不开，需要对照组 | 已在攒 | 中（跟 Wyckoff 合并测） |
| **价格冲击系数**（Kyle's λ） | 单位订单流带来的价格变动 | 挂单簿变化量 → 价格变化量的回归斜率 | 需要**订单流方向**（主动买还是主动卖），当前只有挂单存量、推不出方向 | ⚪ 数据不足 | 低 |
| **买卖价差当成本**（不是信号） | 价差 = 即时交易成本 | `bidSpread` 已被证伪为操盘信号，但**作为成本有效**：C3 回测里"低价品流动性折价"现在没有量化依据，`bidSpread` 正好是它 | 只对有求购数据的平台成立（C5 直连那路两列是 null） | 已在攒 | **高**（PLAN C3 明确要求扣流动性折价，这是唯一现成的量化来源） |

### 3.2 技术分析（价值低，大部分已被覆盖或证伪，但有两条值得测）

**为什么整体低**：技术指标绝大多数是价格的确定性函数，彼此高度相关。项目已有三个价格特征 AUC 0.74~0.82，再叠加同源指标基本是冗余——`absReturn1h` 已经演示过这个结局（p≈0 但 AUC 中位 0.531）。**默认判定：技术指标进候选池前，先说明它相对已有三个特征提供了什么独立信息，说不出就不进。**

| 概念 | CS2 形式 | ⚠️ 前提条件 / 独立性 | 状态 |
|---|---|---|---|
| **波动率压缩**（布林带收口 / Squeeze） | 波动率持续低于自身基线的**持续期长度**（不是瞬时值） | **唯一一条能说清独立信息的**：已有三个特征全是"异动强度"，测的是**变大**；压缩测的是**持续变小**，方向相反。且 Wyckoff 说吸货 = 低波动 + 区间收窄，可跟 3.1 的冰山单指纹**合并成"吸货 = 波动压缩 + 买盘温和增厚"的复合假设** | 🟡 **值得测** |
| **平台突破**（Breakout） | 长期横盘后放量突破前高 | "放量"这一半在 CS2 不可测。只剩"突破前高"，那基本是 `maDev` + `absReturn24h` 的组合 | 🟡 低，先证明独立性 |
| **跳空缺口**（Gap） | 价格不连续跳变 | 真实存在（`★ Moto Gloves \| Cool Mint` 24h 涨 121.84%）。但要先区分**真跳变**和**数据毛刺**——z≥15 确认率只有 31%，极端值多是毛刺 | 🟡 低 |
| OBV / 资金流向 / 量价背离 | —— | 全部需要成交量 | ⚫ 不适用 |
| MACD / KDJ / 各类均线变体 | —— | 都是价格的确定性函数，跟已有三个特征同源 | ⚫ 不进 |

### 3.3 行为金融（不产出交易信号，但解释为什么信号有效）

| 概念 | 在这个项目里的位置 |
|---|---|
| **短期反转效应**（Short-term Reversal） | **给"别追涨"补上文献锚**。这是学术上最稳的异象之一，项目独立测出 24h 涨幅 >15% 未来 7 天 −10.74%、70.8% 为负，动量基准两次 −28.8%/−33.1%。**意义不是新信号，是把已验证结论从"我们跑出来的"升级成"跟公开文献一致的"——这提高了它在小样本下的可信度** |
| **羊群效应 / 信息瀑布** | PLAN.md E2 的"多个互不相关的高分用户短窗口内加同一饰品 = 最强信号"就是它。已在路线上 |
| **处置效应**（Disposition Effect） | 散户倾向"卖出盈利、死扛亏损"。**跟 T+7 叠加会放大**：锁仓期内想卖卖不掉，解锁那天集中抛售 → **可测形式：拉盘启动后第 7~8 天存在系统性抛压**。直接对应 D1 逃顶时机 🟡 |
| **彩票偏好 / 低价高波动** | 散户偏好低价高波动标的。已有旁证：`<¥5` 触发的异常确认率 **0%（121 条无一命中）**——低价品波动是噪音不是操盘。已固化为 `MIN_PRICE_FOR_ANOMALY_SCAN=5` |
| **锚定效应** | 历史高点作为心理阻力位。可测：价格接近 90 天高点时的后续收益分布 🟡 低 |

### 3.4 CS2 特有的供需（股市没有对应物，但这才是"基本面"）

**这一族目前项目里完全空白**，全部是纯技术面。但操盘的前提是筹码可控，而筹码结构在 CS2 里是**公开可推的**——这是股市没有的优势。

| 概念 | 可测形式 | 数据 | 优先级 |
|---|---|---|---|
| **流通盘 / 筹码集中度** | 存世量：箱子产出速度 × 开箱率 × 磨损分布，或直接看在售总量 | 部分可从 `item_catalog` + 在售数推 | 高（**操盘的前置条件是盘子小**，可能是最强的静态筛选维度） |
| **绝版 / 供给冻结** | 印花胶囊取消、箱子停产 → 供给端归零 | 5-22 已是实例（旧 Major 印花重定价）。可从 ByMykel 推 | 中 |
| **磨损稀缺度** | 同皮肤某个磨损区间产出概率低 → 天然小盘 | ByMykel 有 min/max float | 中 |
| **赛事周期** | Major 前后印花/胶囊需求周期性 | 需要外部赛程日历 | 中（**同时是外部事件的重要来源，标注时要能识别**） |
| **游戏活跃度** | 玩家数 → 整体需求 | SteamDB 公开 | 低（影响的是大盘 beta，已被超额口径扣掉） |

### 3.5 组合与风险（不是信号，是评估口径——但项目现在真的缺）

| 概念 | 为什么现在就该有 |
|---|---|
| **最大回撤 / 夏普 / 索提诺** | `report-paper-trades.mjs` 现在只算平均收益和精度。**平均收益掩盖路径**——T+7 锁仓下中途回撤是真实痛点（回撤 30% 但你卖不掉） |
| **集中度风险** | **已经在你身上兑现了**：56 个品种整体浮亏 **−44.5%**、无一盈利，**前 5 个品种占总成本 40%（¥20,612）**，印花那批 −82%~−88%。不是理论风险 |
| **相关性 = 分散化失效** | 同收藏品联动已验证（coMove24h 中位 AUC 0.749）——**意味着"买了 20 个 Austin 2025 Holo 印花"在风险上约等于买了 1 个**。持仓页应该按联动分组显示实际暴露，而不是按品种数 |
| **凯利 / 仓位管理** | 依赖胜率和赔率估计，**现在还没有**（零平仓）。等有了再谈，别提前上 |
| **T+7 = 强制持有成本** | 锁仓 7 天等于承担 7 天价格风险却拿不到流动性溢价。可量化为"入场必须要求的最低预期收益"，直接作为买入门槛 🟡 |

### 3.6 明确判定不进的

高频/做市（无 API 交易权限）、期权希腊字母（无衍生品）、宏观利率汇率（无传导路径）、财报基本面（无发行主体）、ESG 及各类另类数据（无对应物）。

---

## 4. 方法学条目

### 4.1 🔴 数据窥探偏差（Data Snooping）—— **当前正在发生**

在同一批数据上反复重跑特征脚本、反复看 AUC 有没有动，这正是数据窥探的定义。`analyze-manipulation-features.mjs` 已跑至少四轮（7-20 / 7-25 / 7-27 / 8-03）。

**关键点：Bonferroni 挡不住这个。** 它管一次分析里的多个特征，管不了跨多次分析对同一批数据的反复凝视。

**缓解措施**：
1. **留 holdout，探索阶段一次都不看，只在最终上线确认时用一次。**
   ⚠️ **待项目所有者决定：按时间划还是按标的划。** 按时间划跟"求购深度 4-6 周后重跑"直接打架——`bidRatio` 只有 16 个饰品、样本只能往后攒，把未来数据封起来它永远攒不够。**倾向按标的划**：留出一批饰品全程不看。代价是联动类特征需要分组结构，**必须整组抽不能随机抽单品**，否则留出组里的联动特征全是退化样本（就是 coMove 那个坑）。比例待定。
2. 每次重跑在 `HANDOFF.md` 记"这是第几轮"，让重复检验次数可见。
3. **上线前最后一关必须是时间外样本**，不是同一批数据上的又一次校验。

> 跟 PLAN.md 风险 4（自我实现偏差）是两件不同的事，都要防。

### 4.2 🟢 三关（已固化）
见第五节。2026-08-03 一天内连踩三种统计陷阱后总结的，**三次都让"看数字"得出与真相相反的结论，而且都不会报错**。

### 4.3 🟢 循环论证防线
- 标注不能由 AI 按价格形态生成（踩坑 35）。判据：`price_zscore` 人工确认率 68%（485 样本）vs `manipulation_score` 36%——**差异本身就是人类标注含有价格数据里没有的信息的证据**。
- 剧本验证必须先无监督切段、后对号入座（PLAN.md 风险 5）。

---

## 5. 毕业门槛：从这份文档进 `lib/signals` / `lib/rules`

**全部通过才能进，一步不减。**

**第一关：样本是不是自相关的？**
池化 AUC 的分母是"小时样本数"，但同一饰品的一波行情能贡献几百条高度相关的样本——名义 916 条正类的有效样本量可能只有个位数。**必须"按标的各自算 AUC + 对命中标的数做符号检验"**。求购深度那六个特征里能立住的两个跟池化排序**不一样**。

**第二关：有多少样本对这个特征是退化的？**
全并列时 AUC **机械地等于 0.5**。coMove 第一版 32/67、中位恰好 0.500、p=0.687，看着彻底没信号；实际是 34 个饰品根本没有 collection、特征恒为 0。剔除后 **32/32、中位 0.749**，结论完全反转。

**第三关：量纲是不是跟标的规模有关？**
是的话必须先对自身基线归一化。`bidAskRatio` 池化 AUC 最高但只有 10/16、p=0.227——绝对水平被饰品间差异污染。

**第四关：多重检验校正。** α = 0.05 / k（k = 本次分析测了几个特征）。

**第五关：效应量下限。**（2026-08 复检时补）
过了显著性还要看**幅度**。`absReturn1h` **57/66、p≈0**，但各饰品 AUC 中位数只有 **0.531**——统计显著、效应量太小，不进。参照线：生产在用的三个是 **0.744~0.819**。
> 这一关跟第六关不是重复：**第五关管筛选阶段的效应量，第六关管阈值阶段的经济显著。** 位置不同，都要。

**第六关：阈值必须能从回测反推。**
不许拍脑袋。参照 `build-sell-rule-baseline.mjs`：分档统计超额收益中位数和为负占比，台阶在哪阈值就在哪。**并区分统计显著和经济显著**——5~15% 档 p=0.0000 但超额只有 −3% 上下，扣掉 C5 1% 手续费和买卖价差后所剩无几，所以 v2 明确不触发。

**第七关：影子并行。**
改变真实建议的规则，先并行记录至少一轮，拿到触发次数和假信号率再谈替换。**样本量不到三位数不下结论。**

---

## 6. 新增条目模板

```markdown
### <概念名>
- **来源**：技术分析 / 市场微观结构 / 行为金融 / CS2 供需 / 项目所有者口述
- **原始形式**：
- **CS2 里的可测形式**：（写不出来 → ⚫ 不适用，别硬凑）
- **⚠️ 前提条件**：（原始形式隐含了什么，CS2 这边成不成立 —— 成交量那次就栽在这栏）
- **相对已有特征的独立信息**：（说不出 → 大概率是冗余投影，参考 absReturn1h）
- **需要什么数据**：（现有？要攒多久？）
- **验证脚本**：
- **状态**：🟡 待验证
- **结论**：（验证后填，含符号检验 p、按标的 AUC 中位数、效应量判断）
```

---

## 7. 使用约定

- **新概念先进这里，不直接进代码。** 哪怕看起来再标准——成交量就是"看起来标准"进去的。
- **已证伪条目永不删除。** 想删时回想一下"价差收窄 = 吸货"这个直觉有多顺理成章。
- **不要把这份文档搬进 `CLAUDE.md`。** 那里的东西会变成每个未来会话不假思索的前提，这正是要避免的。`HANDOFF.md` 写"业务地基（用户口述）"这个标注习惯要保住——**标明出处，就不会被当成已验证事实**。
- LLM 提示词（`lib/api/nvidia-llm.ts`）可以用这里的词汇让理由更好读（"这个形态更像洗盘不像出货"比堆数字强），但**措辞不许把置信度说过头**。
- **候选池里的概念不许出现在任何面向用户的文案里**，直到它毕业。写"疑似虚假挂单"而它还没验证，就是在编。

---

**commit 建议**：`docs: 新增特征假设库`
