# 求购深度特征验证报告（第一版）

跑法：`node scripts/analyze-bidding-depth-features.mjs`（要在云端容器里跑，本机 db 是旧快照）
数据截止：2026-08-03

---

## 一句话结论

**求购挂单数相对自身基线的变化（`bidRatio`）是这个项目至今第一个统计上站得住的非价格特征**：16 个可检验的饰品里 **15 个**在操盘窗口内呈现同方向，符号检验 **p=0.0003**，各饰品 AUC 中位数 **0.747**。

但**样本只有 16 个饰品、两周数据，不足以校准生产阈值**——这是一条值得继续攒的线索，不是一个可以上线的特征。

---

## 为什么现在才能做这件事

`biddingPrice`/`biddingCount`（求购价/求购数）是 SteamDT 价格接口一直在返回、但被白白丢弃了一个月的字段，2026-07-20 才接进 `price_snapshots`（迁移 `013`）。在此之前这个项目**只有价格没有挂单数据**，导致操盘剧本六阶段里"吸货期"和"出货期"一直分不出独立指纹（见 REPORT-manipulation-playbook-stages.md）。K 线接口补不了这段历史——它本身不带量，所以求购深度只能从 7-20 往后攒。

两周之后第一次有足够数据可以验证。

---

## 方法

- **标注口径跟 `analyze-manipulation-features.mjs` 完全一致**：`manipulation_tags` 的窗口内是正类（操盘期），`anomaly_events` 里 `status=external` 的前后一天是外部事件（单独统计，不进正负类），其余是负类（平时）。
- **参考平台不能沿用 C5 优先**：C5 直连价格接口不返回求购数据，`platform='C5'` 的行多数这两列是 null。改成按"哪个平台的求购数据最多"选，实际落在 C5（走 SteamDT 聚合那一路的行）/BUFF/悠悠 三者之间。
- **按 `captured_at >= 2026-07-20` 过滤**：`captured_at` 存的是平台 `updateTime`（那条报价最后一次变动的时间）不是观测时间，冷门品会留下很早的时间戳，全库有约 1000 行求购数据的时间戳落在 2025 年，不过滤会混进时序分析。
- **同一小时多条只留最后一条**，跟 `lib/signals/resample.ts` 同口径。

### 关键的方法学修正：池化 AUC 会虚高

第一版只算了池化 AUC（把所有饰品的小时样本倒进一个池子）。这个数字**不能直接信**：同一个饰品的一波行情能贡献几百条高度自相关的小时样本，名义上 916 条正类样本，有效样本量远小于这个数。

改成**按饰品各自算 AUC，再对"有多少个饰品呈现同方向"做符号检验**——特征无效时每个饰品 AUC>0.5 应该是抛硬币，所以 P(至少 k 个命中) 是二项分布尾概率。**下面两张表的差异本身就是结论的一部分**。

---

## 结果

样本量：操盘期 916 小时 | 平时 15236 | 外部事件期 48（对应 67 个有操盘标记且求购数据足够的饰品，其中 16 个同时具备操盘/平时两类样本、可做按饰品检验）

### 池化 AUC（参考，不作为判据）

| 特征 | 操盘期中位数 | 平时中位数 | 外部事件中位数 | AUC |
|---|---|---|---|---|
| `bidCount` 求购挂单数 | 62.0 | 31.0 | 22.0 | 0.571 |
| `bidRatio` 求购数/自身168h均值 | 1.0824 | 0.9802 | 0.8022 | 0.609 |
| `bidAskRatio` 求购数/在售数 | 0.2655 | 0.1653 | 0.0778 | 0.652 |
| `bidAskRatioRel` 厚度比/自身基线 | 1.0828 | 0.9639 | 0.8167 | 0.620 |
| `bidSpread` (在售价−求购价)/在售价 | 0.0690 | 0.0572 | −0.0066 | 0.499 |
| `bidCountChg24h` 求购数24h变化率 | 0.0031 | 0.0000 | −0.2500 | 0.552 |

### 按饰品检验（判据）

| 特征 | 饰品数 | AUC>0.5 的 | 各饰品AUC中位数 | 符号检验 p |
|---|---|---|---|---|
| `bidRatio` | 16 | **15** | 0.747 | **0.0003** |
| `bidAskRatioRel` | 16 | **14** | 0.710 | **0.0021** |
| `bidCount` | 16 | 11 | 0.740 | 0.105 |
| `bidCountChg24h` | 16 | 11 | 0.598 | 0.105 |
| `bidAskRatio` | 16 | 10 | 0.748 | 0.227 |
| `bidSpread` | 16 | 9 | 0.540 | 0.402 |

6 个特征做 Bonferroni 校正后阈值是 α=0.05/6≈0.0083，**`bidRatio` 和 `bidAskRatioRel` 两个都过得了**。

---

## 三条读得出来的东西

### 1. 有效的是"相对自身基线"，不是绝对水平

`bidAskRatio`（绝对厚度比）池化 AUC 最高（0.652）、各饰品 AUC 中位数也最高（0.748），**但只有 10/16 个饰品同方向，p=0.227，不显著**。`bidCount`（绝对求购数）同理。

原因是**绝对水平被饰品之间的差异污染了**：贵价品和廉价品、热门品和冷门品的挂单簿厚度天差地别，池化之后"操盘期样本恰好来自挂单更厚的那批饰品"就能把 AUC 抬起来，那是选择效应不是信号。而两个"除以自身 168 小时基线"的特征（`bidRatio`、`bidAskRatioRel`）在按饰品检验里稳稳站住。

**这条对以后加任何特征都适用**：只要特征的量纲跟饰品本身的规模有关，就必须先对自身基线归一化，否则池化统计量会骗人。

### 2. 外部事件的方向是**反的**，这可能是个突破口

三类样本的 `bidRatio` 中位数：**操盘期 1.0824 > 平时 0.9802 > 外部事件 0.8022**。

也就是说外部事件期间求购盘反而比平时**更薄**，跟操盘期正好相反。这很符合直觉——外部事件（比如 5-22 那种版本更新）是消息面驱动的普涨普跌，买方观望；而操盘是有人在下方真金白银堆买单。

**这一条如果能立住，直接打在项目最硬的那个卡点上**：HANDOFF 问题三、REPORT-prediction-baseline.md 的结论都写着"分不开操盘和外部事件"是 C1/C2 预测模型上不了线的核心原因。价格特征做不到这件事（涨就是涨，看不出谁推的），但**挂单簿的需求侧天然带着"谁在买"的信息**。

**但现在还不能当结论**：外部事件样本只有 **48 条小时样本**，且没做按饰品检验（`external` 样本太少凑不出足够饰品）。**这是明确的下一步观察点**，等 `external` 标注攒到能按饰品拆的量级再复核。

### 3. 买卖价差（`bidSpread`）没有信号，别再试了

9/16、p=0.402、AUC 中位数 0.540——基本是抛硬币。操盘期中位数 0.0690 vs 平时 0.0572 看起来有差别，但那是池化的假象。**记下来免得以后有人再想"价差收窄=有人在吸货"这个直觉**，实测不支持。

---

## 没做 / 做不了的

- **没有改任何生产代码**。`lib/signals/manipulation-score.ts` 的特征和阈值一个都没动——16 个饰品不足以校准阈值，按 CLAUDE.md 的规矩阈值要有实测依据。
- **没法回溯历史**。求购数据 7-20 才开始攒，4~7 月中旬那批操盘标记（184 条里的绝大多数）永远没有对应的挂单数据。这个特征的样本量只能靠**往后攒**，每多一周多一批。
- **"出货期"仍然没有指纹**。这次验证的方向是"吸货=买盘变厚"，反向的"出货=卖盘变厚"没有单独验证——`bidAskRatio` 的分母是在售数，理论上能看，但它在按饰品检验里不显著，说不出话。

## 下一步

1. **继续攒，四到六周后重跑**。判据看两个：可检验饰品数从 16 涨到多少；`bidRatio` 的符号检验 p 是否稳住。
2. **等 `external` 样本够了复核第 2 条**——这是三条里最有价值的一条。
3. 样本够之后再考虑把 `bidRatio` 加进 `manipulation-score.ts`，**在那之前不要动生产代码**。
