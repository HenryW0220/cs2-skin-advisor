# 交接文档（HANDOFF）

> 写给完全没有上下文的新会话。先读 CLAUDE.md（项目规范），再读这份（现状与坑），路线图在 PLAN.md。
> 最后更新：2026-08-14（**v1 规则引擎的趋势项已删除并上线口径改造；两处"静默死亡"被挖出来并修好**。这一天的主线不是加功能，是**把三件坏了却不报错的东西挖出来**：一个信号方向反了照样在给建议、推送订阅归零两天没人知道、异地备份断了八天没人知道。
>
> **① 趋势项已删，`ENTRY_MIN_SCORE` 维持 30，另加一条成本线**（项目所有者决定，详细理由和数字见 PLAN.md D1 的 2026-08-13 条）。**用今天的库复核过 8-03 的结论**（714 个饰品/109 天，比上次多 10 天）：RSI<30 超卖 **+0.61%**（小时，698 个饰品，p=0.0000）/ **+0.37%**（日，513 个）；趋势项配对检验小时 **587/713**、差距中位 +1.71%，日 **554/652**、+5.11%，p 全 0.0000——**方向确实是反的**。删完可达 score 塌缩成 `{−30, 0, +30}`，42 个饰品 `TRIM`→`HOLD`（停止基于一个反向信号建议减仓 12.9% 的持仓），SELL 结构上不可达（有意的，真实平仓归 v2）。新增 `lib/rules/cost-line.ts`：往返成本做成常量（6.7% 下界 / 12% 项目所有者口径），每一档的回测超额跟着信号走，`/positions`、`/watchlist`、饰品详情页把没过成本线的档**显式标成"不可行动"并显示该档历史超额**，LLM 提示词强制带上这个标注（不带的话 `+30` 喂进去出来的必然是"超卖，存在反弹机会"——用自然语言把证伪过的信号讲回机会）。`paper_trades` 加 `entry_rule_version`（迁移 022，旧仓位 `v1`、新仓位 `v2`），否则 8-13 之后的平仓样本是两套买入规则的混合、拆不开。
>
> **② RSI 这条线到此为止，不再做"窗口延长"的尝试**——这条是预先封口，不是结论复述。下一个人（包括三个月后的我）看到"超额是 7 天窗口"必然会想"那拿 14 天/30 天是不是就够了"。**不要跑**，三个理由：**(a) 先验方向就是反的**——差距是 10~20 倍不是 1.5 倍，要靠拉长窗口覆盖成本，需要这个效应在更长尺度上线性累积且不衰减，而它本身来自**短期反转**，按定义会衰减；**(b) 这份数据已经被反复检视过了**，再加 10 个横截面档位 × 多个窗口，等于把"窗口长度"也变成一个可搜的超参，真跑就必须预先声明这是第 11 个及以后的检验、进同一套 Bonferroni、经济显著性门槛不变——按这个代价算收益不值得；**(c) 要重开必须有新数据源而不是新窗口**，比如求购深度攒够之后的重跑。
>
> **③ 方法学：AUC 只能用来淘汰，不能用来录取**（新踩坑 46，**这一轮最重要的发现**）。阳性对照 `return24h` 按标的 AUC 中位数 0.567，而被判死刑的 RSI 是 0.578——**已上线的那个比被否掉的这个还低**。原因是 AUC 衡量全域排序能力，而 v2 的全部价值在尾部（>30% 档超额 −18.69%）。拿它当录取标准会同时放进 RSI、否掉 return24h。以后录取判据只能是**分档后尾部档位的超额幅度 vs 成本线 + 样本饰品数 + 符号检验**。
>
> **④ 推送静默死亡：不是一次事故，是这套推送的稳态**。用逐日翻数据库备份（本机 7-23~8-05 镜像 + 云端 7-26/8-02/8-06~8-13，共 22 份）复原出完整曲线：**7-24 起 4 条 → 7-31 掉到 2 → 8-09 掉到 1 → 8-12 归零**，历史累计建过 6 条（`sqlite_sequence`）。**注意订阅是 7-21~7-23 建的，都在换 HTTPS 域名之后**——所以"从换域名那天起就没发出去过"是错的，真实机制是 FCM/Apple 端点周期性轮换、服务端收到 404/410 就删（这一步是对的，不然每轮都对着死端点重试），**但没有任何机制把它建回来**，于是只减不增。**补的是自愈两处**：页面加载时读 `getSubscription()` 并按 endpoint upsert（浏览器换了端点，打开一次页面就自动纠正）、service worker 监听 `pushsubscriptionchange`（这个事件存在的唯一目的就是这件事）。**外加留痕**：`health_signals` 表（迁移 021，**只放三样**：最近成功推送、最近尝试、掉线事件）+ `/settings` 的"运行状态"面板（0 订阅时红字明说"预警不会送达任何设备"）。**还修了一个更隐蔽的显示 bug**：浏览器本地订阅还在、服务端那条已被删的时候，界面显示"已订阅"且只给"取消订阅"按钮——用户根本没有机会重订，这正是 8-12 之后的状态。
>
> **⑤ 大盘基准做成物化表，影子成绩单第一次真的跑出了"卖后走势"**。`market_baseline_daily`（迁移 023）+ `scripts/build-market-baseline.mjs` + `scripts/market-baseline-store.mjs`，三个评估脚本共用一份基准（口径统一到 `build-sell-rule-baseline.mjs` 那套，历史长度门槛 `24×(窗口+14)` 小时也对齐了——参与基准的饰品集合一变，v2 那些阈值的依据就漂了）。**报告脚本一律只读**，缺哪天就提示去跑 builder。首次回填 1104 秒、111 天（04-18 ~ 08-06），过程踩的两个坑见踩坑 48。**跑出来的第一份卖后前向超额**（`report-shadow-sell-signals.mjs`）：`SELL` **2 条可评估、超额中位 −7.61%、2/2 判对**；`SELL_STRONG` **1 条、+1.49%、判错**（就是那笔 `MP9 | Hot Rod`，绝对收益 +47.54% 很漂亮，但卖了之后它又跑赢大盘 1.5%）；`HOLD` 611 条、+0.27%、53% 判对，**没有系统性判反**。**n=3，什么结论都下不了**——记在这里是因为这是这条链路第一次端到端跑通，不是因为数字有意义。其余 7 条卖出信号要到 8-14~8-20 才到 T+7。

**⑥ 异地备份断了 8 天，根因是仓库改名**（新踩坑 47）：计划任务的动作写死 `d:\cs2-skin-advisor\...`，仓库已经挪到 `d:\AI Solution\cs2-skin-advisor`，PowerShell 返回 **−196608**（`-File` 文件不存在，已复现），而这个码只记在 `Last Result` 里没人看。**已做**：今天的备份手动补拉、`pull-cloud-backup.ps1` 改造（成功/失败都写 `data/backups/pull-cloud-backup.log`；成功后把心跳 scp 回云端 `data/`，由 `/settings` 显示"异地备份最近成功于何时"——本机的死活云端看不见，只能本机主动上报）、`scripts/fix-backup-task.ps1`（**要管理员权限**，S4U 任务不提权改不了）。**新增第六节"仓库外配置清单"**，每条都写了"丢了怎么重建"。顺带给云端 Caddy 补了 access log（此前**根本没配**，所以"点了订阅但没记录"那次两侧都无痕可查）。
>
> **⑦ 上线后实地核对（2026-08-14 03:31 那一轮整点同步之后）**：`item_signal_summaries` 的 score 集合确实塌缩成 **{−30: 13, 0: 170, +30: 142}**，action 从 **HOLD 262 / TRIM 63** 变成 **HOLD 312 / TRIM 13**——**50 个饰品不再被建议减仓**（8-03 预览说 42 个，差的 8 个是这十天行情变化，方向一致）。部署后新开的 3 笔仓位 `entry_rule_version` 都是 `v2`（buy_score 全是 30），历史 279 笔是 `v1`。`health_signals` 也在记了：`last_push_attempt_at=2026-08-14T03:25`，**说明有预警触发并尝试推送，但订阅数是 0、一条都没送达**——这正是那块面板要暴露的东西。LLM 文案抽查通过，原文："RSI 超卖信号历史超额中位仅 0.61%，远低于 6.7% 的往返成本，不值得为此换手，HOLD 建议合理"，没有出现"存在反弹机会"那类讲法。
>
> **⑧ 又挖出两个静默故障，都已修（踩坑 50、51）**。**(a) AI 理由生成整条死了半个月**：NIM 把不带日期的模型别名 `deepseek-ai/deepseek-v4-flash` 下线，调用只回 HTTP 410，而页面表现是"AI 理由那块不显示"，`reason_cache` 最后一条停在 **7-31** 没人注意。改成 `deepseek-ai/deepseek-v4-flash-0731` 后恢复。**(b) `docker compose restart` 根本不重新读 `.env.local`**——改环境变量必须 `up -d`。**HANDOFF 里两处旧记录写的"改 env + restart 秒级生效"是错的**，`PAPER_TRADING_DISABLED`/`C5_FAST_SYNC_DISABLED` 那两个止血开关同样如此。
>
> **⑨ 我把生产采集器饿死了 40 分钟（踩坑 49），这条是运维上的硬约束**：一次补 7 个窗口的基准回填期间，`price_snapshots` **40 分钟一条没写**，两个定时同步全停，日志里既无错误也无例行输出；杀掉分析进程后下一秒就恢复。**"对副本跑"只解决写锁冲突、不解决磁盘争用**——改成对副本跑之后实测采集器照样交不出写入，`next-server` 停在 D 状态。加了 `--throttle-ms` 让路开关（sleep 用 `Atomics.wait` 不用忙等：单核机器上忙等是把"占磁盘"换成"占 CPU"），但**结论是这台 VM 上全表扫的分析和采集不能共存**。**以后重活的正确做法：在本机对下载下来的备份跑**（`data/backups/cloud-db-*.sqlite.gz`），算完只把结果那几百行 scp 上去合并（`scripts/merge-market-baseline.mjs`），云端零影响。
>
> **⑩ 撤回上面第 ⑤ 条里"HOLD 那行没有系统性判反"这个说法，它不构成任何证据**（项目所有者指出，这是这轮最重要的一条）。"卖出类命中率过半 + HOLD 没有系统性判反"原本是替换 v1 的**条件**，被读成了正面证据；而 611（现 628）条 HOLD 里绝大多数是 24h 涨幅不到 5% 的品，**v2 在那个区间根本不打算表态**（5~15% 是经济上不显著明确不触发，5% 以下连统计显著都没有）。所以"+0.27%、53% 判对"约等于"没被卖掉的东西平均跟大盘一样"——**那是市场的性质，不是规则的判断力**。**`n=611` 里 600 条是弃权票，信息量跟 `n=1` 没差多少**；这跟"`SELL_STRONG` 判错一笔不代表规则坏"是同一个错误的两面（一个拿噪音当反证，一个拿弃权当正证）。
> **`report-shadow-sell-signals.mjs` 已加 HOLD 按 24h 涨幅分档**，档位和回测那张表对齐、每档带上回测同档超额做对照，并把"弃权/判断/否决"三种性质标在行上。**2026-08-14 04:36 跑出来的第一份分档**（口径 7 天窗口、超额、已剔除未到期）：
>
> | 涨幅档 | 性质 | 可评估 | 超额中位数 | 为负占比 | 饰品数 | 回测同档 |
> |---|---|---|---|---|---|---|
> | 跌 | 弃权 | 421 | +0.53% | 43.0% | 159 | +0.20% |
> | 0~5% | 弃权 | 186 | −0.32% | 53.2% | 127 | +0.15% |
> | **5~10%** | **判断** | **19** | **−1.76%** | **68.4%** | 19 | −2.75% |
> | **10~15%** | **判断** | **2** | **−1.42%** | **100%** | 2 | −3.17% |
>
> **5~15% 合计 21 条、21 个饰品、超额中位 −1.76%、71.4% 为负**：方向跟回测一致（都是小负），幅度比回测还浅，**离 6.7% 的往返成本线差得很远 ⇒ "明确不触发"这个决定目前站得住**。但 **n=21 不下结论**，判据写进脚本了（可评估 ≥30 条且 ≥10 个饰品）。**≥15% 却仍 HOLD 的一条都没有**——也就是说洗盘否决条款至今零样本，它自己的成绩单还是空白，别把上面这行的"站得住"顺手安到它头上。8-14~8-20 那 7 条到期的卖出信号**不单独报，攒着一起看**。
>
> **⑪ 给 `market_baseline_daily` 上了三条护栏（迁移 024），因为它是"用今天的数据算出来、被三个脚本长期复用的中间产物"**——这类东西会**静默失配**，而库里别的表都是观测事实、不会。**(a) 口径必须记下来**：新增 `market_baseline_meta`，一版一行，存口径 JSON（参考平台优先级、历史长度门槛、每饰品快照下限、每天样本下限、中位数在偶数样本上的取法、定型等待）、生成它的脚本内容指纹、镜像 commit（Dockerfile 新增 `ARG GIT_COMMIT`，CI 传 `github.sha`——容器里没有 `.git`，不带进来就永远是 `unknown`）、算过哪些窗口、覆盖的日期区间、行数。三个评估脚本抬头都打一行 `baselineProvenance()`：**报告里出现的每个"超额"，都要能说清是哪一版基准算的**。**(b) 不能就地重算**：主键加进 `calc_version`，同一 (天,窗口) 的不同口径**并存**，改口径只能新写一版。`calc_version` 不是手写版本号，是那几个常量的**指纹**（当前 `b9645fa10`）——手写的迟早有人忘了改，指纹是改了常量就自动变，想覆盖也覆盖不了；`assertBaselineTable()` 发现"当前代码的指纹在表里没有"会**直接报错并说清这是口径失配不是数据没到**（不挡的话 `loadBaseline` 返回空 Map，报告只会说"去跑 builder"，把口径问题说成数据问题）。原来 `market-baseline-store.mjs` 里那句"改了口径必须先 `DELETE` 再重跑"**已经删掉**，那正是要禁的动作：一覆盖，8-13 以来所有引用这张表的结论就都没法复现了。**(c) 缺基准的剔除要留痕到"哪一天、哪个窗口"**：`report-paper-trades.mjs` 现在打印被剔除仓位**按持有窗口的分布**（不只是总数——缺口如果系统性地只落在长持有窗口上，剔除就从随机损耗变成选择偏差，看总数是看不出来的），`report-shadow-sell-signals.mjs` 打印缺的具体日期。**实测第一次就抓到一例**：影子报告里 9 条缺基准全部落在 **08-07 一天**，原因是那天的 7 天窗口要到 08-14 06:00 UTC 才定型——**缺口永远落在最新那一天，被剔掉的永远是最新鲜的样本**，这是系统性的、不是随机的，读数字时要知道。
>
> **⑫ `build-market-baseline.mjs` 的窗口参数有个静默 bug，已修**：没传 `--throttle-ms` 时 `indexOf` 返回 −1，过滤条件 `i !== throttleIdx + 1` 就变成了 `i !== 0`，**第一个窗口参数被吃掉**。`node scripts/build-market-baseline.mjs 8 9 10 11 12 13 16` 实际只算了 9 起的六个，而且照样打印"窗口：9, 10, …"、照样成功退出——**不核对抬头那一行根本发现不了**。发现方式就是核对抬头。顺带加了"有参数没被识别成窗口"的告警。**8-13 之前的所有回填不受影响**（那次是不带参数跑的默认 7 天）。
>
> **⑬ ⑩⑪⑫ 这三项改动截至写这行时还没部署，是有意等的**：等基准的多窗口回填和 19:55 那轮整点同步都落地，再一次性部署，免得部署把正在跑的东西打断、也免得出问题时分不清是哪一件引起的。**部署时按五项核对**（前四项沿用 8-14 那次的做法，第五项是新加的）：**①** 迁移 024 真的跑了——`market_baseline_daily` 带上了 `calc_version` 列、存量 111 行全部标成 `b9645fa10`、`market_baseline_meta` 里有对应那一行；**②** 影子和模拟盘两条链路在新镜像上继续在写（`shadow_sell_signals` 有新记录、`paper_trades` 计数在动），**这两段都包在 try/catch 里，静默失败是这个项目反复踩过的模式，只报"已部署"不算数**；**③** 一轮**完整的整点同步**在新镜像上跑过（`itemCount`/`snapshotCount`/`errors`）；**④** 容器和站点健康（`RestartCount=0`、内存、HTTPS 401 即 Basic Auth 正常）；**⑤【新增】把容器里手工拷进去的脚本清干净，容器和 git 之间不留私货**。**为什么加这条**：8-13 那轮为了跑回填，`build-market-baseline.mjs`、`market-baseline-store.mjs`、`copy-db-for-analysis.mjs`、`merge-market-baseline.mjs` 四个文件是 `docker cp` 进容器的（镜像是 19:23 构建的，比这些脚本早），容器里那份和 git 里那份没有任何东西保证一致。**这次是分析脚本、无害，但这个习惯留着迟早有一次不是无害的**——跑出来的数字到底是哪版代码算的会变成没法回答的问题（这也正是 ⑪ 要记 `script_sha` 的原因）。**换镜像 `up -d` 会重建容器、`/app` 整个来自镜像，手工拷的东西自然消失**，所以这一项主要是**部署后确认**：`docker exec <容器> ls -la /app/scripts` 的 mtime 应该全部等于镜像构建时间、没有例外，`/tmp` 里不该留 `analysis.sqlite*` 之类的残骸（8-14 04:36 已清掉一次，当时还剩着上一轮的 `-shm`/`-wal`）。宿主机 `~/cs2-skin-advisor` 是 git 工作树（`git status` 应干净），但宿主机 `/tmp` 下攒了一批历次临时脚本（`q1.mjs`~`q11.mjs`、`market-drift.js`、`list-scarce.mjs` 等）**顺手清掉**——留着只会让下一个人以为它们是流程的一部分。
>
> **⑭ 追"洗盘否决零样本"追出了一个更硬的结论：这条否决改不了任何决策，它的名字起错了**。起因是 ⑩ 里"≥15% 却仍 HOLD 的一条都没有"，本来要判断它是"罕见但合理"还是"实盘判定跟回测反推不一致"。**两个都不是**。查法分两步：**(a) 回测里这个组合并不罕见**——给 `build-sell-rule-baseline.mjs` 的洗盘两列补上样本数（此前只有中位数，**中位数背后是 3 个样本还是 3000 个样本读起来完全是两回事**，这一列早该有），714 个饰品重跑：≥15% 三档里洗盘样本 844 / 790 / 1613 条，**合计 3247 条、占该三档的 10.4%**，一点都不稀少。**(b) 那为什么实盘一次没有**——因为洗盘否决的生效条件是 `return24h < 5%`，而它唯一可能拦下的两个动作要求 `return24h ≥ 15%`，**两个区间不相交**。影子表里那个分支其实**执行过 4 次、3 个饰品**，但 24h 涨幅全部在 −15.6% ~ −0.2%，也就是全落在"不走这个分支也一样是 HOLD"的地方——**它改变的只有 `reason` 那句话，从来没有、也不可能否决掉一次卖出**。
> **所以它跟成交量死信号是同一族但不同因**：成交量项是喂错了数据导致**不可能**触发，这条是**触发了也不改变结果**。**结论是保留现状但改名**——它实际是一条 HOLD 的理由文案（对 LLM 说法有价值），不是一条经过验证的风控规则，别再当后者引用。**不要试图"让它变得有用"**：想让它真的能否决一次卖出，就得放宽到 ≥15% 档，而同一次重跑显示洗盘态在高涨幅档不但不保护反而更差（20~30%：−7.14% vs 非洗盘 −5.87%；>30%：−20.53% vs −18.58%），放宽等于亏更多。已加单测锁住"洗盘分支只改 reason、改不了 action"，并做了变异测试确认不是空绿（把 `WASHOUT_VETO_MAX_RETURN` 提到 0.2，正好这一条转红）。
> **顺带把 v2 的实盘覆盖率写清楚**（"已上线并行"这个说法给人的印象比事实大得多）：**七个档位里实盘只走到过两个**——`SELL_STRONG` 1 次（还判错了）、`SELL` 2 次可评估。10 笔平仓看着不少，但按"规则的哪条分支被验证过"数，是 1 和 2。
>
> **⑮ 把 ⑫ 那个参数 bug 当成一类问题处理，不是修一处**。它属于**参数解析层的静默失效**——不报错、不告警、结果看起来合理，跟踩坑 43（字段语义错配）、成交量死信号同族；而它被发现靠的是"人恰好核对了抬头那行"，**防线不能是人恰好看了输出**。**先自查**：`scripts/` 里只有 `list-scarce-anomalies.mjs` 有同形状的 `indexOf` 写法，但它用的是 `i >= 0` 保护，是对的，没有第二份复制品。**再统一**：新增 `scripts/script-args.mjs`，全部 24 个 `.mjs` 都从它过一遍，两条规矩——**① 不认识的参数直接报错退出（exit 1），不是告警**（告警会被几十行日志滚上去，而这批脚本动辄跑几十分钟，早退出的代价远小于跑完拿到一份不知道是错的的数字）；**② 抬头固定回显一行"我理解到的实参"**，跟基准的 provenance 那行并排——把上次那个偶然变成惯例。缺值（`--throttle-ms` 后面没跟数）、多余位置参数、值不是数字，全部硬失败。**顺带统一了第三件事**：同一个"这个脚本读哪个库"此前有四种写法（写死 `data/db.sqlite` / `process.argv[2]` / `CS2_DB_PATH` / 按 `import.meta.url` 算仓库根），**换个跑法就读到不同的库，而读错库跟参数被吃掉一样不会报错**；现在统一成 `resolveDbPath()`：显式位置参数 > `CS2_DB_PATH` > 仓库根的 `data/db.sqlite`（兜底那条按 `import.meta.url` 算，不用 `process.cwd()`）。
>
> **⑯ 多窗口回填跑完了，10 笔平仓的新口径超额是 +16.79%，跟旧的 +18.01% 差 1.22 个百分点**。回填按踩坑 49 的正解**在本机对 8-14 的云端备份跑**（云端零影响）：窗口 7/8/9/10/11/12/13/16 共 858 行，覆盖 04-18 ~ 08-06。**差 1.22pp 在"加注即可"的范围内**（判据是项目所有者定的：1~2pp 以内加注，差得多就把 8-13 之前引用临时口径的数字统一标废）——所以**旧数字不标废，但凡是 8-13 之前用临时口径算出来的超额，引用时都要注明"临时口径，物化表口径下约低 1.2pp"**。新口径全表：绝对收益中位 **+13.08%**、超额中位 **+16.79%**、为负占比 **0%**；分档 `sell_rule_v2` 9 笔 +15.31%、`sell_rule_v2_strong` 1 笔 +53.60%；集中度 10 笔 / 10 个饰品 / 5 个开仓日（最集中的 7-26 占 30%）。**缺基准 0 笔**——多窗口回填之前那 9 笔取不到基准的问题已经消失。
> **但有一条必须显式记下来，它比上面所有数字都重要**：**反事实对照 10 笔全部算不出（100%）**。"提前卖 vs 一直持到第 30 天"这个对照现在是**完全空白**的——原因是饰品移出观察池后就不再同步，第 30 天那个时点取不到价格。也就是说 `report-paper-trades.mjs` 五条口径里的第 ④ 条（当初专门设计来回答"提前卖值不值"的那条）目前一个样本都没有。**+16.79% 只说明"这 10 笔跑赢了大盘"，完全没有回答"v2 提前卖是不是比拿着强"**，别把前者当后者读。要修得让被平仓的饰品在平仓后再多同步 30 天，这件事还没做。
>
> 2026-08-03 之六（**新增 `HYPOTHESES.md` 特征假设库**，并按它的口径补了两件只读的活。
>
> **① `HYPOTHESES.md` 落地，它是外部概念进入这个项目的闸门**。要解决的问题是：股市技术分析、市场微观结构、行为金融、CS2 供给侧这些外部概念，此前没有统一入口——有价值的（洗盘剧本 → `drawdown48h`）和有毒的（"成交量异动"）走的是同一条路，全靠临时判断。现在按状态归档：🟢 已上线 / 🔵 已验证未上线 / 🟡 待验证 / ⚪ 数据不足 / 🔴 **已证伪** / ⚫ 不适用。**两条硬规矩**：概念只能当**假设来源**不能当**证据**，进 `lib/signals`、`lib/rules` 必须过第五节那七关；**已证伪条目永远不删**——它们防的是几个月后有人（包括 AI）冒出同样的直觉、再花一遍同样的时间（文档自己给的检验办法：想删的时候回想一下"价差收窄 = 吸货"这个直觉有多顺理成章，而它实测是 9/16、p=0.402）。**候选池里每条必须能填「⚠️ 前提条件」栏**，填不出来说明还没想清楚它在 CS2 里到底测的是什么，不许进——这一栏就是成交量那次栽的地方：导入词汇的同时默认导入了判据，但没有导入前提条件（股市"成交量"是流量、有真实撮合数据，CS2 只拿得到存量）。README 文档地图已加条目。
>
> **② 顺路补了第五节的第五关「效应量下限」**，是 8-03 之五那次复检逼出来的：`absReturn1h` **57/66、p≈0** 过了 Bonferroni，但各饰品 AUC 中位数只有 **0.531**，而生产在用的三个是 **0.744~0.819**。这不是"弱"，是**统计显著、效应量太小**，大概率是 `absReturn24h`（0.760）的低维投影而非独立信号。它跟第六关（阈值从回测反推）不重复：**第五关管筛选阶段的效应量，第六关管阈值阶段的经济显著**。
>
> 2026-08-03 之五（**清掉成交量死信号 + 模拟盘平仓改判 v2 + 补两个评估脚本**。四件事，前两件动了生产代码，后两件是只读脚本。
>
> **① 成交量死信号已清除（踩坑 43 的收尾）**。`lib/signals/volume.ts` 整个模块连同两个调用点（规则引擎打分、异常扫描的 `volume_ratio` 事件）全部删除。**删之前先证明它等价于 no-op**：用生产管线的完全相同口径（21 天窗口 → 按小时重采样 → `volume ?? 0` → window=7/threshold=2）逐个复算 325 个饰品的比值，**中位 1.0154、p75 1.0369、p95 1.0927、最大 1.2200，达到 2 倍阈值的 0 个、比值非有限的 0 个**——那个分支在当前数据下不可达，所以 score/action 逐个不变是数学结论不是抽样结论（这比"改动前后跑一遍 diff"强，后者会被同期行情漂移污染，没法区分"代码改动"和"价格变了"）。异常扫描那条通道也一并删了，**它的历史事件数是 0 条**（同期 `price_zscore` 2043 条、`manipulation_score` 83 条），所以 `volume_ratio` 从 `IAnomalyMetric` 里删掉没有遗留数据要兼容。`price_snapshots.volume` **列名没改**（改名要动迁移，破坏性大于收益），改成在 `lib/types.ts` 的列定义上写清"这是在售挂单数量（存量）不是成交量（流量）"和这次的实测数字——**注释要挡在代码里而不是只写在 HANDOFF 里**，不然下一个人还是会照名字用。另外给规则引擎补了一条锁"可达 score 集合"的单测：现在能产出的是 `-55/-30/-25/-15/0/5/15/30/45` 九个值，SELL（≤-40）只有 `-55`（超买+趋势走弱）这一条路。测试 171→169（删掉 volume 的 3 条、新增 1 条）。
>
> **② 模拟盘平仓判定换成卖出规则 v2**。平仓条件从"过 T+7 且 v1 出 SELL"改成"过 T+7 且 v2 出 SELL/SELL_STRONG"，30 天超时强平保留作兜底。**只改模拟账本——`/positions` 上给人看的建议仍然完全由 v1 出，一个字没动**。为什么必须在 8-21 之前做：`paper_trades` 现在 230 笔 open / 0 笔 closed，最老的 7-22 开仓、8-21 满 30 天，按原配置这 230 笔**注定全部以 `timeout` 收场**，而 timeout 的含义是"持满 30 天按当时价卖掉"，对"卖出规则准不准"**零信息量**——模拟盘存在的意义就是验证卖出规则，拿一个永不触发的规则去验证等于什么都没验。三个实现要点：v2 **一轮只判定一次**，结果同时喂给真实平仓和影子记录（`evaluateSellDecision`），两边各算各的话影子表和模拟盘流水就对不上、没法互相印证；`close_reason` 按档位分成 `sell_rule_v2_strong` / `sell_rule_v2` 不合并（回测里两档超额差一个数量级，-18.69% vs -4.35%~-5.89%，评估时必须拆得开）；超时和卖出档同时成立时记卖出档（否则会高估超时占比）。`sell_score` 存 `null` 不存 0。**原来那条锁"v2 说卖仓位必须还是 open"的单测是并行期语义，已改写成"影子记录和真实平仓必须同源"**。**做了双向变异测试确认新用例不是空绿**：阈值调低到 0.5% 有 4 条平仓用例转为失败，调高到不可达有 6 条转为失败。顺带加了 `PAPER_TRADING_DISABLED=1` 止血开关（做法同 `C5_FAST_SYNC_DISABLED`，改 `.env.local` + ~~`docker compose restart`~~ **`docker compose up -d`**，秒级生效不用 rebuild。**2026-08-14 更正：`restart` 不重新读 env 文件，必须 `up -d`，见踩坑 51**）。
>
> **③ 新增 `scripts/report-paper-trades.mjs`，刻意赶在有平仓数据之前写完**。7-30 那次归因是等数据摆在面前才现搭口径的，差点把大盘普跌当成策略失败——带着已经看到的数字定口径会不自觉地被结果牵着走。五条口径写死在脚本里：报超额不报绝对收益（基准 = 同开仓日、同持有天数的全市场中位数，跟 `build-sell-rule-baseline.mjs` 对齐）、先报集中度再报收益（不同饰品数/不同开仓日数/最大单日占比，让"独立赌注数"摆在明面上）、`stale_data` 整批剔除、**反事实对照**（每笔 v2 平掉的仓位同时算"持到第 30 天会是多少"——这个反事实能事后从 `price_snapshots` 直接算，**不需要真的留一组不平仓的对照组**；算不到的显式计数标出占比不静默丢弃）、样本不足时直接打印"不要下结论"（判据：可评估 <30 笔或开仓日 <5 天）。脚本第一个参数可指定库文件，**加这个是为了能先跑通**——真实平仓出现前整个主体是没执行过的代码，等 8-21 才发现写错就晚了；已用构造的 12 笔样本库走通全部分支，并在生产库上验过零平仓时的提前返回。
>
> **④ 新增 `scripts/recheck-degenerate-features.mjs`（只读），结论是负结论**。coMove 被"没区分度"跟踪四次后被推翻，但同样的方法还没系统套在别的旧结论上。复检 `absReturn1h`、`absZ`、`bidSpread`，**外加 `volumeRatio` 作阴性对照**——它已被两条独立路径确认是死信号，如果这三关把它也"救活"，说明方法本身在造信号、整份输出都不能信。**在三关之外补了第四道：效应量下限**。符号检验只回答方向是否一致，样本够多时 AUC 0.53 也能给出 p=0.0000，但那离能用差得远；取各饰品 AUC 中位数 ≥0.60（生产在用的三个是 0.744~0.819）。**结果：没有任何旧结论被推翻**——阴性对照 `volumeRatio` 仍不显著（17/37、p=0.7428、AUC 0.470）✅；`absReturn1h` 57/66、`absZ` 47/67，p 都过 Bonferroni α，**但 AUC 中位数只有 0.524~0.531，统计显著而效应量太小，旧结论方向没错**；`bidSpread` 3/9、p=0.5078，确认"别再试了"。**六个特征里 AUC 恰好等于 0.500 的饰品数全是 0**，也就是说这批不存在 coMove 那种退化稀释，第二关这次没有命中。**这条负结论本身值得记**：coMove 那次的反转是个例，不是"还有一堆被埋掉的特征"的信号，以后不必再靠直觉去翻旧账，要翻就跑这个脚本。过程中自己引入并抓到一个错：挂单类特征沿用了优先挑 C5 的参考平台，而 C5 直连接口不返回求购价，导致 `bidSpread` 拿到整列 null、报成"0 个可检验饰品"——已改成按求购数据最多的平台选。
>
> **本次没做的**：没碰 `manipulation-score.ts` 的任何特征和阈值，没碰 SELL 阈值（-40 保持不动，理由见踩坑 43：正确的做法不是修 v1 的卖出侧而是承认它修不好，让有回测依据的 v2 接手），没碰 `ENTRY_MIN_SCORE`，没碰 `group_linkage` 的观察期设置，没审任何异常事件。
>
> **部署后已手动触发一轮完整同步实地验证**（不等整点。影子记录那段包在 try/catch 里，静默失败是这个项目反复踩过的模式，只报"已部署"不算数）：`{"itemCount":325,"snapshotCount":2532,"errors":[],"anomaliesDetected":1,"paperTradesOpened":1,"paperTradesClosed":0}`，容器 `RestartCount=0`、内存 131MiB/954MiB、站点 HTTPS 401（Basic Auth 正常）。四项落地核对：**①** `item_signal_summaries` 325 行全部重算（`computed_at` 刷到 05:50:43），**score 取值集合仍是 `-30/-25/-15/0/5/15/30`、SELL 仍然 0 个**——删掉成交量项没有引入任何新分值；**②** 新数据上重算成交量比值，**最大仍是 1.2200、达标 0 个**，等价性在新快照上继续成立；**③** 影子表**落了新记录**（127→128 条，最新 05:49:02），说明 v2 判定和影子记录这条路在新代码上真的跑了，不是静默失败——只 +1 条是对的，24 小时去重把其余 127 笔压住了；**④** 0 平仓**已核实是条件没满足不是 bug**：231 笔 open 里 128 笔过了 T+7（这批才可能被平），**这 128 笔的 24h 涨幅最大只有 3.57%**（`★ Hand Wraps | Overprint (MW)`），≥15% 的 0 笔、满 30 天的 0 笔。
>
> **顺带查清一个看着像 bug 的现象**：全库当前最大 24h 涨幅其实有 **121.84%**（`★ Moto Gloves | Cool Mint (FN)`，8-02 从 13468 → 17492 → 30000，是真实价格跳变不是毛刺），够到强卖出档却没触发任何平仓——原因是**这个饰品根本没有 paper_trades 记录**，它不在模拟盘里。**这条记下来是因为它正是"全库最大涨幅"和"可平仓仓位里的最大涨幅"两个数不能混用的例子**：核对零平仓要看后者，看前者会得出"够到阈值却没平，代码坏了"的错误结论。
>
> **回来看什么**：**8-21 前后**跑 `node scripts/report-paper-trades.mjs`——那是最老仓位满 30 天的日子，也是第一批平仓记录必然出现的时候。在那之前如果一笔都没平，**先别怀疑代码**：v2 要 24h 涨幅 ≥15%，回测里这三档合计只占全部饰品-小时的 **2.7%**。核对办法见上面第 ④ 条：查**已过 T+7 的开仓**那批的 24h 涨幅，不是查全库。）
>
> 2026-08-03 之四（**按项目所有者的两个决定落地：联动分组写入 + 卖出规则 v2 影子并行**。两件都做完并部署了，但**两件都处在"观察期"，都还没有最终结论**，接手的人主要看这一节最后的"什么时候回来看什么"。
>
> **① 联动分组已写入生产（42 行）**。印花按赛事胶囊、探员按所属组织，落进 `item_metadata.collection`，新增 `collection_source` 列区分 `official`/`derived`（迁移 019）。**官方收藏品 703 条一条没动**，脚本只碰 `collection IS NULL` 的行，撤销也只清 `derived`（`scripts/backfill-linkage-groups.mjs --revert --apply`）。分组结果：Budapest 2025 印花 10 张、Austin 2025 印花 7 张、Antwerp 2022 印花 5 张，共 16 组、其中 7 组有 ≥2 个饰品（只有这 7 组会真正产生联动信号）。**预警走新的 `group_linkage` 类型而不是复用 `collection_linkage`**——胶囊/组织是平级同涨同跌，没有"上级拉升→下级炼金料跟涨"那种层级链条（这两类饰品本来也没有 `rarity_rank`），而且分开才数得出这批预警的质量。**观察期内只入库、不推送**，`/anomalies` 页面用同色系更淡一档的标签展示。**演练时抓到一个真错误**：`StatTrak™ Music Kit | 艺人, 曲名`、`Charm | 名字` 这类同样是"两段式且不带磨损后缀"，被误判成了探员组织——已在 `lib/item-metadata-groups.ts` 加品类排除名单并补测试（剥掉 StatTrak™/Souvenir 前缀再判品类，否则漏过）。
>
> **② 卖出规则 v2 已上线并行记录，但不参与任何真实决策**。口径是项目所有者定的：只用回测验证过的信号做核心权重、阈值必须能从回测反推、先并行跑至少一轮拿到触发次数和假信号率再谈替换。**新增 `scripts/build-sell-rule-baseline.mjs` 反推阈值**（714 个饰品、99 天大盘基准），口径是"未来 7 天收益 − 当天全市场中位数"的**超额**：
>
> | 24h涨幅档 | 超额中位数 | 为负占比 | 有该档的饰品数/中位数为负的 | 符号检验 p |
> |---|---|---|---|---|
> | 跌 / 0~5% | +0.20% / +0.15% | 48.5% | 714 / 307、360 | 0.9999 / 0.4258（无信号）|
> | 5~10% | −2.75% | 63.2% | 638 / 495 | 0.0000 |
> | 10~15% | −3.17% | 59.3% | 447 / 293 | 0.0000 |
> | 15~20% | −4.35% | 61.4% | 310 / 198 | 0.0000 |
> | 20~30% | −5.89% | 61.5% | 258 / 161 | 0.0000 |
> | **>30%** | **−18.69%** | **71.4%** | 203 / 155 | 0.0000 |
>
> **规则据此定成**：>30% 强卖出（整张表唯一的台阶，陡一个数量级；71.4% 这个数字跟 REPORT-t7 独立算出的"70.8% 概率为负"几乎一致，两条路径互相印证）、15~30% 普通卖出（−4.35%~−5.89%，扣掉 C5 1% 手续费仍有余量，且 15% 正好是 REPORT-t7 已验证的追涨阈值）、**5~15% 明确不触发**（超额只有 −3% 上下，扣手续费和买卖价差后所剩无几——**数据上显著 p=0.0000 但经济上不显著，是两回事**）、**深回撤且低涨幅时明确不卖**（洗盘态超额反而是 +3.69%/+2.09%，跟洗盘指纹和两版预测基线里 drawdown48h 是最大正权重特征三处一致；注意这个否决只在低涨幅档成立，20~30% 档在洗盘时超额 −7.03% 比不洗盘还差）。**RSI/均线/成交量三项没有任何回测支撑，按既定口径不进规则**。
>
> **并行机制**：`lib/rules/sell-rule-v2.ts`（纯函数，10 条单测）+ `shadow_sell_signals` 表（迁移 020）+ 模拟盘每轮记一条影子判断，**真实平仓仍然完全由 v1 决定**（有一条单测专门锁这个：v2 说 SELL_STRONG 时仓位必须还是 open）。**记录按动作变化去重**：不去重的话 119 笔过 T+7 的仓位每小时各记一条 = 每天 2856 行，而"持续 5 小时的卖出信号是一次信号不是五次"，触发次数这个指标会直接失真；现在是动作变了就记、没变则每天补一条。评估脚本 `scripts/report-shadow-sell-signals.mjs` 出触发次数和命中率（假信号率 = 1 − 命中率），**同样用超额口径**——这段时间大盘一直在跌，不扣基准的话什么都"卖对了"（7-30 模拟盘归因那次就是这么被误导的）。
>
> **③ 部署预热的第一个版本是坏的，已修**。上线那轮日志里四个页面全是 `000|0.000000s`——看着像超时，其实是**连接被拒**：`docker compose up -d` 一返回就开始发请求，而 Node 还要几秒才 listen。已加就绪等待循环（最多 60 秒），并且**要带 Basic Auth 凭证**，否则拿到 401、一行数据库都没读到等于没预热。
>
> **什么时候回来看什么（两件都有明确判据，别凭感觉）**：
> - **`group_linkage` 预警质量小结**：建议 **2026-08-10 前后**（攒一到两周）统计触发次数、其中多少真正值得看、密集程度（会不会一天几十条），**用这份数据决定要不要开推送**。查法：`SELECT COUNT(*), COUNT(DISTINCT item_name) FROM anomaly_events WHERE metric='group_linkage'` 加按天分布。开推送的做法是在 `lib/anomaly-scan.ts` 那段 `group_linkage` 循环里补 `pushNotifications.push(...)`。
> **部署后已手动触发一轮完整同步实地验证**（不等整点，因为影子记录那段包了 try/catch，静默失败正是当天在单测里掩盖过问题的同一种模式，不能只报"已部署"）：同步 325 个饰品/2532 条快照/0 错误，**影子表落了 127 条记录**（127 笔过 T+7 的仓位各一条），字段和文案都对（`24h 涨幅 3.6%，没到 15% 的卖出档`）。**127 条全是 HOLD**——当前追踪的品里 24h 最大涨幅只有 3.57%，离 15% 还很远。**这跟 v1 的零触发不是一回事，别混为一谈**：v1 是**结构上够不到阈值**（全库最负 score −30 vs 门槛 −40，怎么等都不会触发），v2 是**条件确实没满足**（有品涨过 15% 就会触发）。`group_linkage` 也是 0 条，因为这一轮压根没检测到任何异动（`anomaliesDetected: 0`），不是分组没生效。
>
> **对攒样本速度的预期（别等急了就下调阈值）**：回测里 ≥15% 的三档合计约占全部饰品-小时的 **2.7%**，按现在 127 笔仓位估算，每天大概只有个位数的卖出信号，**攒到三位数要几周**。这个慢是数据本身决定的，不是规则写坏了。
>
> - **影子信号成绩单**：跑 `node scripts/report-shadow-sell-signals.mjs`。**样本量不够时不要下结论**——回测里 15~20% 档的为负占比也才 61%，几十条样本的随机波动完全能盖过这个幅度，至少攒到三位数。要替换 v1 需要看到：卖出类命中率明显过半、超额中位数为负、且 HOLD 那行没有系统性判反。)
>
> 2026-08-03 之三（**把新的检验方法回头套在旧特征上，推翻了一条跟踪四次的结论：coMove 一直是有效特征**。做完求购深度那版之后，意识到 `analyze-manipulation-features.mjs` 用的是同一种会虚高的池化口径，于是给它**追加**了一节按饰品检验（主表原样保留，保住跨次可比性）。结果分两半：
>
> **① 三个生产特征在更严的检验下不但没垮，反而更强**——`vol24h` **67/67 个饰品同方向**、各饰品 AUC 中位数 **0.819**（池化只有 0.751）；`absReturn24h` 67/67、中位 **0.760**；`maDev` 66/67、中位 **0.744**，p 全部≈0。`lib/signals/manipulation-score.ts` 建立在这三个特征上，**这个地基是稳的**，不用动。弱的两个也确认了：`absZ` 42/67、p=0.0249（勉强），`volumeRatio` **29/67、p=0.889，没有信号**——跟同一天查出来的"规则引擎成交量项是死信号"是同一个根因（`price_snapshots.volume` 是在售量这个**存量**，小时尺度上几乎不动，中位比值恰好 1.0000），两处独立证据互相印证。
>
> **② coMove 的"没区分度"是统计假象，纠正之**。第一版按饰品检验显示 coMove 32/67、中位 AUC 恰好 0.500、p=0.687，看起来彻底没信号——**差点就这么报出去了**。停下来查了一步：**全是并列值时 AUC 会机械地等于 0.5**。一查果然——67 个有标记的饰品里 **34 个的 `item_metadata.collection` 是空的**（23 张印花 + 10 个探员 + 1 个皮肤），对它们 coMove 恒为 0。**剔除这类退化样本后结论完全反转**：coMove **32/32 同方向**、中位 AUC **0.613**；coMove24h **32/32**、中位 **0.749**——跟 maDev 同级，而且是独立于价格的信息源。**项目从首次校准以来一直记着的"coMove 卡在 0.500~0.520、远没到能用"是错的**，那个数字是被一半根本不适用的饰品稀释出来的。已同步改掉 HANDOFF 第三节第 2 条和第四节第 5 条这两处旧结论。
>
> **顺着往下挖了一步（这是可行动的部分）**：印花和探员没有收藏品**是数据正确不是缺失**，但它们有天然等价物——印花属于同一赛事胶囊、探员属于同一组织。新增只读脚本 `scripts/analyze-group-comove.mjs` 验证：按胶囊/组织分组后 **25/26 个饰品同方向、24 小时联动中位 AUC 0.651**，符号检验 **p≈4×10⁻⁷**；最强的是 Budapest 2025 / Antwerp 2022 的 Holo 印花（0.68~0.81）和 `Little Kev | The Professionals`（0.844）。**要不要把这个分组写进生产的 `item_metadata.collection` 是待决定项**（会立刻改变线上联动预警和推送量），细节和取舍写在第四节第 5 条。
>
> **方法学结论，比任何单个数字都重要**：这一天连着踩到**三种**会让统计量骗人的情况——池化掩盖自相关（求购深度那版）、退化样本稀释命中率（coMove 这版）、绝对水平混入饰品间差异（`bidAskRatio` 那版）。**共同点是三次都让"看数字"得出与真相相反的结论，而且都不会报错**。以后任何"某特征没区分度"的结论，下判断前必须先过这三关：样本是不是自相关的？有多少样本对这个特征根本是退化的？这个特征的量纲是不是跟标的规模有关？)
>
> 2026-08-03 之二（**求购深度第一次验证：拿到了这个项目至今第一个统计上站得住的非价格特征**，详见新增的 `REPORT-bidding-depth-features.md`。这是 HANDOFF 四.2 挂了半个月的那件事——"求购深度数据已开始积累，等攒够几周再重跑，缺的是时间"，7-20 起攒，现在两周，够跑第一版了。**新增 `scripts/analyze-bidding-depth-features.mjs`**（独立脚本，没动 `analyze-manipulation-features.mjs`——那个的 AUC 数字被跨多次运行对比着，往里加特征会破坏可比性）。
>
> **结论**：`bidRatio`（求购挂单数 / 自身 168 小时均值）在 16 个可检验饰品里 **15 个**呈现同方向，符号检验 **p=0.0003**，各饰品 AUC 中位数 **0.747**；`bidAskRatioRel`（买卖盘厚度比/自身基线）14/16，p=0.0021。**6 个特征做 Bonferroni 校正（α≈0.0083）后这两个都过得了**。作为对照，另一个非价格特征 coMove 从首版到现在四次复跑一直卡在 0.500→0.507→0.518→0.520。
>
> **三条值得记住的**：①**有效的是"相对自身基线"，绝对水平会骗人**——`bidAskRatio`（绝对厚度比）池化 AUC 最高（0.652）、各饰品 AUC 中位数也最高（0.748），**但只有 10/16 同方向、p=0.227 不显著**，因为绝对水平被"贵价品/廉价品挂单簿厚度天差地别"这个饰品间差异污染了，池化统计量把选择效应当成了信号。以后加任何跟饰品规模有关量纲的特征，都要先对自身基线归一化。②**外部事件的方向是反的，这可能打在最硬的卡点上**——三类的 `bidRatio` 中位数是 **操盘期 1.0824 > 平时 0.9802 > 外部事件 0.8022**，外部事件期间求购盘反而更薄。价格特征分不开"操盘 vs 外部事件"（这正是 C1/C2 预测模型上不了线的核心原因，见 REPORT-prediction-baseline.md），但挂单簿的需求侧天然带着"谁在买"的信息。**但 external 只有 48 条小时样本、凑不出按饰品检验，现在只是线索不是结论**，等 `external` 标注攒够再复核——**这是三条里最有价值的一条**。③**买卖价差没有信号**（9/16、p=0.402），"价差收窄=有人吸货"这个直觉实测不支持，记下来免得再试。
>
> **方法学上的一个修正，比结论本身更值得记**：第一版只算了池化 AUC（所有饰品的小时样本倒一个池子），数字是 0.609/0.652 那一档。**这个数不能直接信**——同一个饰品的一波行情贡献几百条高度自相关的小时样本，名义 916 条正类的有效样本量远小于此。改成"按饰品各自算 AUC + 对命中饰品数做符号检验"之后才发现：真正的样本量是 **16 个饰品**，而且六个特征里哪两个能立住跟池化排序**不一样**。`analyze-manipulation-features.mjs` 用的是同一种池化口径，**它历史上那些 AUC 数字（vol24h 0.710 等）大概率也偏高**，以后要用来做上线决策时得先补同样的按饰品检验（这次没回头改那个脚本，因为会破坏跨次可比性）。
>
> **没做**：没有改任何生产代码，`manipulation-score.ts` 的特征和阈值一个都没动——16 个饰品不足以校准阈值。求购数据也永远补不了历史（K 线不带量），184 条操盘标记里绝大多数窗口在 7-20 之前，这个特征的样本只能往后攒。建议 **4-6 周后重跑**，看可检验饰品数涨到多少、p 值稳不稳。)
>
> 2026-08-03（**模拟盘卖出侧查明：不是没跑，是结构上永远触发不了——"成交量异动"这一项被喂了在售量（存量）而不是成交量（流量），SELL 阈值因此够不着**。查这件事的起因是 7-30 记录里那句"卖出侧逻辑到现在一次都没运行过"，修复已经部署三天，回头验收。**现状**：`paper_trades` **230 笔 open、0 笔 closed**，其中 **119 笔已经过了 T+7 可以平仓**；开仓在继续（153→230）说明 `runPaperTradingTick` 每小时确实在跑、平仓循环也确实在跑，**只是从来没触发过**。
>
> **根因链条（每一环都有实测数字，不是推断）**：
>
> ① **把 325 个饰品当前的 score 按规则权重反解**，能看到规则引擎实际只产出这 8 个值：`-30`(10个)=RSI超买单独、`-25`(52)=趋势走弱单独、`-15`(11)=超买+趋势走强、`0`(110)=无信号、`+5`(71)=超卖+趋势走弱、`+15`(17)=趋势走强单独、`+30`(52)=超卖单独、`+45`(2)=超卖+趋势走强。**注意这里面缺了什么**：`-55`（超买+趋势走弱）**0 个**——这两个状态几乎互斥（RSI 超买意味着刚急涨，趋势走弱意味着价格在 MA7 之下且 MA7 在 MA30 之下），实测 325 个饰品里一个同时满足的都没有。而 SELL 门槛是 **score ≤ -40**，**全库最负的 score 是 -30，差 10 分**。
>
> ② **本来有第三条路能凑到 -40**：成交量异动这一项（`score<0` 时再 -15），`-25-15` 或 `-30-15` 都够。**但它从来不触发**。`lib/signals/volume.ts` 的判据是"最新一小时的量 ≥ 前 7 小时均值的 2 倍"，而喂进去的 `price_snapshots.volume` 来自 SteamDT 的 `sellCount` / C5 K线的 `entry.count`——**那是在售挂单数量（存量），不是成交量（流量）**。存量在小时尺度上根本不会翻倍。**实测 325 个饰品的这个比值**：中位数 **1.000**、75% 位 1.012、95% 位 1.038、99% 位 1.086、**最大 1.167**，达到 2 倍阈值的 **0 个**，连偏离 1.0 超过 10% 的都只有 5 个。**这一项是事实上的死代码**，而且死得完全静默——它不报错、不警告，只是永远返回一个约等于 1 的比值。
>
> ③ 于是 **SELL 这个 action 在当前数据下不可达**，`item_signal_summaries` 里 325 个饰品的 action 只有 HOLD/TRIM/WATCH，SELL **0 个**。模拟盘的平仓条件是"过了 T+7 且出 SELL 信号，或持有满 30 天强平"，前者不可达，**所以 230 笔仓位注定全部走 30 天超时平仓这条路，最早从 8-21 开始**（最老的仓位现在持有 11 天）。
>
> **为什么这件事重要**：模拟盘存在的意义是**验证规则引擎准不准**（PLAN.md 阶段 C 的前置）。但按现在这个配置，将来所有平仓记录的 `close_reason` 都会是 `timeout`，含义是"持满 30 天然后按当时价格卖掉"——**这批数据对"卖出规则准不准"零信息量**，因为卖出规则从来没参与过决策。7-30 那次归因把零平仓归给了崩溃三天，**那个解释不完整**：崩溃修好了，零平仓仍然是零，真正的原因在这里。
>
> **需要项目所有者判断的地方（我没有擅自改，理由见下）**：这三个可选项都是策略决策，不是 bug 修复——规则引擎的权重和阈值按 CLAUDE.md/PLAN.md 的既定原则应该由真实标注校准，不该由我拍脑袋改，而且它直接影响你看到的买卖建议：**(a) 把成交量那一项重新定义成适配存量的信号**（比如"在售量 24 小时变化超过 ±10%"，实测这个量级是有区分度的：99% 位 1.086，10% 就在分布尾部）；**(b) 直接砍掉成交量这一项**，承认没有流量数据（SteamDT/C5 都不返回真实成交笔数，K 线也不带量，见踩坑 6），并把 SELL 阈值从 -40 下调到 -25 或 -30 让"趋势走弱"或"RSI 超买"单独就能触发；**(c) 什么都不改**，接受模拟盘只验证买入侧、卖出侧等以后有更好的数据源再说。**注意 (a) 和 (b) 都会立刻改变 /positions 页面上的买卖建议**，不只是影响模拟盘。
>
> 本次只读了数据库，没有改任何生产代码、没有动任何标注。**顺带排除的一个误报**：查覆盖率时看到最近 7 天还有 STEAM/HALOSKINS 快照，一度以为 7-27 那次"停止收集"回归了，按天拆开后确认最后一条写在 **2026-07-27 05:27**（正是排除名单部署的那一刻），只是 7 天窗口刚好切进去了，没有回归。)
>
> 2026-08-02（**部署方式重做：构建搬去 GitHub Actions，云端只 pull + 重启**。这是 7-31 记录第④条点名的"仍是未解决的真问题"，也是踩坑 40 的正解。**问题**：1 核 1GB 的 VM 上 `docker compose up -d --build` 期间 CPU 被吃满，站点整个构建期（4-8 分钟起，撞上崩溃循环时到过 60+ 分钟）白屏；7-31 试过的替代方案"构建前先停容器"构建确实快很多，但站点同样完全不可访问——**两种方式都不可接受，因为问题不在快慢，在于构建和服务抢的是同一颗 CPU**。**改法**：`.github/workflows/build-image.yml` 在 GitHub Actions 上构建镜像 → 冒烟测试通过才推到 GHCR → 云端 `./scripts/deploy-cloud.sh` 只做 `git pull` + `docker compose pull` + `up -d`，停机窗口变成换容器的几秒。`docker-compose.yml` 里**故意删掉了 `build:` 键**，这样误敲 `up -d --build` 也不会在生产机上开始编译。
>
> **顺带做的三件事，每件都是这次改动逼出来的，不是顺手加功能**：
>
> **① 镜像切到 `output: "standalone"`**。原来的 Dockerfile 是 `COPY --from=builder /app ./`——整个工作目录（含 dev 依赖、源码、`.next` 全量）一层塞进去，**这一层每次构建都变，云端每次部署要重新拉整层**。standalone 只带 server.js + 追踪出来的那部分 node_modules（实测 node_modules 从几百 MB 降到 **23MB**），部署拉取量和这台小机器的网络/磁盘压力都小一个量级。**standalone 不会自动带的四样已在 Dockerfile 里显式 COPY**：`.next/static`、`public`、`db/`（迁移是进程启动时按 `process.cwd()/db/migrations` 读的，不带就起不来）、`scripts/`（一次性分析脚本靠 `docker compose exec` 在容器里跑，HANDOFF 里多处流程依赖它）。**本机已实测**：把 standalone 产物配一个**空的** data 目录跑起来，`/positions`、`/settings`、`/watchlist` 全部 200，data/ 下自动建出 db.sqlite + 迁移跑完——也就是说原生模块加载、迁移读取这两个 standalone 最容易漏的点都验过了，CI 里也把这一步固化成了推镜像前的硬门槛。
>
> **② 修了一个"搬构建就会静默坏掉"的东西：`NEXT_PUBLIC_VAPID_PUBLIC_KEY`**（详见新增的踩坑 41）。这类变量是**构建期内联**的，CI 上没有 `.env.local` → 内联成空字符串 → 客户端订阅和**服务端发推送**两条路一起坏，且**没有任何报错**。已改名成不带前缀的 `VAPID_PUBLIC_KEY`（服务端运行时读），浏览器那份由 `/settings` 页请求时读出来当 prop 传下去，并给该页加了 `force-dynamic`（否则静态预渲染又把"请求时读"变回"构建时读"，踩坑 11）。**服务器 `.env.local` 已同步改名并留了备份 `.env.local.bak-20260802`**，本机 `.env.local`、`.env.local.example`、CLAUDE.md 都已跟上。**本机实测同一份构建产物**：不带环境变量启动时 HTML 里没有公钥，带上就出现了，确认耦合真的解除了。
>
> **③ 加了 `.dockerignore`（此前没有）**。原来 `COPY . .` 是把服务器工作目录整个拷进镜像的——也就是说**现在跑着的镜像里就烤着一份 `.env.local`（全部 API key、Basic Auth 密码、VAPID 私钥）和一份真实的 `data/db.sqlite`**。之前镜像只存在于那台机器上、没往任何仓库推，所以没造成实际泄露；但**这次要往镜像仓库推，不堵上就是直接把持仓成本价和密钥发出去**。另外发现 Next 的文件追踪会主动顺着 `path.join(process.cwd(), "data", "db.sqlite")` 把数据库拖进 standalone 产物（本机实测 240MB，见踩坑 42），所以防线做了三层：`.dockerignore` 排除源文件、Dockerfile 里 `RUN rm -rf ./data` 保证确定性、CI 里一步硬检查（镜像里出现 `.env*` 或 `data/db.sqlite` 就让构建失败）。
>
> **首次切换已完成并验证，不需要任何手动配置**。原本担心 GHCR 的 package 默认 private、云端 `docker compose pull` 会 403，实测**由 Actions 用 `GITHUB_TOKEN` 推出来的 package 直接继承了仓库的公开可见性**（匿名 token 就能拉到 manifest），所以云端不用 `docker login`、不用存任何凭证。**验收数字（首次真实部署）**：整个 `deploy-cloud.sh` **47 秒**（含 git pull + 拉镜像 + 换容器），对比之前就地构建的 4-8 分钟起、且全程站点不可访问；**镜像 2.09GB → 387MB（−81%）**；容器 `RestartCount=0`、内存 64MiB/954MiB、`✓ Ready`，站点 HTTPS 返回 401（Basic Auth 正常要求认证）；`VAPID_PUBLIC_KEY` 已注入容器（长度 87 正确），调度器两个定时器都挂上了，且启动补跑判断打印的是 `上次完整同步是 45 分钟前`——7-31 那个修复在新镜像上依然生效。**旧的本地构建镜像 `cs2-skin-advisor-app:latest`（2.09GB）故意没删**，留作回滚退路；确认稳定后可以 `sudo docker rmi cs2-skin-advisor-app:latest` 回收 2GB。**用服务器自己的凭证做过真实数据库下的端到端验证**：`/positions` `/watchlist` `/anomalies` `/paper` `/ledger` `/settings` 六个页面全部 200，且 `/settings` 返回的 HTML 里确实带着 `VAPID_PUBLIC_KEY` 的值（证明运行时读取在生产也成立，不是只在本机成立）。
>
> **新部署方式的残余代价（不是 bug，但要知道）**：换容器之后**第一次访问是冷的**——重启丢掉的不只是进程内缓存，还有 240MB 数据库在系统页缓存里的那份。实测换容器后首次 `/paper` **136 秒**（它是最重的页：153 笔 open 仓逐笔取信号快照），同一页 warm 之后是 2.27s → 0.82s → **0.32s**，`/positions` warm 0.81s。这跟 7-31 记录里"`/positions` 冷启动首请求 30.7s"是同一个现象，**不是这次改动引入的**（这次没碰任何查询路径）。**账要这么算**：旧方式是站点连续几分钟到几十分钟完全不可访问，新方式是几秒内恢复服务、代价是第一个访问的人要等一会儿——量级上仍然是压倒性的改善。真要连这个也去掉，得在部署后自己先打一遍主要页面预热。**2026-08-03 已补上并验证**：`deploy-cloud.sh` 换完容器后自己带凭证打一遍 `/positions` `/watchlist` `/paper` `/anomalies` `/ledger`（不带 Basic Auth 凭证会拿到 401、一行数据库都没读到，等于没预热）。实测预热轮 `/paper` 花了 **96.7 秒**、`/positions` 3.3 秒，之后 `/paper` **0.21s**、`/positions` 0.68s——**这段等待现在由部署脚本吃掉，不再落在下一个打开页面的人头上**，代价是部署总耗时从 47 秒变成约 2 分 13 秒（没人在等的那两分钟）。**第一次上线时踩到一个小坑**：那一轮部署没有预热输出，因为脚本自身的更新要下一次才生效（函数定义在 `git pull` 之前就加载完了），已在脚本注释里写明。
>
> **没做的**：`/watchlist` 页面 1.69MB 那件事仍然没做（7-31 记录第③条，大头是表格标记本身不是走势图，要动分页/虚拟滚动，属于另一件事）。)
>
> 2026-07-31 深夜二（**队列清零 + 调度器停摆 bug + 性能修复验收数字**。
>
> **① 待审核队列 1442 条已由项目所有者全部审完，清零**。去向核对过：`dismissed` 1420（其中 `price_zscore` 1381）、`confirmed` 33（合并成 **5 条新操盘标记**，`manipulation_tags` 179→**184**）、`external` **0 条**——**没有任何一条被标成外部事件，跟当天早些时候第 2 步的结论完全一致**（该标的 55 条纪念品/印花早就标完了，剩下的本来就没有该标的）。**41 条稀缺类型全部处理完**（`manipulation_score` 33、`momentum_chase` 6、`washout_signal` 4），这是这次审核真正的价值所在。**一个要留给将来的提醒**：1420 条 dismissed 的 `review_note` 是空的，且是靠"按饰品整批确认"（`scope=item`）快速处理的，**跟之前 131 条逐条判断的 dismissed 混在一张表里分不出来**。按踩坑 34，`dismissed` 现在没有任何脚本读、对当前训练零影响；但迁移 007 写明将来要拿它当负样本——**到那天要意识到这 1420 条的标注质量低于早期那 131 条**，别当成等价样本用（这跟当初特意新开 `archived` 而不复用 `dismissed` 是同一个考虑，只是这次没有机会提前分开）。
>
> **② 发现并修复"每次部署后完整同步停摆最多一小时"（commit `1872adf`）**。核对队列时发现 `item_signal_summaries.computed_at` 停在 17:01 而当时已经 19:08，查日志看到 `[price-sync] 上次同步是 4 分钟前，跳过启动补跑`。**根因**：`lib/scheduler.ts` 判断"距离上次同步多久"用的是 `getLatestSnapshotTime()`（`price_snapshots` 的最新写入时间），而 **C5 高频 tick 每 10 分钟就写一次快照**，这个时间永远是新的 → 启动补跑**永远不触发**；而整点定时器又是从进程启动时刻重新计时的，所以每部署一次，异常扫描/模拟盘/信号预计算就被推迟最多一小时（当天部署两次，完整同步从 17:01 一直停到 19:28）。**这是当天第三次遇到"C5 高频 tick 让别处的假设失效"**（另两次：`recent_prices` 涨到 831 点、7-27 那次 OOM）——**加高频写入这种改动的影响面远超"多写几行数据"，凡是隐含"数据多久变一次"的逻辑都要一起检查**。**修法**：判断依据改成 `getLastFullSyncTime()`（读 `item_signal_summaries.computed_at`）——`precomputeSignalSummaries` 是 `lib/sync.ts` 流水线的最后一步，它写了就说明整轮跑完了，中途崩掉不会更新、下次启动正好会补跑。顺带删掉已无调用方的 `getLatestSnapshotTime`（避免以后有人拿它把 bug 重新引入）。**部署后已验证**：日志变成 `上次完整同步是 40 分钟前，跳过启动补跑`——报的是真实的完整同步年龄而不再是被 C5 顶出来的几分钟，逻辑正确。
>
> **③ 性能修复的验收数字（前 → 后）**：`recent_prices` 全表 **1,684,442 → 337,221 字节（−80%）**，单品均值 **831 → 166 点**，最大 890 → 168；`/positions` HTML **1,522,104 → 681,834 字节（−55%）**，单个走势图 `points` 属性 **7951 → 753 字节（−90%）**，热请求 TTFB 0.69~1.39s → **0.28~0.91s**；`/anomalies` TTFB **0.02~0.13s**（队列清零后 HTML 只剩 14.9KB）。**剩下的没做**：`/watchlist` 仍有 **1,688,139 字节**（282 行 × 约 6KB），**大头已经不是走势图而是每行的表格标记本身**（`/positions` 是 121 行 × 约 5.6KB，同一个量级），要再往下压得动表格结构或改分页/虚拟滚动，属于另一件事，**没做，也没有证据说明现在必须做**。
>
> **④ 部署方式仍是未解决的真问题**：这次特意试了"构建前先 `docker compose stop`"（踩坑 32 的建议），构建确实快很多，但代价是站点在整个构建期间**完全不可访问**。两种方式都不可接受，**正解是把构建挪出这台机器**（别处构建镜像、机器上只 pull + 重启），**还没做，见踩坑 40**。)
>
> 2026-07-31 深夜（**修复页面卡顿：走势图数据涨到 831 点/饰品，持仓页 HTML 1.52MB**。项目所有者反馈"进场会卡很久、点某个按键要等很久"。**先量再改（踩坑 25）**，实测基线：`/positions` 冷启动首请求 TTFB **30.7s**、热请求 0.69~1.39s、**HTML 1,522,104 字节**，单个走势图的 `polyline points` 属性就有 **7951 字节**；`/anomalies` HTML 只有 164KB（它没有走势图，对照组）。**根因**：`item_signal_summaries.recent_prices` 这一列在库里 **1.68MB、单品均值 831 个点**——它只喂一个 **80×28 像素**的迷你走势图。追下去是 `lib/signal-summary.ts` 取 `recentPrices` 时读的是**原始 history 而不是重采样后的 hourly**；这行代码写于 2026-07-26 重采样改造，**当时原始数据就是每小时一条，7 天 = 169 点，完全合理**，但同期上线的 C5 高频 tick 把写入频率提到 10 分钟一次，同一行代码就变成 7×144≈1000 点。**跟踩坑 28（OOM）是同一个模式：当时正确的决定，被后来的改动作废，没人回来看它**。**影响面比页面更广**：这一列还喂给 LLM 提示词（`lib/api/nvidia-llm.ts` 把价格序列全部拼进去），等于每次生成 AI 建议都白烧上千个数字的 token。**两层修复，分别独立生效**：①**数据侧**（commit `172eac0`）`recentPrices` 改用已经算好的 `hourly` 序列，点数固定 ≤169 且**跟同步频率解耦**，新增 `lib/signal-summary.test.ts` 锁住这个性质（同一段行情喂 10 分钟频率和 1 小时频率必须给出等长走势，以后谁再调同步频率这条会先失败）；②**渲染侧**（commit `37d3918`）`components/ui/sparkline.tsx` 按 SVG 实际宽度等间距抽样到 ≤80 点（80 像素显示不了更多），首尾保留。**为什么两层都要做**：数据侧管住源头，渲染侧保证就算将来又有人往这个字段塞进过多的点也不会再撑爆页面。**过程中自己引入并抓到一个 bug**：抽样后 x 坐标的分母忘了跟着换成抽样后的长度，会把整条线挤到图的左边，已修并补 `sparkline.test.ts`（4 条）。测试 144→152 全过，lint/tsc 干净。**注意数据侧的收益要等下一轮整点同步**（`precomputeSignalSummaries` 重算 `recent_prices`）才落地，渲染侧是部署即生效。
>
> **运维教训（这次踩得很实）**：部署期间站点**实测不可用**——1 核机器上 `docker compose up -d --build` 会把 CPU 吃满，连 `ssh` 执行一条 `docker ps` 都超过 120 秒不返回，项目所有者那边是白屏转圈。**临时缓解手段验证有效**：`sudo renice -n 19 -p <构建进程>` + `sudo renice -n -5 -p <应用进程>`，之后 `/positions` TTFB 从"完全无响应"回到 1.48s，代价是构建变慢。**注意 `pgrep -f` 的匹配模式别写 `node`**——我第一次就是这么写的，它会把应用自己的 Node 进程也匹配进去，等于把要保护的对象一起降级（这次侥幸没命中，但模式本身是错的，要按容器 PID 精确定位：`docker inspect --format '{{.State.Pid}}'`）。**根本问题没解决**：每次部署站点都要卡十几到几十分钟，对一个天天要看的工具不合理，**下一步应该改成在别处构建镜像、机器上只做拉取替换，或者构建前先停应用容器**（踩坑 32 验证过后者能大幅缩短构建时间）。**这件事还没做，留给下次。**)
>
> 2026-07-31 晚（**异常队列三步走收尾：第 3 步交付、第 2 步查完是负结论不执行、¥5 门槛的预估被证伪**。三件事都跟昨天的预期不一样，按重要性排：
>
> **① ¥5 门槛对新增事件的实际效果 ≈ 0，昨天写的"下次会话第一个观察点"预估错了**。部署后核实：容器 `RestartCount=0`、整点同步每轮 325 个饰品 2532 条快照全部正常、模拟盘恢复开仓（每轮 1~3 笔），但**每日新增事件数 7-31 是 13 条，跟之前的 16/19/8/11/17 没有区别**。查清了原因：**历史上 <¥5 的事件一共 203 条，创建时间全部集中在 7-17、7-20、7-27、7-28 这四天**，而 `detected_at` 散布在 4~7 月——说明它们全部来自 `scanHistoricalPriceAnomalies`（观察池加新品时一次性回扫 90 天历史），**不是每小时扫描产生的**。每小时扫描本来就几乎不碰 <¥5 的品（最近 14 天里只有零星几条）。**所以 `MIN_PRICE_FOR_ANOMALY_SCAN=5` 真正的价值在 `lib/anomaly-scan.ts:247`（历史回扫那处）不是 `:68`（实时扫描那处）——它防的是"下次观察池再扩容时，回扫又灌进来一批低价噪音"，对当前的每小时流入没有任何影响**。这个区别很重要：以后再判断队列增长的来源，先看 `created_at` 是不是集中在少数几天（=批量回扫）还是均匀分布（=每小时流入），两者的止血手段完全不同。**队列现状 1442 条 pending**（比昨天 1432 多 10 条，是这一天的正常流入）。
>
> **② 第 3 步（稀缺类型清单）已交付，但发现它不是一次性任务而是持续流**。新增 `scripts/list-scarce-anomalies.mjs`，按 `manipulation_score`/`momentum_chase`/`washout_signal` 三类拉 pending，每条附：T-7/T/T+7/T+14/当前的价格与涨跌、已有操盘标记条数与窗口、同收藏品±1天的伴生异动、持仓还是观察池（持仓会显示购入价，`buy_price>0` = 当初凭消息买的）、详情页链接，另加一个 41 行的总览表方便一眼挑重点。输出在 `data/scarce-anomalies-2026-07-31.md`（`data/` 已被 gitignore 排除，脚本进 git、产物不进）。**清单只整理上下文不代判**，理由见踩坑 35。**新发现（昨天没看出来）**：这 41 条的 `detected_at` **全部落在最近 4 天内**（7-27~7-31），最早一条也才 4 天；查历史流入速度是**每天 6~10 条**。也就是说稀缺类型**根本没有存量积压，它是个持续流**——更早的都已经被审掉了。**所以正确的用法不是"生成一次清单一次性清完"，而是每周重跑一次脚本、跟上当周的流量**（脚本设计成可重复跑的就是为了这个）。**另一个后果**：41 条的 T+7 全都还没到（最老的才 4 天），**现在判断的信息量比等一周再判要少**，清单里对这类条目标了 ⏳ 提示"拿不准就先放着，过一周重跑再看"。
>
> **③ 第 2 步（外部事件核对）查完了，结论是"没有可批量标注的目标"，不执行——这是个负结论不是没做完**。原计划是按 5-22 更新/7-15 新规的日期窗口找 pending、方向吻合的标 `external`。逐项核实后全部落空：**(a)** 5-22 窗口（05-08..05-29）有 542 条 pending，但按品类拆是 **539 条普通皮肤 + 3 条布章，纪念品和印花一条都没有**——而 5-22 的两条叙事（纪念品可炼金→纪念品涨、取消印花胶囊→旧 Major 印花涨）**只对纪念品和印花做预测，对普通皮肤没有任何方向预测**，拿它去标 539 条普通皮肤就是踩坑 3b 说的"拿日期窗口反推批量改 status"。**(b)** 方向数据也不支持：这 542 条的 24h 方向是 **涨 179 / 跌 280 / 持平 83**，叙事预测的是涨，实际多数在跌。**(c)** 真正吻合叙事的那批**早就已经标完了**——现存 57 条 `external` 里 **55 条正是纪念品/印花**，日期从 4-29 到 7-27 都有。**(d)** 现存 26 条印花 pending 的日期**全部在 7-21 之后**，跟 5 月的印花胶囊事件毫无关系，是持仓印花最近自己的行情。**(e)** 7-15 窗口 26 条 pending，叙事是"全市场普跌"，实际方向是 **涨 12 / 跌 4 / 持平 10**，只有 4 条方向对得上（`#9834 Tec-9 Toxic`、`#9958 PP-Bizon Lumen`、`#10166 M4A4 Desolate Space`、`#10405 FAMAS Waters of Nephthys`），4 条不值得走批量流程，跟着日常审核走即可。**所以昨天写的"先跑 10 条样例确认口径再批量"这一步不用做了**——没有需要确认口径的批量操作存在。**这条本身是踩坑 3b 的一次正面验证**：如果不做品类拆分、只看日期窗口命中数（542 条，听起来很可观），会以为这是笔大买卖，实际一条都不该动。
>
> 本次会话没有改任何生产代码，只加了一个只读分析脚本；数据库只读，`manipulation_tags`/`confirmed`/`external` 一个字节没碰。)
>
> 2026-07-31（**异常审核队列机械归档（分三步走的第 1 步已完成）+ 一个推翻直觉的实测结论**：项目所有者说"异常提醒我确实不知道怎么判断，能不能全帮我处理了"。**没有照做，理由必须写清楚**：这些标签是训练数据，价值来自项目所有者的小道消息——价格数据里没有的信息。由 AI 按价格形态判断"是不是操盘"，等于用 `vol24h/move24h/maDev` 生成标签再去校准同样这几个特征的 AUC，**是循环论证**，会产出漂亮但毫无意义的数字，且几个月后完全看不出是假的（PLAN.md 已把"标注自我实现偏差"列为已知风险，踩坑 3b 也记过"不能拿日期窗口批量改 status"）。数据也支持这个判断：`price_zscore` 确认率 68%（485 样本）而 `manipulation_score` 只有 36%，**两者差异恰恰证明人工标注带进了价格数据里没有的信息**。**改为提出分三步走的方案，第 1 步（纯机械清理，不含任何操盘判断）已执行**。**队列现状**：1578 条 pending（页面可见 1458，其余 120 是开箱品被过滤），按类型是 `price_zscore` 1425 / `manipulation_score` 26 / `momentum_chase` 5 / `washout_signal` 2。**核心洞察：问题不是队列长，是审的东西不对**——`price_zscore` 已有 485 个标注样本，再标 1425 条同类的 AUC 不会动（这正是 7-20/7-25/7-27 三次复跑都"跑了没质变"的原因），稀缺的是另外三类（一共才 33 条）和收藏品配对案例。**两个实测结论（用已审核的 485 条 price_zscore 回算，不是拍脑袋）**：①**按触发价分档的确认率极其锐利**——`<¥5` **0%（121 条无一命中）**、`¥5-20` 56%、`¥20-100` **99%**、`¥100+` 95%，所以 `MIN_PRICE_FOR_ANOMALY_SCAN` 从 1 提到 **5** 从源头止血；②**按 z-score 强度筛是错的，明确不要做**——z `6-7` 确认率 68%、`7-8` 72%、`8-10` 76%，几乎无差别，而 **z≥15 反而只有 31%**（极端 z 多是数据毛刺不是真行情），按强度砍会误伤 133 条真操盘。**这条已写进 `lib/anomaly-scan.ts` 的注释**，免得以后有人（包括 AI）再想这么"优化"。**追清楚了标签的真实流向（重要，之前一直没写明）**：`confirmed` → `app/api/anomaly-events/[id]/confirm/route.ts` 调 `addManipulationTag()` 写 `manipulation_tags` 表，**那才是特征分析脚本读的正类**，anomaly_events 上的 confirmed 只是审核痕迹；`external` → 脚本直接读作排除窗口；**`dismissed` 目前没有任何脚本读它**（特征脚本的负类是"不在操盘窗口也不在外部事件窗口的其余时段"）——也就是说一条事件是 pending 还是 dismissed，对当前训练结果完全一样，**队列是注意力问题不是数据完整性问题**。**新增 `archived` 状态而不是复用 `dismissed`**：dismissed 语义是"人看过、判断为正常波动"是真实负标签，007 迁移注释写明将来要当负样本用，到那时必须能分清哪些是人判断过的、哪些是机器归档的；代价几乎为零却保住了这个选项。**执行结果**：`scripts/archive-unreviewable-anomalies.mjs`（默认演练不写库，`--apply` 执行，`--revert --apply` 回滚），归档 146 条（开箱品 120 + 触发价<¥5 的 26），pending 1578→**1432**；`manipulation_tags` 仍 179 条、`external` 仍 57 条、`confirmed` 仍 345、`dismissed` 仍 131，**训练管线读的东西一个都没碰**，误伤已审核记录 0 条。`lib/paper-trading.ts` 的 `MIN_ENTRY_PRICE` **有意维持 1 未跟随**（模拟盘至今零平仓，没有已实现收益可回算低价品信噪比），注释写明了这个差异是刻意的。**没兑现的预估**：一开始说能压到 150 条以内，实际只到 1432——因为撤回了 z-score 筛选。剩下 1432 条涉及约 240 个饰品，得靠**按饰品折叠**处理（页面已有 `scope=item` 整批确认能力，`listPendingAnomalyEventsForItem`，点一次处理掉一个饰品的一整波行情）。**后续两步（还没做）**：第 2 步——按踩坑 3b 的方法逐条核对每个事件的价格方向是否和已知外部事件（5-22 更新 / 7-15 新规）吻合，吻合的标 `external`，注意日期最集中的三天正好是 05-22（106 条）、05-27（83）、05-25（57）；第 3 步——把 33 条稀缺类型整理成带价格走势/涨跌幅/同收藏品情况的清单，**这部分必须项目所有者亲自判断**，不知道的标"正常波动"也是有效负样本。**部署状态：全部完成并验证**。代码 `ed78032` 已 push；归档是直接改数据库，执行完即生效；`MIN_PRICE_FOR_ANOMALY_SCAN=5` 需要重新构建，已于 2026-07-31 03:56 部署完成并验证（新容器 `RestartCount=0`、内存 109.7MiB/954MiB、`Ready in 1449ms`、站点 HTTPS 401 即 Basic Auth 正常）。**下次会话的第一个观察点**：¥5 门槛生效后的第一轮整点同步，新增事件数应该明显低于之前（之前每天 8~54 条，其中相当比例是低价噪音）；查法 `ssh cs2-cloud "sudo docker logs cs2-skin-advisor-app-1 --timestamps 2>&1 | grep '个饰品，写入' | grep -v c5-fast | tail -3"`，看"新发现 N 个异常波动"那段。)
>
> 2026-07-31（**生成持仓+建议报告给外部 AI 用**：项目所有者要一份"所有有市场价的库存持仓 + 买卖建议"的表格，喂给别的 AI 获取建议。产出 `data/position-report-2026-07-30.md`（含完整口径说明）和 `.csv`（结构化输入用）——**放 `data/` 是因为它已被 .gitignore 排除，这份东西含完整持仓和成本价，仓库是公开的，绝对不能提交**。**过程中发现三件事**：①**206 条 inventory 记录其实只对应 56 个不同饰品**（一行=一件实例，`quantity` 全是 1，`Col. Mangos Dabisi | Guerrilla Warfare` 有 28 件、`Sticker | Aurora (Holo) | Austin 2025` 有 20 件），直接按行列表会严重误导，报告按品种聚合、件数单列一栏——**这也解释了为什么 HANDOFF 里一直说"持仓 56 个"而库存表有 200 多行**；②**整体浮亏 -44.5%**（成本 ¥51,561 → 市值 ¥28,629），56 个品种**无一盈利**，亏 70%+ 的有 77 件；核对过已实现流水（M4A4 Buzz Kill 买 3080 卖 5488 等 5 笔）确认 `buy_price` 是真实成交价不是标签，所以这个数字是真的；风险高度集中——**前 5 个品种占总成本 40%（¥20,612）**，印花那批最惨（Austin 2025 Holo 系列 -82% ~ -88%）；③**`buy_date` 不可用**——203/206 条都是 `2026-06-22`，那是 Steam 库存导入日期不是真实购买日，所以报告**没有**列"持有天数"而不是放个错的进去。**报告里写了给 AI 的口径说明**（否则对方拿裸数据会瞎判断）：score/action 的计算方式**及其未经验证的事实**（模拟盘至今零平仓，这套评分的实际胜率未知）、价格是在售价未扣手续费、T+7 锁定新规、"24h 涨幅>15% 未来 7 天平均 -10.74%"这条已验证结论。**踩坑**：CS2 饰品名自带 `|`（"AK-47 | Redline"），生成 Markdown 表格必须转义成 `\|`，否则列会被切断——第一版就是这么错的，靠"数每行未转义竖线数应等于列数+1"的校验发现。)
>
> 2026-07-30（**修复云端每小时 OOM 崩溃（停机 3 天）**：项目所有者几天没看，让检查进度，一查发现云端从 **2026-07-27 15:22 起每小时崩一次，3 天崩了 90 次**，Docker `restart: unless-stopped` 每次都把它拉起来，所以**表面上服务一直"活着"，页面能打开、价格也在涨，完全看不出问题**。**根因**：`lib/db/snapshots.ts` 的 `getPriceHistory` 不带 `sinceIso` 时读全量历史，并把结果永久缓存进 `lib/signal-cache.ts` 的全局 Map；而 `scanForAnomalies` 是"遍历全部追踪饰品"的批处理循环，缓存把本该"一次只留一个饰品"的流式循环变成"338 个饰品的完整历史同时驻留在堆里"——338 × 约 3300 行 × 约 400 字节 ≈ **440MB**，正好撑爆 1GB 机器上 Node 的约 477MB 堆上限（崩溃栈指向 better-sqlite3 的 `Statement::JS_all`，跟这个算术对得上）。**触发时点**：7-26 观察池扩容到 338 个饰品 + C5 提频到 10 分钟一次，两件事叠加越过临界点，7-27 那次部署重启后立刻显形——**这两个改动本身都没错，是它们把一个一直存在的 O(饰品数 × 历史长度) 内存缺陷推过了阈值**。**两处改动**（commit `aef4325`）：①**去掉价格历史缓存**（`signal-cache.ts` 只保留"各平台最新一条"，每饰品几行，无害）——关键依据是持仓/观察池页面早在加 `item_signal_summaries` 预计算表时就不再现场算信号了，当初这层缓存要优化的页面热路径**已经不存在**，现在只剩批处理在用，纯负收益；②信号计算改走新的 `getRecentPriceHistory`，只取最近 **21 天**（`SIGNAL_HISTORY_WINDOW_DAYS`），窗口按信号函数最长回溯需求推导（嫌疑分 `slice(-169,-1)` = 169 个小时桶 ≈ 7.05 天）留 3 倍余量，需要完整 90 天的两处（详情页图表"全部"区间、`scanHistoricalPriceAnomalies` 一次性回溯扫描）继续用全量 `getPriceHistory`。**为什么两个都要做**：去缓存把峰值从"338 个饰品"降到"1 个饰品"，加窗口则是防止 C5 高频 tick 每天给每个饰品多写 144 行、迟早再撞回来——**结构上从 O(饰品数 × 历史长度) 变成 O(饰品数 × 4 行) + O(1 个饰品的窗口历史)，观察池以后再扩容也不会重演**。实测单饰品查询 3316 行降到 1333 行，测试 137→141 条全过。**精度影响为零**（重要，别被"截断历史"吓到）：所有信号函数都只从数组末尾往回取固定期数（嫌疑分 169、z-score/成交量 168、洗盘 48、追涨 24、MA30/RSI14 30/14），21 天 = 504 个小时桶，只要窗口内桶数 ≥169 输出就**逐位相同**，不是近似；唯一理论例外是 RSI 的 Wilder 平滑（递归依赖全序列），但种子影响按 (13/14)^504 ≈ 10⁻¹⁷ 衰减，浮点数都表示不出来。**训练/特征分析完全不受影响**：`scripts/*.mjs` 一个都没引用 `lib/`，全部直接打原生 SQL 读 `price_snapshots`（因为 node 跑不了 lib 的 .ts，见踩坑 12），窗口是读取参数不是保留策略，**一行历史数据都没删**。**部署与验证状态**：两个 commit（`aef4325` OOM 修复、`ab685c3` 模拟盘僵尸仓位）都已 push + 云端 `docker compose up -d --build` 部署完成，服务端 git 在 `ab685c3`。**已验证**：容器 `RestartCount=0`（崩溃循环已断，此前是 90）；RSS 21~25MiB / 954MiB（此前 638MiB 且持续爬升到崩）；**最有力的一条是 C5 高频 tick 的早期触发真的跑了一次完整 `scanForAnomalies()`（338 个饰品全扫，日志 `检测到短时大幅波动（USP-S | Purple DDPAT (Minimal Wear)），已提前跑一次完整异常扫描`）——正是三天来每小时必崩的那段代码，这次完整跑完没崩**；站点从服务端实测 `https://170-9-25-139.sslip.io/` 返回 401（Basic Auth 正常要求认证，不是故障），Caddy 已连续运行 9 天。**尚未验证（下次会话第一件事就去看）**：一轮**完整的整点同步**还没在新代码上跑过（容器 19:08 重建，整点定时器约 20:08 才触发），要确认的是 `[price-sync] 定时: ...` 完成日志重新出现、`collection_linkage` 联动预警恢复产生、`item_signal_summaries.computed_at` 刷新到当天、模拟盘重新开平仓（尤其那 9 笔已过 T+7 的）。查法：`ssh cs2-cloud "sudo docker logs cs2-skin-advisor-app-1 --timestamps 2>&1 | grep '个饰品，写入' | grep -v c5-fast | tail -3"`。)
>
> 2026-07-30（**崩溃 3 天的影响面盘点**：写下来是因为这批数据缺口以后做特征分析时会撞上，不知情会当成市场信号误读。**没受影响**：价格快照采集——C5 高频 tick 是独立定时器，写完快照就返回，根本走不到崩溃点，7-28/29/30 每天仍有 5.7~6.5 万条快照入库（比崩溃前的 7-25 还多）；数据完整性——`insertPriceSnapshot` 是 `INSERT OR IGNORE` 幂等写入，SQLite 同步提交，进程 abort 不会写坏表。**受影响（崩溃点在 `lib/sync.ts` 的 `scanForAnomalies()`，它前面的活干完了，后面的全没干）**：①**异常扫描每小时只跑了一半，而且每次是同一半**——`getTrackedItemNames()` 返回顺序稳定、堆随循环推进逐渐涨满，所以靠前那批饰品每小时都扫得到，靠后那批**连续三天一次都没扫过**，是系统性盲区不是随机丢失；②**联动预警（`collection_linkage`）三天一条都没出**——它的逻辑在饰品循环**之后**，循环从没跑完，数据库里 7-26 有 2 条、7-27 有 1 条，7-28 起归零，这条是最硬的证据；③**推送一条都没发**——预警先攒进 `pushNotifications` 数组、统一在函数最末尾发送，7-28~7-30 检测出的 **21 条嫌疑分预警**全进了库但推送随进程一起丢了；④**模拟盘停摆**（`runPaperTradingTick` 排在崩溃点后）——153 笔 open 仓三天无人评估平仓、零新开仓；⑤**信号汇总表冻结**——`item_signal_summaries` 全部 325 行的 `computed_at` 卡在 `2026-07-27 14:22~14:23`（正是最后一次跑完的同步那一刻），所以这三天页面上的市场价/今日涨跌/action/score/走势图全是旧的，**底层快照是新的，只是没人重算**；⑥**SteamDT 侧（BUFF/悠悠）有真实的小时级缺口**——按平台统计"每天有数据的小时数"：崩溃前 7-26 是 BUFF 24/24、悠悠 24/24、C5 24/24，7-28 变成 BUFF **15/24**、悠悠 18/24、C5 24/24，7-29 BUFF 20/24，主因是崩溃-重启把整点定时器往后顺延（每轮实际 65 分钟而非 60 分钟，一天少跑约两轮）叠加个别轮次分块限流失败。**这些缺口补不回来**：K 线接口能回填历史价格，但补不了当时的求购深度（`bidding_count`/`bidding_price`），而那正是下一步想拿来补吸货期/出货期指纹的数据。)
>
> 2026-07-30（**模拟盘首次归因分析：表面难看，归因后超额收益约等于 0，真正的问题不在收益率**：项目所有者问"模拟盘是不是很不理想"，拉真实数据算了一遍。**表面数字**：153 笔全部未平仓，按当前价扣 1% 手续费估算，净浮盈为正的只有 24 笔（15.7%），平均净收益 **-3.15%**，分布是亏 10~20% 有 10 笔、亏 20%+ 有 1 笔、赚 10%+ 有 3 笔（最好 MP9 Hot Rod +42.7%，最差 -15% 上下）。**但对照基准一算，大部分是市场 beta**——同一时间窗里观察池全体 282 个饰品：7-26 起算中位 **-1.96%**、只有 20% 在涨，7-27 起算中位 -1.49%、24% 在涨；模拟盘选中的那批中位数**反而略好于全体**（-1.77% vs -1.96%、-1.22% vs -1.49%）。**结论：-3.15% 里绝大部分是大盘普跌，归因给策略的超额收益接近 0，既没跑赢也没跑输**（这跟 C1/C2 那次的教训是同一件事——不放进对照基准看，很容易把 beta 当成 alpha 或把 beta 当成策略失败）。**真正值得担心的是三件事，都不是收益率**：①**零平仓完全是崩溃造成的，不是策略结果**——最早一批 7-22 开仓、T+7 到期是 7-29，而 tick 从 7-28 就死了，所以**卖出侧逻辑到现在一次都没运行过**，这是最大的空白；②**样本高度集中在一天**——153 笔里 79 笔（52%）开在 7-26 同一天、40 笔在 7-27，那正是观察池从 41 扩到 282 的当天（一次性给全部新饰品算信号、达标的全开了），这批仓位本质上是**一个市场瞬间的切片，同涨同跌，统计上更接近 n=2 而不是 n=153**；③**入场信号几乎没有区分度**——买入分数分布是 **136 笔 score=30、17 笔 score=40**，score 30 正好是 `ENTRY_MIN_SCORE` 下限、等于"RSI 超卖"这一个信号，所以模拟盘现在实际上是个**单因子抄底策略**，而 T+7 锁定又让它没法止损（抄底 + 不能止损 + 下跌市 = 现在这个样子）。**下一步**：真正有信息量的数据要等修复生效后那 9 笔已过 T+7 的仓位被评估、7-24 那 18 笔到期、以及后续在**不同市场时点**分散建仓；原本估计 3-4 周才有足够平仓样本谈胜率，崩了三天要再往后推。**待项目所有者决定**：要不要把 `ENTRY_MIN_SCORE` 从 30 提到 40 挡掉单信号交易——代价是大幅减少样本量，是"样本量 vs 信号质量"的取舍，没有客观最优解，所以没擅自改。)
>
> 2026-07-30（**模拟盘"僵尸仓位"预防性修复 + 云端磁盘清理**：①**僵尸仓位**（commit `ab685c3`）：`lib/paper-trading.ts` 的平仓循环原来是"先取信号快照、取不到就 `continue`"，把 30 天超时强平也一起跳过了。改动前全量历史下 `computeSignalSummary` 几乎不可能返回 null，但**加了 21 天窗口之后**，饰品被移出观察池就不再同步、21 天后滑出窗口，这笔仓位会永远停在 `open`：既不平仓，也不进胜率统计——**这笔决策永远没有结论，是模拟盘验证实验里最没用的一种状态**。改成先判 T+7 和超时、再取信号快照：没快照且已超时的按 `getLatestPricesByPlatform` 的最后一条已知快照价平掉（同样扣 C5 1% 手续费），`close_reason` 单独记成新值 **`stale_data`**（因为成交价不是"决策当时"的价，跟正常平仓不可比，**评估胜率时要把这批整体剔掉**），`/paper` 页面给了独立的琥珀色"数据中断"标签而不是混进"超时平仓"，`sell_score` 存 `null` 不存 0（0 会被误读成"算出来是中性分"）；查不到最后已知价时保持开仓，不拿别的平台的价顶替（那等于换了个标的算收益）。过程中发现**原有测试 `拿不到信号快照的持仓本轮跳过` 正好把这个 bug 当成期望行为固化了**（用的还是 31 天前开仓、明明该超时的样本），已改写。现存 153 笔 open 仓全在 C5、也都还在观察池里，眼下没有这类仓位，属于预防性修复。测试 141→144 条全过。②**磁盘清理**：排查时发现云端磁盘从 7-22 的 36% 涨到 **80%**，元凶是 **Docker build cache 27.8GB**（反复部署攒的，跟崩溃无关；core dump 查过没落盘）。跑 `sudo docker builder prune -f --filter until=24h` 回收 **19.47GB，80% → 31%**——**特意用 `until=24h` 过滤而不是全清**，这样当天构建的热缓存会保留下来给紧接着的第二次构建复用。建议以后加进每周 cron。)
>
> 2026-07-27（**给 lib/db/* 全量补单元测试**：动机是 PLAN.md 阶段 E1 第一步"引入 Drizzle ORM 重写 lib/db/*"——项目所有者问这步风险大不大，排查发现 `lib/db/` 14 个文件 735 行原始 SQL 完全没有测试覆盖，Drizzle 改写这种"每条查询语义必须跟原来一模一样"的重构没有回归安全网，决定先补测试再考虑要不要真的动 Drizzle。**基础设施**：`lib/db/client.ts` 的 `createConnection` 参数化成接受任意路径（原来硬编码真实文件路径），新增 `lib/db/testing.ts::createTestDb()` 用 `:memory:` 建一个跑过真实 `db/migrations/*.sql` 的临时库——特意用真实 migrations 而不是手写精简 schema，这样 schema 和代码假设不一致时测试能测出来（Drizzle 改写最容易在这里出漂移）；`lib/signal-cache.ts` 加了单测专用的 `resetPriceCacheForTesting()`，因为价格缓存是模块级全局，不会跟着每个用例新建的内存库一起重置，不清会导致上一个用例的缓存数据泄漏进下一个。**覆盖范围**：14 个文件全部覆盖（`client.ts` 通过其余 13 个文件的测试间接跑到，没有单独再写），新增 137-78=59 条测试，重点覆盖了容易在 Drizzle 改写时悄悄改变语义的分支——`UNIQUE`/`ON CONFLICT DO NOTHING`/`DO UPDATE` 这几种去重与更新策略的区别（`snapshots.ts`/`market-candidate-pool.ts`/`push-subscriptions.ts`/`item-metadata.ts`/`item-catalog.ts` 各有一种）、`anomaly-events.ts` 的状态过滤和冷却窗口查询、`paper-trades.ts` 的 `closePaperTrade` 只能作用于 `status=open` 行这条隐藏约束、`snapshots.ts` 的 STEAM/HALOSKINS 排除和缓存失效。过程中揪出一个测试本身写错的 bug（`listOpenPaperTrades` 按 `opened_at` 降序排列，一开始按升序假设取错了下标），修正后全部通过，`npm run test` 从 63 条涨到 137 条（25 个测试文件），`lint`/`tsc` 干净。**没做的**：这次只补测试，没有真的开始 Drizzle 改写，那个决定留给项目所有者。)
>
> 2026-07-27（**待审核队列清零后重跑特征分析**：项目所有者审完页面上可见的全部待审核异常（`/anomalies` 页面按既定规则不展示 buy_price=0 开箱所得饰品的事件，这批共 120 条继续留在数据库里不算"待审核"，页面可见数确实是 0），云端跑了一次 `analyze-manipulation-features.mjs`。**样本量**：操盘期 16220 小时样本、平时 112275、外部事件 160，比 7-25 那次更厚（这轮新增 345 条确认操盘 + 57 条外部事件）。**特征 AUC**：vol24h 0.707→0.710、move24h 0.654→0.657、maDev 0.637→0.639，全部基本没变；coMove 0.518→0.520，延续 0.500→0.507→0.518→0.520 的同方向小幅爬升，还是远没到能用的量级。**结论：不调整 `manipulation-score.ts` 的阈值**——操盘期/平时中位数（vol24h 0.0135 vs 现有锚点 0.0154、move24h 0.0388 vs 0.047、maDev 0.0506 vs 0.055）都还在同一量级，跟 7-20/7-25 两次一样是"跑了没质变"。这次只跑了只读分析脚本，没改任何生产代码。)
>
> 2026-07-27（**C5 高频 tick 接上早期触发扫描**：C5 高频 tick（10分钟一次）上线并稳定运行几小时后，把之前搁置的"用高频数据降低检测延迟"补上——`lib/sync.ts::syncC5PricesOnly` 现在会拿这一轮 C5 价格跟上一轮（10分钟前，读 `getLatestPricesByPlatform` 缓存，不用现场查历史）比，任何饰品变化超过 8%（`EARLY_TRIGGER_MOVE_THRESHOLD`，工程门槛不是校准阈值，参考 washout/momentum-chase 已验证的 15%/24-48h窗口缩到10分钟窗口保守取值）就提前跑一次完整 `scanForAnomalies()`（带联动预警+推送），不用等到整点——这样联动预警/嫌疑分预警的检测延迟从最坏 60 分钟压到最坏 10 分钟。**刻意保留的边界**：早期扫描只跑异常检测，不跑模拟盘/信号预计算（那两样还是跟着整点大同步走，不改变模拟盘开平仓节奏和持仓页数据刷新频率）；加了 1 小时冷却（进程内变量，非持久化——重启清零无害，最坏多触发一次），避免价格持续高位时同一小时内反复整轮扫描。`lib/scheduler.ts` 的 `[c5-fast-sync]` 日志现在会打印触发了哪些饰品。`npm run lint`/`tsc`/`test`（63条）全过，这块代码本身没加新测试（复用已有的 `scanForAnomalies`/`resampleHourly`，新增的只是一个比较+门槛判断，逻辑简单，且没有 mock 掉 db 的现成测试基础设施能覆盖这种"进程内定时器+外部API"的集成路径，投入产出比低）。)
>
> 2026-07-27（**停止收集 STEAM/HALOSKINS 两个平台**：项目所有者要求，这两个都不是国内玩家主要交易平台（HALOSKINS 是海外小众平台；STEAM 社区市场余额提现有折损、标价一直虚高，早就在 `pickReferencePlatform` 里垫底）。改法：`lib/db/snapshots.ts` 新增 `EXCLUDED_PLATFORMS = ["STEAM","HALOSKINS"]`，`lib/sync.ts` 写入 SteamDT 批量数据前按这个名单过滤（不写新快照，`snapshotCount` 统计不含被过滤的），`getLatestPricesByPlatform` 的查询也加了同一份排除名单——这样两边覆盖：以后不再收集 + 之前已经写进去的历史行不再被任何页面/信号读到（跨平台价差、参考平台选择、饰品详情页的平台切换 tab 全部走这个函数，一处过滤全部生效）。顺手清了 `pickReferencePlatform` 里跟着变成死代码的 STEAM 特判（`nonSteam ?? candidates[0]` 简化成 `candidates[0]`，因为 STEAM 现在压根不会出现在候选里）。**没有删历史数据**——`price_snapshots` 里之前的 STEAM（9694行）/HALOSKINS（19950行）快照还在表里，只是不再被查询到，纯粹是为了不做破坏性操作；真要清空间需要另外单独执行一次 DELETE，这次没做。`npm run lint`/`tsc`/`test`（63条）全过。)
>
> 2026-07-26（**C5 高频 tick 上线**：前置的小时重采样改造（见下条）落地后，接着实现了"C5 单独提频"——`lib/sync.ts` 新增 `syncC5PricesOnly`（只调 C5 批量报价+写快照，不跑异常扫描/模拟盘/信号预计算，那三样继续跟着整点大同步走），`lib/scheduler.ts` 新增独立的 10 分钟定时器 `C5_FAST_SYNC_INTERVAL_MS`。**两处安全设计**：①`getProductPrices` 分块结果落库的逻辑（"部分成功时只把没返回的饰品记错误"）从 `syncPriceSnapshots` 里抽成共享函数 `applyC5Prices`，两处调用，避免以后改一处忘了改另一处；②独立开关 `C5_FAST_SYNC_DISABLED=1`（读法跟已有的 `PRICE_SYNC_DISABLED` 一致），出问题时 SSH 上去在 `.env.local` 加这行 + ~~`docker compose restart`~~ **`docker compose up -d`**（**不用 rebuild**，几秒钟生效。**2026-08-14 更正：`restart` 不重新读 env 文件，见踩坑 51**）就能单关掉高频 tick，不影响整点大同步。**为什么现在做这个安全**：C5 批量接口限流是 50 QPS（远宽松于 SteamDT 的每分钟1次，见 lib/api/c5.ts），10 分钟一个 tick 只额外调 1 次批量请求，负载可忽略；`insertPriceSnapshot` 是 `INSERT OR IGNORE` 纯增量写入，从不删改已有行，就算这个 tick 哪里错了也不会腐蚀整点同步或模拟盘的数据；信号计算那边已经被前置的小时重采样保护，不会因为 C5 写入变频而算错。**已知的次要影响，不是 bug**：`lib/signal-cache.ts` 的持仓/观察池页面缓存按 `item_name` 失效，之前假设"两次写入之间"是一小时，现在 C5 这一路缩到 10 分钟，缓存命中率会降但仍远好于不缓存，无需处理。`npm run lint`/`tsc`/`test`（63条）全过。**观察计划**：部署后盯 `[c5-fast-sync]` 日志几小时，确认错误数低、`/positions` 页面延迟没有明显反弹。)
>
> 2026-07-26（**信号层加小时重采样，拔掉"数组下标=小时"的隐性假设**：项目所有者提议"C5 单独提频同步"（SteamDT 批量接口官方限流每分钟 1 次，C5 批量接口没有这个限制，实测 50 QPS 额度绰绰有余），排查后发现现在不能直接做——`lib/signals/` 下嫌疑分（manipulation-score.ts）、洗盘（washout.ts）、追涨（momentum-chase.ts）、成交量异动（volume.ts）、价格 z-score（anomaly.ts）全部把价格/成交量数组的下标当"过去第 N 小时"用（`slice(-24)` 当作"最近24小时"、`hourlyPrices[n-25]` 当作"24小时前"），这个假设现在成立只是因为整点同步和 K 线回填天然都是一小时一条——如果给 C5 单独加密同步，同一个 item+platform 序列里一个下标就不再等于一小时，嫌疑分那批从真实标注数据校准出来的阈值（0.0064/0.031 那些）会全部静默算错，且不会报错，模拟盘正在跑的验证实验也会因为信号语义中途改变而被污染。**已实现的前置改造**（先做这个，还没开提频开关）：新增 `lib/signals/resample.ts` 的 `resampleHourly`，按 UTC 整点小时给快照序列分桶、每桶只留最晚一条，接入 `lib/signal-summary.ts`（喂给 MA/RSI/成交量异动/嫌疑分之前）和 `lib/anomaly-scan.ts` 的三处调用（实时扫描的价格z-score/成交量/嫌疑分/洗盘/追涨，以及历史回溯扫描）——图表用的 `recentPrices`/`changeToday` 仍然读原始 `history`（按时间戳查找，不受采样频率影响，全分辨率对图表展示更好）。**现有数据下这是无损重构**：整点同步+K线回填本来就是一小时一条，重采样后数组内容不变，`npm run test` 从 59 条增到 63 条（新增 `resample.test.ts` 4 条）全过，其余既有测试原值原样通过，验证了这一点。**下一步**（还没做，随时可以开）：给 `lib/api/c5.ts` 加一个独立的高频 tick（只写快照，不跑完整异常扫描/模拟盘），此时上面这层重采样能保证信号计算依然按小时语义读数据，不会被新写入频率污染。`npm run lint`/`npx tsc --noEmit` 干净。这次改动不改变现有数据下的任何输出，风险很低，但还是走标准部署流程验证。)
>
> 2026-07-26（**修复 C5 批量报价接口的参数超限 bug**：昨晚观察池扩容到 338 个追踪饰品后，云端每小时同步日志里 325 个饰品**全部**报 `C5 /merchant/product/price/batch 返回错误 500211: 超出参数数值上限`——正是前一条记录里"残留风险"那段点名怀疑的地方：`lib/api/c5.ts` 的 `getProductPrices` 一直是所有名字一次性发一个请求，没有分块，SteamDT 那边修完限流之后 C5 这边先撞上了参数数量上限。修法跟 SteamDT 那次同源但不能照抄：①分块大小取 100（跟 SteamDT 对齐，338 个饰品报错但没有更精确的上限数据，没必要去试探边界）；②块间隔只加 1 秒，不是 SteamDT 的 60 秒——C5 报的是参数数量超限不是限流，没有证据表明连续请求会被限流，用 60 秒纯粹是白白拉长同步耗时；③`lib/sync.ts` 这次必须跟着改（SteamDT 那次没动 sync.ts，因为它本来就是"谁没出现在返回列表里就记错误"的写法）——C5 那段原来是 `error` 非空就把全部饰品记错误，分块后会出现"部分块成功、error 也非空"的状态，不改的话会把已经成功拿到的数据当失败丢掉，改成只把没出现在合并后 map 里的饰品记错误，逻辑对齐 SteamDT 那段。`npm run lint`/`npx tsc --noEmit`/`npm run test`（59 条）全过，这块代码本身没有单测覆盖（跟 SteamDT 那次一样，属于外部 API 封装层，不在 CLAUDE.md 强制测试范围）。待部署验证：下一轮整点同步后看日志错误数应该从 325 掉回接近 0。)
>
> 2026-07-26（**观察池批量扩容（跨磨损联动候选）+ 修复 SteamDT 批量价格接口的分块限流 bug**：项目所有者贴了一份"私董"喊单截图（点名一批具体品种+拿货价区间），核对后发现里面提到的"抽象派 1337"/"彼岸花"/"喧嚣杀戮"跟 7-20 已验证过真实同小时联动的两组收藏品配对（"激流大行动"收藏品、手套收藏品）是同一批品种——这份喊单大概率就是那批配对信息的原始来源，算是外部独立信源对已知 coMove 信号的一次交叉印证。项目所有者补充了一个新思路：**同一皮肤的不同磨损档位也该一起跟踪**，因为拉盘有时会带动同皮肤其他磨损联动，或者庄家本来就分磨损做盘。处理方式：把喊单里能确定"武器+皮肤名"的 84 个品种对 `item_catalog` 做匹配（排除 StatTrak™/纪念品），本来匹配出 394 条待新增（全磨损档），跟项目所有者确认后精简成只要三个主流磨损（崭新出厂/略有磨损/久经沙场，破损不堪/战痕累累对贵价皮肤成交稀薄、信号密度低但同样占用同步配额），最终 241 条全部通过真实 `/api/watchlist` 接口写入（不是绕过业务逻辑直接插库，走真实校验+回填流程），100% 成功。观察池从 41 条涨到 282 条，加上持仓 56 个，追踪总量 97→338。**过程中揪出一个这次扩容必然会现场炸掉的真 bug**：`lib/api/steamdt.ts` 的 `getBatchPrice` 虽然本来就有 100 个一批的分块逻辑，但**块与块之间完全没有间隔，且第一块失败就直接放弃后续所有块**——HANDOFF 踩坑 6c 早就记录过这个接口连续请求 2 次就 4005 限流、要等约 60 秒恢复，338 个饰品会拆成 4 块，不修的话下一次整点同步大概率是第 1 块（100个，多是原有持仓）成功、后面 3 块（238个，新加的这批基本全军覆没）直接同步不到数据。已修：块间加 60 秒 `sleep`，且某一块失败不再放弃后续块，继续跑完，把失败块的名字合并进 error 文本返回（`sync.ts` 原有的"谁没出现在返回列表里就记错误"逻辑不用改，天然兼容）。**代价**：4 块的话一轮 SteamDT 批量同步单是块间等待就要 3 分钟（3 个间隔×60秒），加上请求本身和后续 C5/异常扫描/模拟盘/信号预计算，一轮同步预计要几分钟，之前是几十秒——hourly 频率下可接受，但如果观察池以后继续这样成倍扩容，总时长会线性增长，接近 1 小时同步间隔时就要重新考虑分层同步（PLAN.md 阶段 E1 已经记了这个方案）。**残留风险，没处理**：C5 的 `getProductPrices` 批量接口没有分块（338 个名字一次性发），代码里没有文档记录的上限，这次没有证据说明会出问题所以没动，但如果以后同步日志里出现 C5 那侧大批饰品报错，先怀疑这里。`npm run lint`/`npx tsc --noEmit` 干净，还没跑 `npm run test`（这个改动没碰任何有单测覆盖的模块）。)
>
> 2026-07-25（**重跑特征分析脚本验收进度**：在云端容器里对着真实数据库重跑了 `analyze-manipulation-features.mjs` 和 `analyze-playbook-stages.mjs`（本机 `data/db.sqlite` 是 7-21 迁移时的旧快照，不代表当前真实状态，见"运行架构"一节，之后要看当前数据必须去云端跑）。**特征 AUC**：vol24h 0.72→0.707、move24h 0.66→0.654、maDev 0.62~0.63→0.637，小幅回落但同一量级，不算变化；**coMove 0.507→0.518，是首次校准以来第一次有肉眼可见的移动**，方向跟 7-20 补的收藏品配对案例（喧嚣杀戮/抽象派那批）吻合，说明继续攒配对案例这条路是有效的，只是幅度还远够不上能用。**剧本验证**：episode 162→173（+11），段落 1829→2036（+11%），k=6 聚类里两个已验证指纹量级基本没变——急拉簇（n=37，涨速中位数 12.69%/天，跟上次一字不差）、深回撤洗盘簇（n=81，回撤中位数 17.4%），吸货期/会员进场/出货期依旧看不出独立指纹。**新发现**：脚本这次第一次打印出"在售量特征覆盖：113/2036 段（仅 2026-06-22 后有量数据）"——`price_snapshots.volume`（在售量，区别于 7-20 补的求购 `biddingCount`）从 6-22 起已经有数据在攒，但覆盖率只有 5.5%，还太稀疏没法用，之前几版报告没提过这个统计，值得记一笔继续跟踪它变厚的速度。**结论**：跟 7-20 一样是"跑了没质变"，但这次多了两个新观测点（coMove 首次移动、在售量覆盖率有了统计），说明几个"攒时间"型任务都在往前走，不是停滞。**同一天更早的记录见下条**（补模拟盘/手续费单元测试）。
>
> 2026-07-25（**补模拟盘/手续费单元测试**：`lib/paper-trading.ts`（模拟盘开平仓决策逻辑——T+7 锁定、30 天超时平仓、7 天再开仓冷却、"先平后开"执行顺序）和 `lib/fees.ts`（手续费计算）之前完全没有单元测试。`paper-trading.ts` 依赖 better-sqlite3 的 db 模块，没法直接单测，用 `vi.hoisted` 建了一份内存态假仓位表 mock 掉 `./db/paper-trades`/`./db/watchlist`/`./signal-summary`，行为对齐真实语义（open/closed 状态过滤、按 item_name 查冷却），覆盖 16 个开平仓分支，另给 `fees.ts` 补了 7 个用例；用调低 `ENTRY_MIN_SCORE` 阈值的方式做过一次变异测试验证这套测试真的会在逻辑改错时失败，不是摆设。**动机**：模拟盘是接下来几周验证规则引擎准不准的唯一数据来源，这块逻辑算错了会悄悄污染平仓样本，属于 CLAUDE.md 测试规范里"算错了直接影响真实交易决策"的范畴，虽然文件本身不在 `lib/signals`/`lib/rules` 强制范围内。`npm run test` 从 36 条增到 59 条全过，`npm run lint` 干净。已 push + 云端 `docker compose up -d --build` 部署（这次改动只加测试文件，不影响运行时行为，重新构建是走标准部署流程，不是必须）。)
>
> 2026-07-24（**加观察池的联想搜索改本地目录，摆脱 Steam 限流 + 三处小修复**：①`middleware.ts` 按 Next 16 弃用通知改名 `proxy.ts`（函数名同步改，纯改名迁移，逻辑不变）；②模拟盘页（`/paper`）修了 React 19 新 purity 规则报的错——组件渲染期间不能直接调用 `Date.now()`，改法是包进独立函数（参考 `/positions` 页 `holdingDays` 的写法）；③观察池页（`/watchlist`）下方"搜索饰品名称"过滤框加了联想下拉（`components/features/watchlist-search-box.tsx`，用本地已加载数据实时匹配+高亮命中片段，不发请求）；④**主要工作**：排查"加入观察池"的联想搜索长期不可靠（用户反馈"搜'破碎'找不到饰品"），根因是 Steam 市场搜索接口（`lib/api/steam.ts` 的 `searchSteamMarketItems`）对云服务器（Oracle Cloud）出口 IP 429 限流是常态（哪怕换成标准浏览器 User-Agent 依然 429，确认是 IP 级限流不是反爬识别），且它每页固定返回 10 条按热度排序、不好翻页拿全，磨损变体多的饰品家族（比如"M4A1消音版 | 破碎铅秋"5 个磨损+纪念品+StatTrak 共 21 条结果）目标磨损经常被挤到第 9/10 名之后搜不到。**解决方案是不再实时查 Steam**：新建 `item_catalog` 表（迁移 `018_add_item_catalog.sql`），从 ByMykel/CSGO-API 数据集（`lib/api/cs-item-db.ts` 新增 `fetchItemCatalogEntries`）全量同步 21470 条（皮肤 21407 + 探员 63）到本地，`/api/items/search` 改查本地表（`lib/db/item-catalog.ts`，支持空格分词——"破碎铅秋 崭新"能直接命中），目录为空时才回退 Steam 实时查询保底；加观察池的 POST 直接用下拉条目自带的中文名/图标入库，不再额外调 Steam。顺带堵了两个 UX 口子：`add-watchlist-form.tsx` 之前不选候选也能直接手打回车提交，Steam 查不到精确匹配时会留一条永远没数据的死记录——现在必须选中下拉一条才能提交（按钮 disabled）；联想查询本身失败时也会把错误露出来（比如"联想查询失败：...429...请稍后重试"），不再和"确实没这个饰品"一样静默显示空列表。设置页（`/settings`）新增"同步饰品目录"按钮，新箱子/新探员发布后手动点一次刷新即可。生产环境已实测：搜"破碎"立刻列出全部磨损变体，添加后带图标。)
>
> 2026-07-22（**运维补课 + 两处线上 bug 修复**：①云端 VM 公网 IP 转成了 OCI Reserved Public IP（原地转换，地址 `170.9.25.139` 没变），HTTPS/PWA/推送不再受实例 stop/start 影响；②云端数据库此前完全没有备份，加了 `scripts/backup-db.sh`（用 better-sqlite3 在线备份 API，不阻塞写入，cron 每天 03:00 UTC 跑，保留最近7天+每周日份留8周）+ 本机 `scripts/pull-cloud-backup.ps1` + Windows 计划任务 `CS2-CloudBackupPull`（每天 21:00 本地时间把云端最新备份拉一份到本机做异地冗余）；③**修复 PWA 装不上的 bug**：Service Worker 注册代码之前只写在 `PushNotificationManager` 里（仅 `/settings` 页渲染），没访问过那个页面的用户浏览器从没注册过 SW，地址栏永远不出安装按钮——新增 `components/features/service-worker-register.tsx` 挪进根 layout，任意页面进站都会注册；④**AI 建议失败体验修复**：NVIDIA NIM 免费接口偶尔短暂过载返回 503，`lib/api/nvidia-llm.ts` 之前没有重试，失败直接把 `NVIDIA NIM 返回 HTTP 503` 这种技术错误文本显示给用户，`ai-insight.tsx` 里失败后点按钮只会切换收起/展开、不会重新请求，得刷新整页——现在请求失败自动重试一次，前端失败状态下按钮变成"重试"可以直接重新请求；⑥**模拟盘（paper trading）上线**：用户提议用前瞻记录验证规则引擎——观察池饰品买入信号 score≥30 自动模拟开仓（价格≥¥1，同一饰品同时只一笔、平仓后7天冷却），T+7 硬锁定期后出 SELL 信号或持有满30天平仓，卖出扣 C5 1% 手续费；逻辑在 `lib/paper-trading.ts`，挂在每小时同步之后（`lib/sync.ts`），表 `paper_trades`（迁移 016），页面 `/paper`。部署当晚第一轮 tick 开了 4 笔仓（Glock-18 Franklin/AK-47 Baroque Purple/P2000 Scorpion/P2000 Dispatch，都是 RSI 超卖触发的 score 30）。**已知偏乐观**：卖出按在售价算没模拟压价和流动性折价，看结果要打折扣；预计3-4周才有足够平仓样本谈胜率。⑤**顺带查了嫌疑分的真实审核确认率**：`manipulation_score` 触发的预警实际审核确认率只有 44%（7/16，样本很小），比最朴素的 `price_zscore`（71%，459个样本）还低，跟 PLAN.md 里已经记录的 coMove 特征不work、标注自我实现偏差风险、样本量小等问题是一致的，结论没变——嫌疑分现在的定位应该是"值得多看一眼的线索"不是"系统判定"，B1 嫌疑分校准闭环仍然卡在标注量和求购深度数据积累不够，没有立即能做的算法层面改进。细节见"运行架构"一节。)
>
> 2026-07-21 晚间（**云端迁移后第一次验收 + 部署稳定性/性能一批修复**：验收 Docker 迁移后的第一夜同步（无断档、无限流报错、求购数据正常入库），过程中发现 Basic Auth 的 `middleware.ts` 在服务器上是从没进过 git 的游离文件、本机 master 还有 2 个 commit 从没推到 origin/main——已推送 + 把 `middleware.ts` 收编进 git，现在本机/origin/云端三边一致；补了 **HTTPS**（Caddy 反代 + `sslip.io` 免费域名 + Let's Encrypt，公网只留 80/443，`3210` 端口收回只给本机），手机 PWA 安装和 Web Push 都要求真实证书，裸 HTTP+IP 之前是装不上的；发现并修复了手机端导航栏、持仓页统计卡片因为中文没有天然折行点、窄屏被挤成竖排单字的 UI bug；发现并修复了持仓/观察池页面的严重性能问题——同一个饰品一次页面渲染被重复查了 3~6 次同一张表，两次小时同步之间的重复页面渲染也一样重新查库，271 件持仓的 `/positions` 页面首字节耗时从 11-18 秒压到稳定 2-3 秒（`/watchlist` <1.3 秒）。细节见"运行架构"一节和第五节新增第 21-25 条踩坑）
>
> 2026-07-21 深夜（**生产环境从本机 Windows 迁到 Oracle Cloud（Always Free VM + Docker）**，本机 Windows 常驻采集器已停用，改成开发环境；公网访问加了 Basic Auth 最低限度保护；细节见"运行架构"一节和第五节第19/20条踩坑记录。当天早些时候还做了：PLAN.md 里 A2/D3/M2 三处过时的 Docker 描述改成实际的 Windows 原生方案——**这条现在又反过来过时了，因为晚上确实部署了 Docker，不用再改，历史记录留着**；C1/C2 用真正的全市场随机候选池复核过，结论比原先更不乐观，见第四节第4条和 REPORT-prediction-baseline.md；T+7 可行动标签第一版跑完，抓到并修复了评估方法的真实bug，"别追涨"和"洗盘回撤"是目前最可靠的复现信号；追涨风险信号上线；buy_price=0 资产离开库存不再落流水）
>
> 2026-07-20 更新：B2 剧本验证已跑完一版；洗盘信号上线；求购深度数据开始入库；C1/C2 预测模型第一版已跑完，结论是不满足上线门槛，见下；待审核队列清零后复核了特征 AUC 和聚类，结论稳定未变；**D3 PWA+Web Push 第一版已上线**；修了 start-collector.cmd 的中文编码坑；核实了 2026-07-15 交易保护新规但判断不能直接拿来批量改标注，理由见第五节

## ⚠️ 运行架构（先搞懂再动服务，2026-07-21 有重大变化）

**常驻采集器已经从本机搬到云端，本机 Windows 现在只是开发环境。**

- **生产环境**：Oracle Cloud Always Free 一台 Ubuntu 24.04 VM（`VM.Standard.E2.1.Micro`，1核1GB+4GB swap，region us-sanjose-1），跑 Docker（`docker compose up -d --build`，用仓库根目录的 `Dockerfile`/`docker-compose.yml`），容器 `restart: unless-stopped` + Docker daemon 开机自启（`systemctl enable docker`），VM 重启/容器崩溃都会自动拉起来，不依赖本机开不开机。
  - SSH 访问：私钥在本机 `~/.ssh/oracle_cs2_deploy`，`~/.ssh/config` 里配了别名，直接 `ssh cs2-cloud` 就能连上（IP/密码等具体连接信息不写进这份文档——这个仓库是公开的，别把访问凭证提交进 git）。
  - **部署/更新流程（2026-08-02 改成"别处构建、这里只拉取"，旧的就地 `--build` 流程已废弃）**：改完代码 → 本机 commit → `git push origin master:main`（远端默认分支是 `main`，本机开发一直用的是 `master`，push 时注意分支名要对上，见下方踩坑记录）→ **等 GitHub Actions 构建完**（`.github/workflows/build-image.yml`，约 3-5 分钟，构建+冒烟测试通过才会把镜像推到 GHCR）→ `ssh cs2-cloud` 上 `cd ~/cs2-skin-advisor && ./scripts/deploy-cloud.sh`（= `git pull` + `docker compose pull` + `up -d`，停机窗口是换容器的几秒）。**这台机器不再编译任何东西**——`docker-compose.yml` 里已经故意不写 `build:` 键，误敲 `up -d --build` 也不会真的开始编译。镜像地址 `ghcr.io/henryw0220/cs2-skin-advisor:latest`。deploy 脚本会核对镜像的 `org.opencontainers.image.revision` 标签和本地 git HEAD 是否一致，不一致会提示"CI 可能还在构建"并要求确认——**代码推上去到镜像可用之间有几分钟窗口，这期间部署只会换上上一版镜像，而表面上完全看不出来**。
  - **公网访问加了 Basic Auth**（`proxy.ts`，2026-07-24 前叫 `middleware.ts`，Next 16 把这个文件约定改名了，逻辑没变；读 `BASIC_AUTH_USER`/`BASIC_AUTH_PASSWORD` 环境变量，服务器 `.env.local` 里配的，本机 `.env.local` 没配这两个变量所以本机开发不受影响不会被锁）。这是最低限度的保护——项目本身没有登录体系，公网IP+没鉴权=任何人能看到持仓/策略/交易记录，Basic Auth 只是先堵上最基本的暴露，不是正经的账号系统，以后真要多人用还是得做真的登录。
  - **HTTPS（2026-07-21 补）**：VM 上装了 [Caddy](https://caddyserver.com)（`sudo systemctl status/restart caddy`，配置在 `/etc/caddy/Caddyfile`，**这个文件不在 git 仓库里，是直接在服务器上写的**），反代到 `localhost:3210`，域名用免费的 [sslip.io](https://sslip.io) 魔法域名 `170-9-25-139.sslip.io`（IP 编码进域名，自动解析到这台 VM，不用买域名），证书是 Let's Encrypt 自动签发/续期的真实受信任证书。访问地址变成 `https://170-9-25-139.sslip.io/`。**`3210` 端口已经从公网收回**（本机 iptables + OCI Security List 两层都删了这条规则），只留 80（跳转用）和 443，防止绕过 Caddy 走明文访问泄露 Basic Auth 密码。手机装 PWA、订阅 Web Push 都必须用这个新 HTTPS 地址——旧的 `http://IP:3210` 书签和已订阅的推送不会自动跟过来（Service Worker 按 origin 算），得重新访问、重新订阅。**2026-07-22 已把 `170.9.25.139` 转成 OCI 的 Reserved Public IP**（OCI 控制台：Compute → Instances → 实例 → Attached VNICs → 对应 VNIC → IP administration 标签页 → 该 Public IP 的 `⋯` 菜单 → Reserve IPv4 address，原地转换不改地址值），实例 stop/start 不会再丢这个 IP，域名/证书的这条风险已解除。
  - 数据库：`data/db.sqlite` 是 2026-07-21 一次性从本机拷贝过去的快照，之后云端自己独立积累。**本机的常驻采集器已经停用**（进程杀掉、开机自启文件夹里的 `.cmd` 改名成 `.disabled-moved-to-cloud` 禁用了，想切回本机运行就把后缀去掉），避免本机和云端各自采集出两份不同步的数据。
  - **数据库备份（2026-07-22 新增，此前云端完全没有备份）**：`scripts/backup-db.sh`（仓库根目录，`chmod +x` 后已放进云端 VM 的 crontab，`crontab -l` 能看到 `0 3 * * *` 这条）——用 better-sqlite3 的 `db.backup()` 在线备份 API（SQLite Online Backup，不停容器不阻塞写入），备份文件落在 `data/backups/`（bind mount，宿主机和容器都能看到），gzip 压缩，保留策略：7天内每天一份，超过7天只留每周日那份、留够8周，更老的自动删。**本机再加一层异地冗余**：`scripts/pull-cloud-backup.ps1` + Windows 计划任务 `CS2-CloudBackupPull`（每天本地时间 21:00 触发，`schtasks /Query /TN CS2-CloudBackupPull` 能查状态），把云端当天的备份 scp 拉一份到本机 `data/backups/cloud-db-*.sqlite.gz`，本机只留最近14天。**注意**：这套本机计划任务依赖 SSH 免密（`~/.ssh/oracle_cs2_deploy` + `~/.ssh/config` 的 `cs2-cloud` 别名）和本机开机联网，不是强保证，是云端 cron 备份之外的第二道保险，不是替代。
- **本机开发服务器**：3000 端口，`npm run dev`，`.env.development` 里 `PRICE_SYNC_DISABLED=1` 不做定时同步。本机的 `data/db.sqlite` 现在是部署那一刻的旧快照，会跟云端持续分叉——**本机这份数据库以后只用来看历史/跑一次性分析脚本，不代表当前真实状态，真实数据以云端为准**。
- Dockerfile/docker-compose.yml 原本注释写着"只给将来部署Linux服务器用"——现在这个"将来"已经发生了，注释里的判断已经落地，不用再改。

## 一、我们在做什么

CS2 皮肤交易决策助手（Next.js 16 + SQLite + Tailwind v4，本地单用户）。表面是持仓/观察池/信号看板，**真正目标：训练能识别、最终能预测"操盘"的模型**。业务地基（用户口述）：市场由拉盘主导；同收藏品上下级联动（上级被拉→下级炼金料跟涨）；用户有小道消息。策略：规则+统计攒标注，样本够了再上小模型（不碰深度学习）。全程交易由用户手动，系统只出信号和记录。

## 二、已完成（全部已 commit，PLAN.md 的 A2/A3/B3/B4/D2 已落地）

- **页面**：持仓（收益率/今日涨跌可排序，未填购入价的沉底）、观察池、饰品详情（SVG 走势图+嫌疑分）、异常审核 /anomalies、**交易流水 /ledger**、**设置 /settings（D3：Web Push 订阅/取消订阅/测试推送，PWA 安装引导）**
- **数据管道**：每小时同步+90 天 K 线回填（17万条）；扫描覆盖持仓+观察池；加观察池自动回填 90 天历史（无快照新品用 C5 兜底）
- **标注体系**：anomaly_events 三分类审核（确认操盘/外部事件/正常波动）+ manipulation_tags 操盘窗口（138 个，confidence=high 来自"已购=操盘"规则批量生成）+ item_metadata 收藏品/品质
- **检测与预警**：操盘嫌疑分 v1（特征/阈值来自真实标注 AUC 分析：24h波动率 0.72/24h涨跌 0.66/偏离均线 0.62；单小时尖峰 0.52 已排除）；嫌疑分≥60 自动预警（3天冷却）；同收藏品上级异动→下级联动预警（事件带 context）；**追涨风险信号（2026-07-21）**：`lib/signals/momentum-chase.ts`，24h涨幅>15%触发（阈值来自 REPORT-t7-actionable-labels.md：这个量级的历史涨幅未来7天平均收益-10.74%，70.8%概率为负），提示性质不进联动预警、不推送，新增 anomaly_events 的 `momentum_chase` 类型——**这个改动要 npm run build + 重启常驻采集器才会生效**（见第五节第10条）
- **交易流水（D2）**：库存同步发现资产消失→自动落卖出记录，卖价优先 C5 卖单匹配，匹配不到用户手填（选平台自动扣手续费：C5 1%/C5会员 0.5%/悠悠 1%，费率集中在 `lib/fees.ts`，gross 留档可重算）；月度已实现盈利。**6 月以来 5 笔已录全**（薇帕姐×3、阴谋者×4打包、喧嚣杀戮），合计盈利 ¥2852.36。**buy_price=0（开箱/来源不明）的资产离开库存不落流水**（2026-07-21）：这类东西没有成本价算不出盈亏，离开库存大多是开箱消耗不是真卖出，落一条待手填卖价的流水纯粹是噪音，逻辑在 `lib/inventory-import.ts`
- **产品化（D3）**：PWA（`app/manifest.ts`+`public/sw.js`+`next/og`生成的图标）+ Web Push（`web-push`库，`/settings`页管理订阅），目前接了嫌疑分预警和联动预警两类推送，详见下方第四节第6条
- **全市场候选池（C1/C2 复核用）**：`market_candidate_pool` 表（迁移 `015_add_market_candidate_pool.sql`）+ `scripts/backfill-candidate-pool.mjs`（抽样+K线回填）+ `scripts/backfill-candidate-pool-metadata.mjs`（补收藏品/品质，让 coMove 特征在全市场范围也能算），都独立于生产采集器。目标规模默认500，脚本增量补齐（已抽过的不重抽）；再跑 `node scripts/backfill-candidate-pool.mjs [目标数]` 扩大规模后记得重跑一遍 metadata 脚本。当前500个里363个有可用价格数据（其余是无真实成交的冷门磨损组合，抽样正常现象），500个全部匹配到了收藏品结构
- **移动端可用性（2026-07-21）**：导航栏（`app/(dashboard)/layout.tsx`）和持仓页统计卡片行（`app/(dashboard)/positions/page.tsx`）原本是纯桌面横向布局，窄屏下中文没有天然折行边界，被挤到极限宽度时浏览器逐字换行，手机上看起来像每个字单独占一行——导航栏改横向可滚动（`overflow-x-auto` + `whitespace-nowrap`），统计卡片/筛选栏窄屏下改纵向堆叠（`grid-cols-1`/`flex-wrap`），宽屏不受影响。
- **持仓/观察池页面性能（2026-07-21）**：两处问题叠加导致 `/positions`（271 件持仓）首字节耗时一度到 11-18 秒。①单次页面渲染内，同一个饰品的"各平台最新价"被 `pickReferencePlatform`/`computeSignalSummary`/显式调用各查一次，加上持仓页 totals 汇总又把整套逻辑重跑一遍，一个饰品实际查了 3~6 次同一张表——`pickReferencePlatform`/`computeSignalSummary`（`lib/signal-summary.ts`）加了可选的 `prefetched` 参数，调用方查一次传下去复用，totals 直接从已经算好的行数据汇总，不再重新查库。②两次小时同步之间页面反复渲染，价格数据根本没变但每次还是重新查库重新算——加了请求间缓存（`lib/signal-cache.ts`，`getLatestPricesByPlatform`/`getPriceHistory` 按饰品名/平台缓存，`insertPriceSnapshot`——快照唯一写入口——写完立刻让对应饰品缓存失效，不需要 TTL）。两项加起来 `/positions` 稳定态首字节压到 2-3 秒，`/watchlist` <1.3 秒。**2026-07-30 更新：②里的价格历史缓存已经删了**——页面后来改读 `item_signal_summaries` 预计算表、不再现场算信号，这层缓存要优化的热路径消失了，却在批处理循环里造成了每小时 OOM（详见顶部 7-30 记录和踩坑 28）。现在 `signal-cache.ts` 只剩 `getLatestPricesByPlatform` 那份（每饰品几行，无害），①的 `prefetched` 参数复用仍然有效、没动。
- **饰品目录（2026-07-24）**：`item_catalog` 表（迁移 `018`）从 ByMykel/CSGO-API 全量同步 21470 条皮肤+探员的中英文名/图标，`/api/items/search`（"加入观察池"的联想搜索用）查这张表，不再实时调 Steam 市场搜索接口——服务器 IP 被 Steam 429 限流是常态，且 Steam 搜索每页 10 条按热度排会截断磨损变体多的目标，本地表两个问题都没有。设置页 `/settings` 手动触发同步，`lib/item-catalog-sync.ts`。
- 备份：`data/backups/db-before-anomaly-review-20260717.sqlite`

## 三、当前卡在哪里

1. ~~212 条待审核异常~~ **已通过范围调整解决**：2026-07-20 确认 buy_price=0（开箱所得、购入渠道未知）的持仓不需要人工判断是不是操盘——用户没内幕消息根本没法判断。已把这类持仓从追踪范围里整体排除（`lib/tracked-items.ts`），/anomalies 页面默认也不展示这类饰品的待审核事件（数据库不删，只是不展示）。**现状：剩余待审核清零**（当时用户手动清了一批到 120 条，全部集中在 buy_price=0 品种，过滤后可见数为 0）。以后异常扫描/K线回填/价格同步的追踪范围只剩持仓里 buy_price>0 的（56 个）+ 观察池（1 个），同步的饰品数从 121 砍到 57，SteamDT 配额压力也缓解了（同一轮同步错误数从 21 降到 0）。**2026-07-31 更新：队列又涨回来了，而且这次是结构性的**——观察池 7-26 从 41 扩到 282（追踪总量 338）之后，每小时扫描的候选面大了一个量级，pending 重新堆到 1578 条。已做第 1 步机械归档降到 **1432 条**（详见顶部 7-31 记录），并把 `MIN_PRICE_FOR_ANOMALY_SCAN` 从 1 提到 5 从源头止血。**但真正的结论是"队列长度不是问题，审的东西不对才是"**：1425 条是 `price_zscore`，这类已有 485 个标注样本、边际价值趋零；真正稀缺的是 `manipulation_score`/`momentum_chase`/`washout_signal` 三类一共 33 条，以及收藏品配对案例。**2026-07-31 晚补充**：第 2 步查完是负结论（5-22 窗口里 542 条 pending 全是普通皮肤，叙事对它们没有方向预测，一条都不该标 `external`），第 3 步清单已交付；稀缺类型现有 41 条、全部是最近 4 天的，**是每天 6~10 条的持续流不是存量积压**。详见顶部记录。
2. ~~**联动特征（coMove）区分度仍验证不出来**~~ **2026-08-03 推翻：coMove 一直是有效特征，"验证不出来"是统计假象**。此前四次复跑记录的 0.500→0.507→0.518→0.520 是**池化 AUC**，两个缺陷叠加把它压在 0.5 附近：①池化把所有饰品的小时样本倒进一个池子，自相关严重；②更致命的是 **67 个有操盘标记的饰品里有 34 个（23 张印花 + 10 个探员 + 1 个皮肤）`item_metadata.collection` 是空的**，对它们 coMove 恒为 0、AUC 机械地等于 0.5，把整体稀释成了抛硬币。**按饰品检验 + 剔除退化样本后**：coMove **32/32 个饰品同方向**、各饰品 AUC 中位数 **0.613**；coMove24h **32/32**、中位 **0.749**（跟 maDev 0.744 同级，仅次于 vol24h 0.819 和 absReturn24h 0.760），符号检验 p≈0。**机制早就证实过**（喧嚣杀戮和 USP-S 次时代在 2026-05-22T04:00 同一小时异动），现在统计上也立住了。**真正的瓶颈从来不是"配对案例太少"，是覆盖率**——一半有标记的饰品压根没有可用的分组信息。**印花/探员没有收藏品是数据正确不是缺失**（那是皮肤才有的概念），但它们有天然等价物：印花属于同一赛事胶囊、探员属于同一组织。**已验证这个等价物成立**（`scripts/analyze-group-comove.mjs`，只读不写库）：按胶囊/组织分组后 **25/26 个饰品同方向、24 小时联动中位 AUC 0.651**，符号检验 p≈4×10⁻⁷；最强的几个是 Budapest 2025 和 Antwerp 2022 的 Holo 印花（0.68~0.81）。**要不要把这个分组落进 `item_metadata.collection` 是待决定项**，见第四节。
3. ~~成交量数据薄~~ **已部分解决（见下）**：SteamDT 批量/单品价格接口一直有返回求购价/求购数（`biddingPrice`/`biddingCount`），之前同步代码只存了在售数，求购这一侧被白白丢弃——不用新调研数据源，2026-07-20 已把这两个字段接进 `price_snapshots`（迁移 `013_add_bidding_depth.sql`），从这次重启起开始积累。C5 直连价格没有求购数据，这两列在 platform='C5' 的行固定是 null；K线回填的历史数据也没有（K线本身不带量），所以求购深度只能从现在开始往后攒，补不了历史。挂单深度调研仍然有缺口：这只是"求购"一侧，"在售"侧的深度（不同价位挂了多少单）还是没有，且这只是 SteamDT 聚合平台（BUFF/YOUPIN/STEAM 等），不是 C5 挂单簿。

## 四、下一步计划（按优先级）

**⏳【两个观察期，到点了才有事做，别提前下结论】**（2026-08-03 开始）

- ~~**`group_linkage` 预警质量小结** —— 建议 2026-08-10 前后跑~~ **2026-08-13 已跑完密度这一半，质量那一半卡在没人审**：8-03 ~ 8-13 共 **63 条、25 个饰品、日均 5.7 条**，最多的一天 13 条，单小时最多 6 条（同组成簇触发，符合设计）。**密度上完全不构成骚扰**（同期 `collection_linkage` 67 条、8-11 单日 22 条还更密）。但"多少条真正值得看"没有数据——63 条里 **61 条还是 pending**，只有 8-04 审掉 2 条（都 dismissed）。**决定推不推送还缺两件事**：① 审一批样本估"值得看的比例"（`node scripts/list-scarce-anomalies.mjs --sample 25 > data/xxx.md`，固定种子可复现）；② **`push_subscriptions` 现在是 0 台**，先把订阅恢复了再谈开推送，否则开了也发不到任何设备。开推送的做法仍是在 `lib/anomaly-scan.ts` 那段 `group_linkage` 循环里补 `pushNotifications.push(...)`。
- **卖出规则 v2 影子成绩单** —— 跑 `node scripts/report-shadow-sell-signals.mjs`（**先跑 `node scripts/build-market-baseline.mjs`**，基准现在读物化表，见迁移 023）。**2026-08-13 首份卖后前向超额**：`SELL` 2 条可评估、超额中位 **−7.61%**、2/2 判对；`SELL_STRONG` 1 条、**+1.49%**、判错；`HOLD` 611 条、+0.27%、53% 判对（无系统性判反）。**n=3 不下任何结论**，其余 7 条要到 8-14~8-20 才到期。**进度**：10 笔真实平仓（9 笔 `sell_rule_v2` + 1 笔 `sell_rule_v2_strong`、0 笔超时），影子表 2204 条里卖出类 10 条。超额口径下 10 笔全为正、中位 +18.01%，**但脚本自己的判据是"样本不足，不要下结论"**（可评估 10 笔 < 30 笔门槛）。**更要紧的是那份超额还回答不了"卖得对不对"**：反事实对照（提前卖 vs 持到第 30 天）10 笔**全部算不出来**——这 10 笔开仓在 7-24~7-31，第 30 天落在 8-23~8-30，**还没到**。判卖点准不准要看**卖出之后**的走势，那是影子报告那一侧的前向超额。**攒到三位数卖出信号再下结论**：回测里 15~20% 档的为负占比也才 61%，几十条样本的随机波动能盖过这个幅度。按 ≥15% 档占全部饰品-小时约 2.7% 估算，**要几周**。替换 v1 的门槛：卖出类命中率明显过半 + 超额中位数为负 + HOLD 那行没有系统性判反。


**0. 【三步走已收尾，剩一件事在项目所有者手上】异常审核队列**

第 1 步（机械归档 146 条 + 门槛提到 ¥5）已做完并部署；**第 2 步查完是负结论、不执行**；**第 3 步清单已交付**。三步的详细结论见顶部 7-31 晚的记录，这里只留下还需要动作的部分：

- **待项目所有者做：审 `data/scarce-anomalies-2026-07-31.md` 里的 41 条**（重跑 `node scripts/list-scarce-anomalies.mjs > data/scarce-anomalies-<日期>.md` 可刷新，要在云端容器里跑，本机 db 是旧快照）。**不必赶**——41 条全部触发不到 4 天、T+7 还没到，过一周重跑信息量更足。**这批必须本人判断**（理由见踩坑 35），不确定的直接标"正常波动"也是有效负样本。
- **稀缺类型是持续流不是存量**：每天新增 6~10 条，建议**每周重跑一次清单**跟上流量，而不是攒着等清仓式处理。
- **剩下 1401 条 `price_zscore` pending 不建议再投入人力**：这类已有 485 个标注、边际价值趋零，三次特征分析复跑都没质变。它们留在 pending 不影响任何训练管线（踩坑 34：`dismissed` 目前没有脚本读，pending 和 dismissed 对训练完全等价）。



1. **B2 操盘剧本验证第一版已产出**：`scripts/analyze-playbook-stages.mjs` + `REPORT-manipulation-playbook-stages.md`。结论：低位横盘（占比最大）、洗盘/砸盘（深回撤>15%接急拉，指纹清晰）、主拉升（急拉簇，涨速12.69%/天中位数，指纹清晰）三段能验证；吸货期和单纯横盘价格形态上分不开、会员进场看不见、出货期没有独立于普通回调的指纹——三个都卡在缺量/挂单数据。**洗盘指纹已实现上线**：`lib/signals/washout.ts`，接入 `lib/anomaly-scan.ts`，新增 `washout_signal` 异常类型（提示性质，独立冷却，不触发联动预警）。
2. **求购深度数据已开始积累**（2026-07-20 起）：等攒够几周数据后，重跑 `analyze-manipulation-features.mjs`/`analyze-playbook-stages.mjs` 时把 `bidding_count`/`bidding_price` 加进特征候选，看能不能补上吸货期/出货期指纹——这是之前卡住的地方，现在有数据来源了，缺的是时间。
3. ~~待审核队列已清零（见上）→ 现在随时可以重跑 `scripts/analyze-manipulation-features.mjs` 校准嫌疑分阈值 + 验证 coMove；episode 数变多后重跑 `analyze-playbook-stages.mjs` 让三个簇的边界更稳~~ **已重跑（2026-07-20）**：两个脚本的结论都稳定，不需要改阈值。`analyze-manipulation-features.mjs`——vol24h/move24h/maDev 三个特征的中位数和 AUC 几乎和首次校准时（`lib/signals/manipulation-score.ts` 注释里那版）一模一样（AUC 0.72/0.66/0.63 vs 首版 0.72/0.66/0.62），coMove 还是 0.507，没进一步移动。`analyze-playbook-stages.mjs`——episode 139→162（+21%），段落 1449→1829，k=6 聚类的崩跌/急拉/横盘三簇量级和首版基本吻合（详见 REPORT-manipulation-playbook-stages.md 复核记录），`washout.ts` 里 15%/2% 的阈值继续有效。**结论：不是"没跑"，是跑了没变化——几天的数据增量还不足以移动这些统计量，要看到显著变化大概率得等 coMove 那类样本更稀疏的特征攒够更长时间。**注意 buy_price=0 的开箱品从这次起不再新增快照，用它们做特征分析时数据会在 2026-07-20 之后断档，历史数据不受影响
4. **C1/C2 预测模型第一版已跑完，且已用真正的全市场随机候选池复核两轮（2026-07-21）**：`scripts/build-prediction-baseline.mjs` + `REPORT-prediction-baseline.md`。第一版逻辑回归名义上打赢了随机基准和 coMove 规则基准，但候选池只是131个用户自己跟踪的饰品，不是真随机样本。**第一轮复核**：`scripts/backfill-candidate-pool.mjs` 从全市场 2万+ 饰品里分层随机抽500个（363个有可用数据，独立于生产采集器，靠 K 线接口一次性回填90天历史，不碰限流很紧的批量价格接口）补进 full 池，模型对随机基准的领先幅度基本消失（N=7 只剩1.08倍，之前2.1倍）。**第二轮复核**：`scripts/backfill-candidate-pool-metadata.mjs` 给这500个样本补上收藏品/品质结构后再重跑，结果更差——模型在 N=1/N=3 上现在连随机基准都赢不了，coMove 规则基准的精度也跟着大幅下滑（不是模型一家的问题，说明"同收藏品联动"这个信号本身在全市场噪声下没那么可靠，跟 coMove 特征 AUC 一直卡在0.507是同一件事的另一个印证）。**不满足上线门槛的结论没变，但现在证据更扎实、也更悲观**。外部事件混淆问题（问题三）依旧没解决。下一步：等求购深度数据积累、找能分开操盘/外部事件的特征，C3 回测在这些问题解决前仍没意义。
5. ~~**coMove 验证需要更多轮收藏品配对补充**~~ **2026-08-03 重写：这条的前提是错的**。coMove 已经验证有效（见第三节第 2 条），瓶颈不是配对案例数量而是**分组覆盖率**——一半有标记的饰品（印花/探员）没有可用分组。**待项目所有者决定**：要不要把"印花按赛事胶囊、探员按所属组织"写进 `item_metadata.collection`。**收益**：coMove 的适用面从 33 个饰品扩到约 59 个，而且正好覆盖持仓里最重的那批（Austin 2025 Holo 印花 20 件、Col. Mangos Dabisi 探员 28 件），已实测这个分组的联动 AUC 中位 0.651。**代价/风险**：`lib/anomaly-scan.ts` 的同收藏品联动预警读的就是这一列，改完会**立刻**给这批印花/探员产生新的联动预警和推送（推送量会涨），而且"collection"这个字段的语义会从"CS2 官方收藏品"变成"联动分组"，以后读这张表的人要知道这件事。**分组规则本身不用猜**，从饰品名就能推（`Sticker | X (Holo) | Austin 2025` → 胶囊 `Austin 2025`；`Col. Mangos Dabisi | Guerrilla Warfare` → 组织 `Guerrilla Warfare`），脚本 `scripts/analyze-group-comove.mjs` 里已有实现，改成写库是小改动。用户手头如果还记得别的"同收藏品多个饰品都被拉过"的案例仍然欢迎补充，但那不再是这条路的瓶颈。
6. **D3 产品化第一版已完成（2026-07-20）**：PWA（`app/manifest.ts` + `public/sw.js` + `app/icon-192`/`app/icon-512`/`app/apple-icon.tsx`，图标用 `next/og` 的 `ImageResponse` 生成，没有额外美术资源）+ Web Push（`web-push` 库，VAPID 密钥在 `.env.local`）。订阅管理在新页面 `/settings`。推送订阅存 `push_subscriptions` 表（迁移 `014_add_push_subscriptions.sql`）。目前只接了两类高优先级信号：`lib/anomaly-scan.ts` 里嫌疑分预警（≥60）和同收藏品联动预警触发时会推送；z-score/成交量/洗盘这些事件型或提示型信号故意没接，太密会被当骚扰关掉通知。逃顶信号（D1）、预测高分（C3）还没做，做出来后要在 `scanForAnomalies` 里补进 `pushNotifications`。`scanForAnomalies` 从同步函数改成了异步（要 `await sendPushNotification`）——**已重启常驻采集器验证生效（2026-07-20）**。
7. ~~两个运维层面的待办（2026-07-21 发现）：①云端 VM 的公网 IP 还不是 OCI 的 Reserved Public IP...②`price_snapshots` 表一晚上从 65 万行涨到 88 万行...~~ **①已处理（2026-07-22）**：`170.9.25.139` 转成了 Reserved Public IP，见"运行架构"一节。**②仍是待观察项，不是待办**：截至 07-22 表已到 89 万+行，增速符合预期（观察池新品回填 90 天历史是一次性的，不是持续增长源），目前有请求间缓存顶着页面不慢，磁盘 36% 用量（29G 剩余）还早。**2026-07-30 更新**：表已到 **164 万行**（观察池扩容到 338 个饰品 + C5 每 10 分钟一写，这次是真的持续增长源了）；磁盘一度涨到 80%，但查清楚元凶是 Docker build cache 27.8GB 不是数据库，清理后回到 31%（见踩坑 31）。数据库本身的体积仍然不是瓶颈，**但"每饰品历史无限增长"这件事已经通过 `SIGNAL_HISTORY_WINDOW_DAYS` 在读取侧封顶了**，信号计算不再随表增长变慢/变重。加了数据库备份后这张表的体积也是备份大小的主要来源（当前压缩后单份 19M），保留策略（7天+每周8周）已经把这个成本控制住了，不用现在处理。
8. **数据库备份已补上（2026-07-22）**：云端 cron 每日 + 本机计划任务异地镜像，细节见"运行架构"一节。这是这次会话里认为的当前最大单点风险（云端此前完全没有备份，标注数据不可再生），已解决。

## 五、踩过的坑——绝对不要再踩

**标注/业务语义（搞错污染训练集）**
1. `buy_price>0` = 用户凭操盘消息买入（饰品级操盘标签）；`=0` = 开箱，未知。已按此批量标注过。
2. Souvenir MP7 | Tall Grass 是外部事件（5-22 纪念品可炼金更新），用户明确判断过，别被规则误翻。
3. 2026-05-22 Valve 更新两件事：纪念品可炼金（廉价纪念品暴涨）+ 取消印花胶囊（旧 Major 印花绝版重定价，前两周板块抢跑）。外部事件必须写明事由。
3b. **2026-07-15 Steam"交易保护"新规（"黄盾"）**：7天交易锁定+双方无条件可撤回，上线48小时内市场普跌（高端刀流动性归零）。**T+7 对策略的全部影响已写进 PLAN.md 原则6**（模拟脚本 `scripts/analyze-t7-forced-hold.mjs`：完美买点下也有60%的盘7天内过峰、仅50%第7天卖还净赚），核心结论：买入侧只有慢盘有意义、C3回测必须硬编码7天锁、逃顶(D1)优先级上调。**T+7 是不对称的（用户口述，2026-07-21）**：庄家吸货是中长线，拉升时手里的货早过锁定期，出一小部分就回本、之后卖什么价都是赚——被锁死的是拉升中段跟进的散户/会员，所以别指望新规逼庄家放慢节奏，快盘会继续存在。**已核实是真事件，但没拿来改任何 anomaly_events 标注**——用项目自己的数据比对过（131 个跟踪饰品里 19 个同期跌幅≥5%），方向和时间对得上，但自动扫描在这个窗口触发的 pending 候选清一色是低价品的价格上蹿，跟"普跌"方向对不上，说明这次事件对当前跟踪的饰品来说大多没跑出自身噪音基线。**教训：外部事件的新闻日期只是"提示去哪儿核对"，不能直接拿日期窗口反推批量改 status，会引入错误标签。**每一条要单独核对该品种自己的价格数据方向是否吻合，再决定标不标。
4. 操盘标记必须带时间窗口；¥0.5 以下低价品波动特征虚高（最小报价单位）。
5. **流水的价格都是单件价**（quantity>1 的打包出售要除以数量再入库），sell_price 是扣完手续费的净价，gross 留档。

**外部 API 实测（文档全不可信）**
6. SteamDT kline `type=1` 是 90 天滚动小时线（2160 条）非日线，无量，platform 参数有效；鉴权 `Authorization: Bearer`；batch 上限 100 名；限流 4005。
6c. **批量价格接口（`/price/batch`）和 K 线接口（`/item/v1/kline`）的限流规则完全不同**（2026-07-21 实测）：批量价格接口连续请求 2 次就 4005 报错，要等约 60 秒才恢复——之前"121个饰品→21个错误"其实就是2次批量请求里第2次（21个）撞了限流，跟总配额、总饰品数无关；砍到57个"修好"只是因为57个饰品一次批量请求（上限100）够用，不需要第2次请求。K 线接口反而没这个限制，250ms 间隔连续请求490次全部成功。**以后任何要扩大同步范围的改动，先确认会不会让批量价格接口单轮请求数超过100（需要拆成第2次请求），会的话必须加约60秒间隔，不能直接抄 kline 现有的250ms节奏。**
6b. SteamDT 价格接口（单品/批量）一直有返回 `biddingPrice`/`biddingCount`（求购价/求购数），之前只用了 `sellPrice`/`sellCount`，这两个字段被平白丢弃了一个月——加新特征前先看现有接口返回里有没有已经在拉但没存的字段，不要默认"没有就得调研新数据源"。
7. Steam 库存 count 上限 2000（超了 400），last_assetid 翻页；**任何一页失败必须整体报错**（同步按"不在=已卖"删行+落流水，半份列表会误删误记）。
8. ByMykel API：skins_not_grouped 才有 market_hash_name，skins.json 才有 collections/rarity，skin_id join；collections/crates 字段可缺省。
9. C5 卖单接口 status 枚举文档没写清（代码里试 2/3/4）；实测用户真实卖出也常匹配不到价（可能在悠悠出的），留 NULL 手填是常态。

**工程**
10. **数据库迁移只在进程启动时跑**：加 migration 必须重启 dev server（采集器要重 build+重启）。重启后用户浏览器旧标签页点导航会 404——让用户 Ctrl+F5，不是代码坏了。
11. Next 16：无 `next lint`（用 npm run lint）；params/searchParams 是 Promise；**无 searchParams 的页面会被生产构建静态预渲染成死数据，要 `export const dynamic = "force-dynamic"`**（/anomalies、/ledger 都踩过）。
12. JSX 中文文案用中文引号（react/no-unescaped-entities）；一次性脚本用 .mjs 放 scripts/（node 跑不了 lib 的 .ts）。
13. **schtasks 计划任务要管理员权限**，被拒就用启动文件夹（register-collector-task.ps1 已做兜底）；**.ps1 带中文必须 UTF-8 BOM**，不然 PowerShell 5.1 按 ANSI 读直接语法炸。**.cmd/.bat 反过来——中文注释千万别存 UTF-8（哪怕带 BOM 也不行,cmd.exe 批处理解析器不吃）**，`cmd.exe` 按系统 GBK 代码页读会把整个文件读乱，报一串跟内容毫不相关的"'x' 不是内部或外部命令"，`start-collector.cmd` 已经踩过、改成纯 ASCII 注释修好了（2026-07-20）。
14. PowerShell 里跑带复杂引号的 `node -e` 会解析炸，用 Bash 工具跑。
15. `.env*` 被 gitignore，非敏感的 .env.development 要 `git add -f`。
16. Commit 规范：`type(scope): 中文描述`，不准出现 claude/AI 字样，按模块拆且每个 commit 可独立构建；涨=红跌=绿。
17. 操盘标记/流水是喂模型和记账的，UI 从简（操盘标记列已按用户要求从持仓页移除，别加回去）。
18. **VSCode 内置 Simple Browser（编辑器里嵌的那个网页预览）不支持 Web Push**：点"开启推送"会报 `Registration failed - push service not available`，不是代码或 VAPID 配置的问题——这个内嵌 webview 本身没接入真正的推送服务。测 Push 相关功能必须在独立浏览器窗口（系统默认的 Chrome/Edge，不是编辑器里点开的那个标签页）里测，2026-07-21 已在独立窗口验证订阅+测试推送成功。
19. **GitHub 远端默认分支是 `main`，本机开发从项目一开始就一直用 `master`**，两者分叉（`git push` 时用 `git push origin master:main`，别直接 `git push` 图省事，会因为没有本地 main 分支报错或者建出一个新分支）——2026-07-21 部署时发现本机 master 领先 main 整整 52 个commit，从来没推送过，云服务器 clone 下来是一年前的老代码，查了半天才发现是分支不对，不是代码丢了。以后每次要把代码同步到云端之前，先确认本机 master 已经推到 main 了。
20. **Oracle Cloud Always Free 的 Ampere A1.Flex 经常显示 "Out of capacity"**（免费机型抢不到是常态，尤其 us-sanjose-1 这种单可用区的region），退而求其次用 `VM.Standard.E2.1.Micro`（x86，1核1GB，稳定能开出来）。**1GB内存编译 Next.js 会因为内存不够变得极慢/有OOM风险，必须先加 swap**（`fallocate -l 4G /swapfile` 那一套），不加会卡在 `npm run build` 出不来。另外 Oracle 的 Ubuntu 镜像自带 iptables 规则只放行 22 端口，云端安全组（Security List）开了端口不代表机器自己防火墙也开了，两边都要配（`sudo iptables -I INPUT <n> -p tcp --dport <port> -j ACCEPT` + `netfilter-persistent save`）。
21. **部署时手动传到服务器的文件很容易漏进 git，出问题前完全看不出来**：2026-07-21 迁移当晚 `middleware.ts`（Basic Auth 全部逻辑）大概率是手动 scp 上去的，从没 `git add` 过，`git status` 显示 `??`。因为 Docker build 时 `COPY . .` 直接拷贝磁盘上的文件（不管有没有入 git），容器照样正常工作，**表面上完全看不出问题**——只有下次有人在服务器上 `git clean -fd`，或者从干净的 clone 重新部署（灾难恢复/换新 VM）时，Basic Auth 会无声消失，公网直接裸奔。这是第 19 条踩过的"master 没推到 main"在新变体上又发生了一次。**教训：任何一次手动登录服务器改文件之后，收工前跑一遍 `git status`，确认改动最终会通过 `git push`+`git pull` 的正常流程走，而不是只活在这一台服务器的磁盘上。**
22. **Oracle Cloud 控制台里，实例详情页的"Security"标签页不是防火墙规则的地方**——那是"Shielded instance"（Secure Boot/vTPM 之类）的设置。真正的入站防火墙规则（Ingress Rules）在 **Networking 标签 → 对应 VNIC → 所在 Subnet → 页面里的 Security Lists → 点进去 → Security Rules 标签**，跟第 20 条已经记录的"两层防火墙"是同一件事，这次是给新端口（80/443）踩了一遍完整流程，顺便记下控制台导航路径，下次不用再摸索。
23. **免费的 `sslip.io`/`nip.io` 这类"IP 编码进域名"服务，能不买域名直接拿到真实 Let's Encrypt 证书**（Caddy 装好、防火墙开 80/443 端口就行，证书自动签发/续期），个人项目/单人使用场景够用，不用为了一个 HTTPS 走完整的买域名+配 DNS 流程。**代价是证书绑定这台机器当前的公网 IP**，IP 一变证书就失效，所以配的时候要么把 IP 转成 OCI 的 Reserved Public IP（免费），要么接受"IP 变了就得重新配一次 Caddyfile+重启"这个小成本。
24. **中文/CJK 文本在窄 flex 容器里会被逐字符换行，不是逐词换行**：西文靠空格分词，浏览器算 flex 子项的"内容最小宽度"时以单词为最小单位；中文没有天然分词符，行内断行允许发生在任意两个汉字之间，所以窄屏把 flex 子项挤到极限时，中文标签会退化成每个字单独一行的竖排效果（英文界面不会出现这个问题，容易在纯桌面测试时漏掉）。**排查依据**：看到界面上文字变成竖着一个字一个字排列，先怀疑是不是这个原因，不是组件坏了。**修法**：给会被挤压的文字容器加 `whitespace-nowrap`（配合 `shrink-0` 防止内容被压缩到消失），父容器视情况用 `overflow-x-auto`（保持单行，超出滚动）或 `flex-wrap`（允许整个子项换行，但子项内部文字不断行）。
25. **SQLite 单机小库/慢盘场景下，"重复查询"和"没有缓存"是两个独立的性能杀手，容易只治一个就以为解决了**：这次 `/positions` 页面先修的是"单次渲染里同一数据被查 3~6 次"（改法：函数加可选 `prefetched` 参数，调用方查一次到处传），这一步单独测（跳过 HTTP，直接测 DB 层）确认查询平均耗时降了 3 倍，但端到端页面耗时几乎没变——因为**两次页面渲染之间**（不同请求）同样的查询还在重复发生，且云端 1 核 1GB 机器内存小、系统盘缓存装不下 80 万+行的 `price_snapshots` 表，每次都有相当比例要真的碰磁盘。加一层"数据没变就不重查"的请求间缓存（用 `insertPriceSnapshot`——快照唯一写入口——做失效触发点，不需要 TTL）才是真正让端到端耗时掉下来的一步。**教训：诊断页面慢，要先用 curl 的 `time_starttransfer`/`time_total` 或直接在 DB 层单独计时，分清"单次请求内浪费多少"和"跨请求重复浪费多少"是两个不同量级的问题，只优化前者可能验证时看着有效、上线后感知不到。**

26. **Steam 市场搜索接口（`/market/search/render/`）对云服务器出口 IP 的限流很严格，换标准浏览器 User-Agent 依然 429**——确认是纯 IP 级限流，不是 UA/反爬识别问题。排查这类"接口偶尔用不了"的问题，先用 `ssh cs2-cloud "sudo docker exec <container> node -e \"fetch(...).then(r=>console.log(r.status))\""` 从容器内部实测真实状态码，别只看代码逻辑对不对（这次代码是对的，是外部服务在限流）；直接从 VM 宿主机 curl 测不出容器内的真实情况（当时宿主机 curl 200，容器内 fetch 429，两者出口路径不完全等价）。同时 Steam 搜索接口每页固定返回 10 条按热度排序，不代表能拿到全部匹配结果，磨损变体/StatTrak/纪念品版本多的饰品（一个基础皮肤能有 21 个变体）目标可能排到第 9/10 名之后，不是可靠的完整搜索。**教训**：凡是"用户输入 → 精确匹配已知实体"的联想搜索，优先建本地目录定期同步（这次用 ByMykel/CSGO-API 建了 `item_catalog` 表），不要依赖外部服务的实时模糊搜索接口，尤其对方接口的限流/分页/排序策略你控制不了。
27. **React 19 的 `react-hooks/purity` lint 规则会 flag 组件（含 Server Component）渲染期间直接调用 `Date.now()`/`Math.random()` 这类不纯函数**，哪怕组件是 `force-dynamic`、每次请求都重新渲染、语义上完全没问题——规则只做静态检查，不追踪运行时语义。修法是把调用包进一个独立具名函数（比如 `heldDaysFromNow(fromIso)`，内部调 `Date.now()`），组件里只调用这个函数，规则只分析组件/hook 函数体本身，不会顺着调用链往下追，包一层就不再报错（`/positions` 页的 `holdingDays` 一直是这么写的，`/paper` 页当初漏了）。

28. **给"批处理循环"和"页面渲染"共用同一层缓存，迟早会 OOM——而且缓存本身还会让你诊断时看错方向**：2026-07-27 起云端每小时崩一次，根因是 `getPriceHistory` 的全量结果被永久缓存在全局 Map 里，页面渲染场景下这是对的（同一饰品反复渲染只查一次），但 `scanForAnomalies`/`precomputeSignalSummaries`/`runPaperTradingTick` 都是**遍历全部追踪饰品的批处理循环**，对它们来说缓存把"一次只留一个饰品"的流式循环变成"全部饰品的完整历史同时驻留"，内存直接变成 O(饰品数 × 历史长度)——两个因子都在随业务增长，撞墙只是时间问题。**判断准则**：一层缓存要问"最坏情况下同时驻留多少条目"，如果答案是"全部实体 × 每个实体的全部历史"，那它不是缓存，是内存泄漏的慢动作版本。**这次还有个额外教训**：加这层缓存时（2026-07-21 优化 `/positions` 页面）它是对的，但后来页面改读 `item_signal_summaries` 预计算表，**它要优化的热路径消失了、却没人回来删它**——重构掉一条调用路径时，要顺手检查为它加的缓存/索引/预计算是不是也该跟着走。**还有**：修的时候要同时做"降峰值"（去缓存，338 → 1 个饰品）和"封上限"（加时间窗口），只做前者的话 C5 高频 tick 每天给每个饰品多写 144 行，迟早再撞回来。
29. **Docker `restart: unless-stopped` 会把"每小时崩一次"伪装成"服务一直正常"**：这次 3 天崩了 90 次，但页面能打开、价格在涨、`docker compose ps` 显示 `Up`，从外部完全看不出问题——因为每次崩溃后 20 秒就被拉起来了，而崩溃点又恰好在"价格已写库、后续处理还没跑"的位置，所以最显眼的指标（快照数）反而是健康的。**排查手法**：`docker inspect --format '{{.RestartCount}}'` 是最快的体检项（正常应该是 0 或个位数，看到 90 就知道有事），配合 `docker logs | grep -c 'FATAL ERROR'` 和"最后一次成功完成的业务日志是什么时候"（这次是 `[price-sync] 定时: ...` 那行，最后出现在 7-27 14:23，比崩溃开始早整整一小时，一下就定位了时间点）。**教训：定时任务的健康检查不能只看"进程在不在"和"数据有没有在涨"，要看"完整一轮跑完的日志有没有按预期频率出现"**。
30. **一轮流水线里，崩溃点之后的步骤会静默全部消失，而且各自的症状看起来毫不相关**：`lib/sync.ts` 的顺序是 SteamDT 写快照 → C5 写快照 → `scanForAnomalies` → `runPaperTradingTick` → `precomputeSignalSummaries`，崩在第 3 步的结果是：价格数据完全正常（前两步跑完了）、异常扫描只完成一半（循环跑到一半崩）、联动预警彻底归零（它在循环**之后**）、推送一条不发（在函数最末尾）、模拟盘停摆、页面数据冻结在三天前。**这六个症状表面上像六个独立的 bug，实际是同一个崩溃点**。**排查手法**：拿到"某功能失效"的报告时，先对照流水线顺序问"它排在第几步、前面几步的产物正不正常"，能一次性锁定崩溃位置；**反过来写代码时也要意识到，把耗内存/易失败的步骤排在流水线中间，等于给后面所有步骤加了一个隐形的单点依赖**。
31. **Docker build cache 会无声吃满磁盘，且不会出现在任何常规监控里**：这次排查顺带发现磁盘从 36% 涨到 80%，27.8GB 全是反复部署攒下的 build cache（`docker system df` 才看得到，`du -sh` 常规目录看不出来）。清理用 `sudo docker builder prune -f --filter until=24h`（一次回收 19.47GB，80% → 31%）——**用 `until=24h` 过滤而不是无参数全清**，这样当天构建的热缓存会保留，紧接着的下一次构建还能复用，不至于从零开始编译。磁盘满了的后果不只是构建失败，是 SQLite 写不进去。
32. **1 核 1GB 的机器上，"应用在崩溃循环" + "同时在构建" 会把构建时间从 4-8 分钟拖到 60+ 分钟**：这次部署修复时正好撞上这个组合，负载 3.69（单核）几乎全是 swap 的 I/O 等待，`docker-buildx` 进程 32 分钟只用了 41 秒 CPU 时间——**看 CPU% 会以为构建卡死了，实际是在等磁盘**。判断构建是否还在推进，看 `docker system df` 里 Build Cache 的大小有没有涨（这次从 27.32GB 涨到 27.8GB，说明活着）。**下次遇到同样情况**：先把崩溃中的容器停掉再构建（它反正也在崩，停掉只损失几个采集 tick），能省大量时间。

33. **要"降噪"时先用已标注样本回算每个候选筛法的信噪比，不要凭直觉挑维度——直觉挑错的概率很高**：2026-07-31 清理 1578 条待审核队列时，我最初提的是"按 z-score 强度砍掉弱信号"（听起来天经地义：z 越大越可疑）。用已人工审核的 485 条回算，结论完全相反——z `6-7` 确认率 68%、`8-10` 76%，**几乎无差别**，而 **z≥15 只有 31%**（极端 z 大多是数据毛刺不是真行情），按强度砍会误伤 133 条真操盘。真正干净的维度是**触发价格**：`<¥5` 确认率 **0%（121 条无一命中）**、`¥20-100` **99%**。**教训：手上只要有几百条人工标注，任何"筛掉噪音"的规则都应该先拿它回算命中率，一次查询就能证伪一个想当然的方案；凭直觉筛的代价是悄悄扔掉真样本，而且事后完全看不出来。**这条已写进 `lib/anomaly-scan.ts` 的注释防止重蹈。
34. **搞清楚标签"实际流向哪张表"再谈标注价值，UI 上的状态字段可能根本没进训练集**：这个项目的 `/anomalies` 页面有 confirmed/external/dismissed 三分类，很容易以为三种都是训练样本。实际追下来：`confirmed` 会调 `addManipulationTag()` 写 **`manipulation_tags` 表，那才是特征脚本读的正类**（anomaly_events 上的 confirmed 只是审核痕迹）；`external` 被脚本直接读作排除窗口；**`dismissed` 没有任何脚本读**——特征脚本的负类是"不在操盘窗口也不在外部事件窗口的其余时段"，所以 pending 和 dismissed 对当前训练**完全等价**。**意义**：①判断"清理队列会不会破坏数据"时，答案取决于流向而不是字段名，这次确认了只要不碰 confirmed/external 就是安全的；②反过来，如果将来想让 dismissed 真的当负样本用，得先改脚本，那时必须能区分"人判断过"和"机器归档"——所以这次特意新开 `archived` 状态而不是复用 `dismissed`。
35. **AI 不能替用户打"需要领域内幕信息"的标注，哪怕用户主动要求**：项目所有者问"异常提醒我不会判断，能不能全帮我处理了"。不能——这些标签的全部价值来自他的小道消息（价格数据里没有的信息），由 AI 按价格形态判断"是不是操盘"等于**用模型的特征去生成模型的标签**，是循环论证，跑出来的 AUC 会虚高且事后无法察觉。**判据**：`price_zscore` 人工确认率 68%（485 样本）vs `manipulation_score` 36%——如果标签只是价格形态的复述，这两个数字不会差这么多，差异本身就是人类标注含有额外信息的证据。**正确做法是重新定义问题**：不是"替他标"，而是①用机械规则（不含领域判断）把无从判断的条目移出队列；②按已标注样本回算，指出他在审的东西边际价值已趋零（1425 条 price_zscore vs 33 条稀缺类型），**把注意力导向真正稀缺的样本**；③剩下确实需要判断的整理成小清单，附上决策所需的上下文让他自己判。
36. **CS2 饰品名自带 `|` 分隔符，生成 Markdown 表格必须转义**："AK-47 | Redline" 这种命名是 CS2 的通用格式，直接塞进 Markdown 表格会把一列切成两列，且后面所有列全部错位——生成的表看起来"有内容"但列对不上，肉眼扫一遍很容易漏掉。转义成 `\|` 即可。**校验方法**：数每行**未被反斜杠转义**的竖线数量，应等于列数+1，全表应只有几种取值（对应几张表），出现杂七杂八的数字就是有行错位了。注意 `awk -F'|'` 这类工具会把转义的 `\|` 也当分隔符，用它校验会得到假阳性。

37. **诊断"队列/表为什么在涨"，要看 `created_at` 的分布形状，不是 `detected_at`——这两个字段回答的是不同问题**：2026-07-31 验证 ¥5 门槛效果时差点得出错误结论。`anomaly_events` 有两个时间字段，`detected_at` 是"异常发生在哪一刻"（业务时间），`created_at` 是"这行什么时候被写进来"（工程时间）。按 `detected_at` 看，<¥5 的事件散布在 4~7 月，像是每小时扫描长期在稳定产生；按 `created_at` 看，**203 条全部集中在 7-17/7-20/7-27/7-28 四天**，一眼就能看出是批量回扫（`scanHistoricalPriceAnomalies`，观察池加新品时一次性扫 90 天历史）的产物，跟每小时扫描没关系。**两者的止血手段完全不同**：批量回扫要在回扫入口设门槛、且只在扩容时才发作；每小时流入才需要调实时检测的阈值。**判据：`created_at` 集中在少数几天 = 批量作业；均匀分布 = 常态流入。**这次还顺带证伪了一个已经写进上一版交接文档的预估（"门槛生效后新增事件数应该明显低于之前"），实际每日新增 13 条 vs 之前 16/19/8/11/17，毫无变化——**部署完成不等于预期效果成立，验收要拿数据对着预估本身核，不能只确认"服务没崩、代码生效了"**。
38. **"命中数量"在做筛选前毫无意义，先按业务语义拆一刀再看还剩多少**：第 2 步原计划按日期窗口找外部事件候选，5-22 窗口命中 542 条 pending，听起来是笔大买卖。但 5-22 的两条叙事（纪念品可炼金、取消印花胶囊）**只对纪念品和印花做方向预测**，而这 542 条按品类拆是 **539 条普通皮肤 + 3 条布章，纪念品印花一条都没有**——叙事对它们什么都没预测，可标注目标实际是 0。真正吻合叙事的那批（55 条纪念品/印花）**早在之前就已经标完了**，剩下的自然全是不吻合的。**教训：一个"看起来很大的待处理集合"，在按它真正的判定条件拆之前，规模数字不构成任何工作量或价值的估计**；先拆一刀往往直接得到"不用做"的结论，比做完再发现标错了便宜得多。这次也是踩坑 3b（不能拿日期窗口反推批量改 status）的一次正面验证。

39. **喂给固定尺寸 UI 组件的数据，必须在源头按那个尺寸封顶——否则它会随采集频率无声膨胀**：持仓页那个走势图是 80×28 像素，最多能显示 80 个点；但喂它的 `recent_prices` 读的是原始快照序列，C5 同步从每小时提到每 10 分钟之后，同一段"近 7 天"从 169 点变成约 1000 点，页面 HTML 涨到 1.52MB（单个 `polyline points` 属性 7951 字节 × 121 行）。**没有任何报错，图看起来也一模一样**——多出来的 800 多个点全落在同一批像素列上，画出来的线一个像素都不差，只是让每次渲染多传多解析 1MB。**判断准则**：任何"喂给图表/列表/摘要"的数据，问一句"展示端最多能用多少条"，答案是固定值的话就在**生成侧**截到那个值，别把"有多少给多少"当默认。**这条跟踩坑 28 是同一个病根的两种表现**：28 是缓存条目数随实体数增长撑爆内存，39 是单条数据的长度随采集频率增长撑爆页面，共同点都是**某个量的上界依赖于一个会随业务增长的变量，而当初写的时候那个变量恰好很小**。**排查手法**：页面慢先量 HTML 字节数（`curl -w '%{size_download}'`）再看 TTFB——这次 TTFB 只有 0.69~1.39s（服务端不慢），1.52MB 的体积才是真凶，只盯 TTFB 会得出"没问题"的错误结论。
40. **1 核机器上"边构建边服务"等于站点下线，部署方式本身要重新设计**：`docker compose up -d --build` 期间实测连 `ssh` 跑一条 `docker ps` 都 120 秒不返回，用户侧是白屏。**应急手段**（验证有效）：给构建进程 `renice -n 19`、给应用进程 `renice -n -5`，站点 TTFB 立刻从"无响应"回到 1.48s。**但 `pgrep -f` 的模式千万别包含 `node`**——会把应用自己的 Node 进程一起降级，正好搞反；要用 `sudo docker inspect --format '{{.State.Pid}}' <容器>` 精确拿到应用 PID。**这只是应急，不是解法**：正解是把构建挪出这台机器（别处构建镜像、机器上只 pull + 重启），或者按踩坑 32 的经验构建前先停掉应用容器。**2026-08-02 已按正解解决**：GitHub Actions 构建 + GHCR + `scripts/deploy-cloud.sh` 只拉取，机器上不再编译任何东西，上面这套 renice 应急手段以后用不到了（留着是因为"1 核机器上 CPU 密集任务会让服务不可用"这个教训本身还成立，以后在这台机器上跑任何重活都要先想到这条）。**搬走构建之后新出现的注意事项**：构建机器上没有 `.env.local`，任何 `NEXT_PUBLIC_` 前缀的变量都会被内联成空字符串（Next 文档明确点名"同一个镜像部署到多个环境"就是这个场景），见踩坑 41。

41. **`NEXT_PUBLIC_` 前缀的环境变量是构建期常量，不是运行时配置——把构建搬到 CI 的那一刻它们会静默变成空字符串**：2026-08-02 把镜像构建挪到 GitHub Actions 时发现的。Next 文档写得很直白："如果你构建一个 Docker 镜像部署到多个环境，所有 `NEXT_PUBLIC_` 变量都会被冻结成构建时求值的结果"。这个项目里 `NEXT_PUBLIC_VAPID_PUBLIC_KEY` 有两处引用，**两处都会坏，而且坏得没有任何报错**：①客户端订阅（`push-notification-manager.tsx`）拿到空 key，浏览器抛的是一句跟原因毫无关系的错；②**服务端发推送**（`lib/api/web-push.ts`）——这处最阴，它是服务端代码，直觉上"服务端当然是运行时读环境变量"，但只要变量名带 `NEXT_PUBLIC_` 前缀，构建时同样会被内联，容器里 env 明明配着却读到空值，推送全部静默发不出去。**修法**：改名成不带前缀的 `VAPID_PUBLIC_KEY`（服务端直接运行时读），浏览器需要的那份由 `/settings` 页在请求时读出来当 prop 传给客户端组件。**连带的坑**：`/settings` 没有 `searchParams`，会被生产构建静态预渲染（踩坑 11），那样"请求时读"又变回"构建时读"，必须加 `export const dynamic = "force-dynamic"`。**判断方法**：镜像要在别的机器上构建时，先 `grep -rn "NEXT_PUBLIC_" --include="*.ts*" .` 把所有引用点过一遍，每一处都问"构建机上有这个值吗"。
42. **Next 的文件追踪（standalone 输出）会把 `path.join(process.cwd(), ...)` 认出来的数据文件一起拷进产物**：本机构建时实测 `.next/standalone/data/db.sqlite` 有 240MB——它是顺着 `lib/db/client.ts` 里的 `path.join(process.cwd(), "data", "db.sqlite")` 拖进来的，`db/migrations/` 同理（这个反而是好事，但**不要依赖它**，Dockerfile 里仍然显式 `COPY db ./db`，因为这是启发式行为不是契约）。**后果**：如果镜像要推到公开仓库，真实持仓数据库就跟着出去了。**三层防住**：`.dockerignore` 排除 `data/`（源头）、Dockerfile 里 `RUN rm -rf ./data`（确定性，不依赖追踪器行为）、CI 里一步硬检查（`ls /app/.env* /app/data/db.sqlite` 有命中就让构建失败）。**这类"闸门坏了完全静默"的东西必须有自动检查**，跟踩坑 21（手动 scp 上服务器的文件从没进 git，只有灾难恢复时才会发现）是同一个模式。

43. **"字段名对得上"不等于"语义对得上"：存量指标喂进按流量设计的阈值，会变成一个永不触发、永不报错的死信号**：`price_snapshots.volume` 存的是 `sellCount`（在售挂单**数量**，存量），而 `lib/signals/volume.ts` 的判据"最新一期 ≥ 前 7 期均值的 2 倍"是按**成交量（流量）**设计的。存量在小时尺度上不会翻倍——实测 325 个饰品这个比值中位数 **1.000**、最大 **1.167**、达标 **0 个**。**后果是级联的且完全静默**：这一项永远不贡献分数 → 规则引擎的 SELL（≤-40）只剩"RSI超买+趋势走弱"这一条路 → 而这两个状态几乎互斥（实测 0/325 同时满足）→ **SELL 永远不出现** → 模拟盘 230 笔仓位一笔也平不掉 → 用来验证规则引擎的实验拿不到任何卖出侧样本。**排查方法（可复用）**：怀疑某个信号"从来不触发"时，不要去读代码猜，**把线上所有标的的 score 按权重反解成分量组合**，看哪些组合的观测数是 0——0 的那些要么是互斥状态，要么是死信号，两种都值得追。**更一般的教训**：接入一个新字段时要问的不是"名字像不像"，而是"它是存量还是流量、单位是什么、变化的时间尺度是多少"，这三个问题任一错配都会让下游阈值静默失效（踩坑 6b 记过同一个接口里被白白丢弃一个月的字段，这条是它的反面：字段接进来了但语义理解错了）。**2026-08-03 之五已收尾**：`lib/signals/volume.ts` 连同两个调用点全部删除，异常扫描那条通道的历史事件数是 **0 条**（同期 `price_zscore` 2043 条），`volume_ratio` 也从 `IAnomalyMetric` 里删了。**删除时验证了"325 个饰品的 score/action 逐个不变"这个事实**——但用的不是"改动前后跑一遍 diff"（那会被同期行情漂移污染，分不清是代码改动还是价格变了），而是用生产管线的完全相同口径复算了全部 325 个饰品的比值：**最大 1.2200，达到 2 倍阈值的 0 个**，即那个分支在当前数据下不可达，等价性是数学结论。**方法可复用**：要证"删掉这段等于没删"，去证它的触发条件在全量数据上不可达，比事后 diff 干净得多。列名 `volume` 有意保留（改名要动迁移），语义警告写进了 `lib/types.ts` 的列定义——**这种注释必须挡在代码里，只写在 HANDOFF 里下一个人还是会照名字用**。

44. **判"某个特征没区分度"之前，必须先过三关，否则统计量会静默地给出相反的结论**：2026-08-03 一天之内连踩三种。**第一关：样本是不是自相关的？** 池化 AUC 的分母是"小时样本数"，但同一饰品的一波行情能贡献几百条高度相关的样本，名义 916 条的有效样本量可能只有个位数——改成"按标的各自算 AUC + 对命中标的数做符号检验"，求购深度那六个特征里能立住的两个跟池化排序**不一样**。**第二关：有多少样本对这个特征是退化的？** coMove 按饰品检验第一版是 32/67、中位 AUC 恰好 0.500、p=0.687，看着像彻底没信号；实际是 34 个饰品（印花/探员）根本没有收藏品信息、该特征恒为 0，**全并列时 AUC 机械地等于 0.5**，把命中率稀释成了抛硬币。剔除退化样本后是 **32/32、中位 0.749**，结论完全反转。**"中位数恰好等于 0.500" 本身就是退化的强烈信号**——真实的无效特征会在 0.5 附近抖动，不会精确落在 0.5。**第三关：特征的量纲跟标的规模有关吗？** `bidAskRatio`（绝对厚度比）池化 AUC 最高、各饰品 AUC 中位数也最高，但只有 10/16 同方向、p=0.227——贵价品和廉价品的挂单簿厚度天差地别，池化时"正类恰好来自挂单更厚的那批标的"就能把 AUC 抬起来，那是选择效应。除以自身基线之后（`bidRatio`）才稳。**三次的共同点：都不报错，都让人得出与真相相反的结论，都只能靠主动去查才发现。**
**2026-08-03 之五补第四关和一条负结论**：**第四关：效应量够不够大？** 符号检验只回答"方向是不是一致"，样本够多时 AUC 0.53 也能给出 p=0.0000——`absReturn1h` 57/66 同方向、p≈0，但各饰品 AUC 中位数只有 **0.531**，而生产在用的三个特征是 0.744~0.819。**统计显著和经济显著是两回事**（卖出规则 v2 里"5~15% 档 p=0.0000 但不进规则"是同一条判据）。现在的门槛是各饰品 AUC 中位数 ≥0.60。**负结论**：`scripts/recheck-degenerate-features.mjs` 把三关套在 `absReturn1h`/`absZ`/`bidSpread` 上，**一条旧结论都没推翻**，而且六个特征里"AUC 恰好等于 0.500"的饰品数全是 0——**这批不存在 coMove 那种退化稀释**。所以 coMove 那次反转是个例，不是"还有一堆被埋掉的特征"，以后别再靠直觉翻旧账，要翻就跑那个脚本。**另外：任何这类复检都必须带阴性对照**（那个脚本里是 `volumeRatio`，已被两条独立路径确认是死信号），如果连它都被"救活"，说明是方法在造信号而不是特征真的有效——没有阴性对照的复检是自说自话。

45. **指标的名字写着"天"，代码喂的是"小时"——名字和实现差一个数量级，而且两边都不报错**：`lib/rules/evaluate.ts` 的 `MA7`/`MA30`/`RSI14` 按行业惯例应该指 7 日 / 30 日 / 14 日，`CLAUDE.md` 的注释规范里也拿"RSI 周期是 14 天"当例子。但 `lib/signal-summary.ts` 喂给它们的是 `resampleHourly(history)` 之后的**小时桶**，所以线上跑的实际是 **7 小时 / 30 小时 / 14 小时**——"价格跌破 MA7 且 MA7 在 MA30 下方"比的是 7 小时均线和 30 小时均线，是个**日内尺度**的判据，而不是名字暗示的周线级趋势。**为什么一直没被发现**：两种解释下代码都能跑、都不报错，输出也都是合理的数字；只有回过头问"这个指标到底在测什么时间尺度"才会暴露。**这条的排查价值**：2026-08-03 回算 RSI/趋势时**两种尺度都算了一遍**，才能把"概念没用"和"尺度接错了"分开——实测日尺度一致更强（趋势走强 −1.54% vs −0.83%、RSI≥70 −3.57% vs −0.94%），确认尺度确实接错了，**但即便接对幅度仍然不够覆盖换手成本**，所以结论没变。**一般教训**：接手任何带周期参数的指标，先确认"喂进去的一格是多久"，不要信名字——这跟踩坑 43（存量当流量用）是同一类错误的两个面，43 是**单位**错，45 是**时间尺度**错，都属于"词对了、东西不是那个东西"。

46. **AUC 只能用来淘汰，不能用来录取——它对"只有尾部有台阶"的分布结构性不敏感**：2026-08-13 回算 RSI/趋势时抓到的，**这是这一轮最重要的方法学发现**。数字摆在一起就很刺眼：阳性对照 `return24h`（v2 全部阈值的地基、唯一在赚钱的特征）按标的 AUC 中位数 **0.567**，而被判死刑的 RSI 是 **0.578**、趋势是 0.546/0.591——**已上线的那个比被否掉的那个 AUC 还低**。原因不是哪个数算错了，是**问错了问题**：AUC 衡量"整个取值范围上的排序能力"，而 v2 的全部价值在**尾部**（24h 涨幅 >30% 那一档超额 −18.69%），中间区段它并不比抛硬币强多少；RSI 则相反，全域都是零点几个百分点、哪一档都不够成本线。拿 AUC 当录取标准，会**同时**放进 RSI 这种全域微弱的、又几乎否掉 return24h 这种真正能赚钱的。**改法（已写进 HYPOTHESES 第五节的关口）**：AUC 低于下限直接出局（淘汰用途保留），但过了下限**什么都不说明**；录取判据必须是**分档之后尾部档位的超额幅度 vs 成本线** + 该档的样本饰品数 + 符号检验。**注意跟踩坑 44 第四关那条 0.744~0.819 不是同一个口径**（那是"操盘期 vs 平时"的判别任务，这里是"预测未来超额为负"），两组数字不能放一起比大小——但要改的是同一件事：**两处都不能拿 AUC 当上线许可**。这条跟 44 的三关是互补的：44 讲的是 AUC **算错了**（自相关、退化样本、量纲混杂），这一条讲的是 AUC **算对了也答非所问**。

51. **`docker compose restart` 不会重新读 `.env.local`——改环境变量必须 `up -d`**：2026-08-14 改 `NVIDIA_MODEL` 时实测：`restart` 之后容器里 `echo $NVIDIA_MODEL` 还是旧值，因为 restart 只是把同一个容器停了再起，而环境变量是**创建容器**那一刻从 env 文件烘进容器配置的；`docker compose up -d` 会发现配置变了、重建容器，新值才生效。**HANDOFF 里两处旧记录写的"改 `.env.local` + `docker compose restart`，秒级生效"是错的**——`PAPER_TRADING_DISABLED`、`C5_FAST_SYNC_DISABLED` 这两个止血开关同样要用 `up -d`。**这类"文档写着能用、实际不生效"的操作步骤比代码 bug 更阴**：真出事时你以为开关已经拉下去了。`up -d` 用的是本地已有镜像，不重新构建，停机窗口同样是换容器的几秒。

50. **模型 id 会被平台悄悄下线，而调用方只会拿到一个 HTTP 410——AI 理由生成整条静默死了半个月**：2026-08-14 抽查新文案时才发现，`/api/signals?withReason=true` 返回 `NVIDIA NIM 返回 HTTP 410`。原因是 NIM 把 `deepseek-ai/deepseek-v4-flash` 这个**不带日期的别名**下线了，现在只剩 `deepseek-ai/deepseek-v4-flash-0731`。**症状完全静默**：页面上 AI 理由那块只是不显示，`reason_cache` 最后一条停在 7-31 没人注意（那也是这个项目"坏了不报错"的第三个例子，跟推送归零、异地备份是同一族）。**排查办法**：`curl -H "Authorization: Bearer $NVIDIA_API_KEY" $NVIDIA_API_BASE_URL/models | grep -o '"id":"[^"]*"'` 列出当前还活着的 id。**改法**：`.env.local` 里 `NVIDIA_MODEL=deepseek-ai/deepseek-v4-flash-0731`。**以后选模型优先用带日期的具体版本**，别名随时会被回收。

49. **对着生产库跑重活会把常驻采集器饿死，而且没有任何报错**：2026-08-13 一次补 7 个窗口的基准回填，长时间占着写锁 + 打满磁盘，实测 **40 分钟里 `price_snapshots` 一条没写**——每小时同步和 10 分钟快速同步全部停摆，容器日志里既没有错误也没有那两条例行日志，**看上去就像什么都没发生**。杀掉分析进程之后立刻恢复。**规矩**：重活（首次回填、一次补多个窗口、任何要全表扫的分析）**对副本跑，只有最后合并结果那一下碰生产库**——`scripts/copy-db-for-analysis.mjs`（SQLite 在线备份 API，不阻塞写入）→ `CS2_DB_PATH=<副本> node scripts/<重活>` → `scripts/merge-market-baseline.mjs`（一次短写事务）。日常增量只补当天那一个窗口，数据量小，直接跑没问题。**这条跟踩坑 40（边构建边服务抢 CPU）、48（工作集装不进内存）是同一台机器的第三个面：抢写锁和磁盘。**

48. **这台机器上"能不能跑得完"是 I/O 问题不是 CPU 问题——工作集装不进内存**：2026-08-13 把大盘基准做成物化表时踩的。首次全量回填要过一遍 `price_snapshots`（270 万行、库 750MB），而机器只有 954MB 内存，**缓存不住工作集**：第一版把样本攒在 JS 数组里，跑了 25 分钟一行没写出来，`top` 里 `kswapd0` 常驻 D 状态——不是在算，是在 swap 上打转。改成**落 SQLite 临时表、让 SQLite 去排序求中位数**之后内存占用跟样本数解耦。第二个坑紧接着出现：**better-sqlite3 的裸 `INSERT` 每条都是一个隐式事务**，一个饰品两千多条样本 × 七百个饰品 = 一百多万次提交，同样跑不完；按饰品打包成一个 `db.transaction` 才正常。最终 1104 秒跑完、写出 111 天。**判断方法**：进程长期处于 **D 状态**且 CPU 时间增长很慢，就是 I/O 瓶颈，优化算法没用，要么把中间结果交给 SQLite、要么缩小扫描范围。**跟踩坑 40 是同一台机器的两面**：40 讲的是 CPU 抢占（边构建边服务），这条讲的是内存装不下、退化成磁盘随机读。顺带记一个背景开销：这台 VM 上 Oracle 自己的 `snap` agent（`agent` + `oci-wlp`）常年吃掉 **35% + 15%** 的 CPU——**以后判断"是不是我们的东西慢"，基数是一半不是全部**，跑重活时要把这一半先扣掉再看。
    **规矩（2026-08-14 补）**：裸 `INSERT` 那个坑在这个项目里已经是**第二次以不同面貌出现**了——第一次是 `lib/db/item-catalog.ts` 全量替换 2.1 万条目录（那次是为**原子性**包的事务，顺带躲过了性能问题），这次是回填基准的一百多万条样本（为**吞吐**才不得不包）。同一个机制、两种理由，说明它跟意图无关，是 better-sqlite3 的默认行为。**以后凡是写入量级到十万条的脚本，默认先包 `db.transaction`，不要等它慢了再说**——等的代价是一次 25 分钟的空跑加一次事后归因，而先包上的代价是一行代码。

47. **绝对路径写在 git 之外 = 改不动、坏了也不报错**：2026-08-13 查异地备份为什么断了 8 天，根因是仓库从 `d:\cs2-skin-advisor` 挪到了 `d:\AI Solution\cs2-skin-advisor`，而 Windows 计划任务 `CS2-CloudBackupPull` 的动作里写的是**绝对路径**，指向一个已经不存在的 `.ps1`。**PowerShell 对这种情况返回 −196608**（`-File` 指向的文件不存在，已手动复现确认），而计划任务只把这个码记在 `Last Result` 里——没有人会主动去看，于是 08-05 到 08-13 每天 21:00 都"跑了"，每天都失败，8 天无人知晓。**这跟踩坑 21（手动 scp 上服务器的文件从没进 git）是同一个模式**：仓库外的配置没有版本、没有校验、没有告警。**处理办法有两条，缺一不可**：① 能改成相对路径/`$PSScriptRoot` 的一律改（脚本内部本来就是这么算的，坏的只是入口那一行）；② 改不了的（计划任务动作本身、Caddyfile、crontab、`.env.local`）**必须进"仓库外配置清单"并写明"丢了怎么重建"**，见第六节。**顺带两个 PowerShell 的坑**（写心跳文件时踩的，都会让下游 `JSON.parse` 静默失败）：PowerShell 5.1 往原生命令 stdin 里写会带 **UTF-8 BOM**；把 JSON 拼进原生命令参数时**双引号会被参数解析吃掉**（写出来是 `{host:DESKTOP,...}`）。稳的做法是写本地临时文件再 `scp`。

## 六、仓库外配置清单（挪目录/换机器/灾难恢复时照着过一遍）

这一节是踩坑 47 逼出来的：这些东西**不在 git 里**，改了没人知道、坏了不报错。每条都写清"在哪"和"丢了怎么重建"，只写位置不写重建方法的话，真出事时还是得现想。

| 配置 | 位置 | 丢了怎么重建 |
|---|---|---|
| **异地备份计划任务** | 本机「任务计划程序」→ `CS2-CloudBackupPull`（每天 21:00，S4U） | 管理员 PowerShell 跑 `scripts/fix-backup-task.ps1`（它会把动作指回当前仓库路径并试跑一次）。整个任务都没了的话，手工新建：触发器每日 21:00，动作 `powershell.exe -ExecutionPolicy Bypass -WindowStyle Hidden -File "<仓库>\scripts\pull-cloud-backup.ps1"`，勾选"不管用户是否登录都运行"+"错过时间尽快补跑" |
| **Caddy 配置** | 云端 `/etc/caddy/Caddyfile` | 内容就三行（域名 `170-9-25-139.sslip.io` → `reverse_proxy localhost:3210`，外加 access log）。写完 `sudo systemctl reload caddy`，证书 Let's Encrypt 自动签 |
| **云端备份 cron** | 云端 `crontab -l` 里那行 `0 3 * * * ~/cs2-skin-advisor/scripts/backup-db.sh >> ~/backup.log 2>&1` | `crontab -e` 把这行加回去（脚本本身在 git 里，记得 `chmod +x`） |
| **云端 `.env.local`** | 云端 `~/cs2-skin-advisor/.env.local` | 变量清单见 CLAUDE.md。**VAPID 那三个不能随便重生成**——换了公钥所有已有订阅立刻失效（要重新在每台设备上订阅）；API key 从 C5/SteamDT/NVIDIA 后台重取；`BASIC_AUTH_*` 自己定；`NVIDIA_MODEL` 要用**带日期**的具体版本（别名会被下线，见踩坑 50）。**改完必须 `docker compose up -d`，`restart` 不重新读这个文件**（踩坑 51） |
| **SSH 密钥与别名** | 本机 `~/.ssh/oracle_cs2_deploy` + `~/.ssh/config` 的 `cs2-cloud` 条目 | 私钥丢了没法找回，要在 OCI 控制台给实例加新的公钥（Console connection 或重建实例）。别名条目照 HANDOFF 运行架构一节重写 |
| **本机采集器回退路径** | 开机启动文件夹（**当前是空的**，原来那个 `.disabled-moved-to-cloud` 文件已经不在了） | `scripts/start-collector.cmd` **还在 git 里**，需要回退到本机采集时，把它复制一份到 `shell:startup` 即可。也就是说回退能力没丢，只是入口要重新放 |
| **GHCR 镜像与 CI** | `.github/workflows/build-image.yml`（在 git 里）+ GitHub 仓库的 Actions 权限 | 工作流本身在仓库里；推 GHCR 用的是内置 `GITHUB_TOKEN`，只要仓库 Actions 的 package 写权限开着就行 |

*持久记忆存了第 1、3 条的摘要，交接以本文档为准。*
