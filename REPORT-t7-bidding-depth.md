# 求购深度（A6）的 T+7 分档表 —— 负结论

> 2026-08-23。脚本 `scripts/build-t7-bidding-baseline.mjs`，库是 `cloud-db-2026-08-22` 的本机副本，
> 基准口径 **`b75ea4af5`**（1019 行、2026-04-18 ~ 08-13）。
>
> **这份报告回答的是 `WAITING.md` 第 7 项**：求购深度是"买入侧唯一活着的候选"，
> 现在数据够了，它到底行不行。

---

## 一句话结论

**不行。四个特征、25 个档位、三个跑法，没有一档的绝对收益中位数够得着 6.7% 的往返成本——
连正数都不是。** 全部档位的绝对收益中位数落在 **−6.67% ~ −1.38%**。

**这不是"样本还不够"**：325 个饰品、6347 条去重样本，比当初那个 16 饰品的口径宽了 20 倍。
**也不是"阈值没调对"**：最厚的那一档（`bidRatio > 2.0`）和最薄的那一档（`< 0.7`）
绝对收益差不到 0.4pp，而成本线在 6.7%。

**按 HYPOTHESES §2.4 的先例，`bidRatio` / `bidAskRatioRel` 从 🔵「已验证未上线」
改判为 🔴「已证伪——作为买入条件」。**⚠️ 注意限定语，见下面第四节。

---

## 二、样本

| | |
|---|---|
| 有求购数据的饰品 | **373** 个（2026-07-20 ~ 08-22，共 34 天，649428 行） |
| 过历史门槛（≥360 小时）的 | **325** 个 |
| 去重后样本（每饰品每天一条） | **6347** 条 |
| 参考平台构成 | YOUPIN 167 / BUFF 88 / C5 70 |
| 缺基准丢弃 | 7928 条，全部落在 08-14 / 08-15 两天（**预期内**：7 天窗口要到 day+7+6h 才定型） |

**⚠️ 一处必须写明的口径分岔**：历史门槛用的是 **360 小时**，不是价格脚本那个
`24×(7+14)=504`。求购数据总共才 34 天，套 504 会把样本砍掉一大半。
**后果是本表参与的饰品集合跟 `build-t7-entry-baseline.mjs` 那张表不同，
两张表的档位不可以直接并排相减**（四点五 #4.6）。

**⚠️ 参考平台也是分岔的**：这里按"哪个平台的求购数据最多"选，
不是价格口径的 `C5 > BUFF > YOUPIN`。因为 `bidding_count` 只有部分平台返回，
沿用价格口径会让绝大多数饰品直接没有求购数据。
这正是 HANDOFF 第四节 0.6 点名"要逐个复核"的四类之一（**任何按平台分组的统计**）。

---

## 三、主表（`--hour-pick first`，主口径）

**录取判据（预先写死）：绝对收益中位数 ≥ 6.7% 且 超额中位数 > 0。**
买入侧决定的是"要不要动用现金"，对照物是**现金**不是市场 ⇒ 必须看绝对值。
只过超额那条的含义是"跌得比大盘少"，那不是买点。

### `bidRatio`（求购数 / 自身 168h 均值）—— 主候选

| 档位 | 样本 | 饰品 | 绝对中位 | 超额中位 | 绝对为正的品 | 够成本线 |
|---|---|---|---|---|---|---|
| <0.7（异常薄） | 511 | 174 | **−3.34%** | −0.21% | 21/111 | 否 |
| 0.7~0.9 | 1355 | 317 | **−3.51%** | −0.22% | 50/290 | 否 |
| 0.9~1.1（常态） | 2731 | 323 | **−3.42%** | −0.03% | 29/316 | 否 |
| 1.1~1.3 | 1156 | 312 | **−3.26%** | +0.37% | 45/281 | 否 |
| 1.3~1.6 | 427 | 190 | **−3.54%** | +0.05% | 17/122 | 否 |
| 1.6~2.0 | 112 | 65 | **−5.91%** | −2.24% | 4/32 | 否 |
| >2.0（异常厚） | 55 | 33 | **−3.71%** | +1.04% | 3/13 | 否 |

**池化 AUC = 0.517**（下限 0.50，过了不构成任何录取理由）

**读法**：**这张表最要紧的不是哪一档最好，是七档全都挤在 −3.3% 附近。**
从"异常薄"到"异常厚"跨了三倍的求购厚度，绝对收益动了不到 2.7pp，而且**没有单调性**
（最差的是 1.6~2.0 那档，不是最薄那档）。**那个 −3.4% 是这一个月的大盘，不是信号。**

### `bidAskRatioRel`（买卖盘厚度比 / 自身基线）

| 档位 | 样本 | 饰品 | 绝对中位 | 超额中位 | 够成本线 |
|---|---|---|---|---|---|
| <0.7 | 591 | 183 | −3.42% | −0.33% | 否 |
| 0.7~0.9 | 1425 | 319 | −3.41% | −0.21% | 否 |
| 0.9~1.1 | 2535 | 323 | −3.42% | +0.01% | 否 |
| 1.1~1.3 | 1115 | 311 | −3.16% | +0.49% | 否 |
| 1.3~1.6 | 502 | 215 | −3.94% | −0.66% | 否 |
| 1.6~2.0 | 116 | 73 | −5.67% | −1.67% | 否 |
| >2.0 | 63 | 38 | **−6.67%** | −2.26% | 否 |

**池化 AUC = 0.511。** **方向甚至是反的**：买盘最厚那一档表现最差（−6.67%）。

### `bidCountChg24h`（求购数 24h 变化率）—— 唯一有一点单调性的

| 档位 | 样本 | 饰品 | 绝对中位 | 超额中位 | 够成本线 |
|---|---|---|---|---|---|
| <−20% | 864 | 269 | −3.56% | −0.22% | 否 |
| −20~−5% | 1498 | 314 | −3.68% | −0.24% | 否 |
| −5~+5%（平） | 1760 | 316 | −3.63% | −0.14% | 否 |
| +5~20% | 1132 | 294 | −3.54% | +0.07% | 否 |
| +20~50% | 684 | 284 | **−2.54%** | +0.65% | 否 |
| >+50% | 409 | 175 | **−2.55%** | +0.77% | 否 |

**池化 AUC = 0.529（四个里最高）。** 方向是对的、单调性也有，
**但幅度是 1.1pp，成本线是 6.7pp——差一个数量级。**
**这是"排序能力 ≠ 幅度"（踩坑 46）的又一个实例**，跟 `drawdown48h` 那次一模一样。

### `bidSpread`（买卖价差）—— 阴性对照

已在标注口径下证伪（9/16、p=0.402）。T+7 口径下同样全档不过，
**池化 AUC = 0.511**。**阴性对照表现得像阴性对照，说明这套口径没有系统性地把什么都推成正的。**

---

## 四、⚠️ 证伪的到底是什么（这一节比上面的表重要）

**证伪的是"用求购深度做买入条件"，不是"求购深度没有信息"。** 两件事，别合并：

| | 标注口径（操盘 vs 平时） | T+7 口径（买入侧） |
|---|---|---|
| `bidRatio` | **14/17 个饰品同方向、p=0.0064**（AUC 中位 0.583，⚠️ 幅度不稳见踩坑 52） | 全档够不着成本线 |
| `bidAskRatioRel` | **15/17、p=0.0012**（AUC 中位 0.601，同样不稳） | 同上，且方向反 |

**两边都是真的。** 求购厚度**确实**能把操盘期和平时分开（这是检测任务），
**但"落在这一档之后未来 7 天能赚多少"够不着换手成本**（这是交易任务）。
**这正是 HYPOTHESES §2.4c「跨任务搬运」那条**：同一个特征的数字不能从一个任务搬到另一个。

**顺带查清一个已记录的数字**：§2.3 原记的是 `bidRatio` **15/16、p=0.0003、AUC 中位 0.747**。
2026-08-23 重跑是 **14/17、p=0.0064、中位 0.583**。
**但"从 0.747 掉到 0.583"这个说法本身就不对**——那不是一次下降，是一个高方差量的两次取值。
把窗口一段段拉长重量：**0.695（08-02 窗口）/ 0.711（08-09）/ 0.618（08-16）/ 0.583（全量）**，
**非单调，跨度 0.164**。两个成因：① 窗口拉长把对照池撑开；
② **标注集本身在变**——用今天的标注重跑 08-02 那个窗口是 **0.695 而不是 0.747**。
**多一天数据就能让它动 0.02。** 详见 HANDOFF 踩坑 52。
**稳的是方向不是幅度**：符号检验一路 13/16 → 14/17、p 0.0106 → 0.0064，方向一次没翻——
这正是"AUC 只能淘汰不能录取"（踩坑 46）的又一条独立理由。

---

## 五、三个跑法（预先声明，不是跑完挑好看的）

| 跑法 | `bidRatio` 池化 AUC | 有没有档过成本线 |
|---|---|---|
| **主口径** `--hour-pick first` | 0.517 | 无 |
| 去重稳健性 `--hour-pick noon` | **0.495 ⇒ 触发淘汰线** | 无 |
| régime 对照 `--since 2026-07-28`（切掉 07-26 C5 提频 / 07-27 OOM 两条边界） | 0.523 | 无 |

**第二行是这张表最硬的一条**：仅仅把"每天取哪个小时"从最早换成最接近正午，
池化 AUC 就从 0.517 掉到 **0.495、跌破 0.50 的淘汰线**。
**一个真实的效应不会因为改变取样小时就翻到中性以下。** 那 0.017 是噪音。

---

## 六、这对买入侧意味着什么

**买入侧现在没有活着的候选了。**

- §2.4 已经证伪"用现有**价格**特征做买入侧"（35 个档位、4 个特征、3 个跑法全军覆没），
  并明写"**要重开只能靠新数据源**——现成的只有求购深度"。
- **求购深度就是那个新数据源，现在它也跑完了，也是负的。**

**所以不要再用"换模型 / 加特征组合 / 拉长窗口"去救**（拉窗口那条 HANDOFF ② 已预先封口）。
**下一个买入侧候选必须是真正的新数据源**，而 §2.6 已经列清楚缺的是什么：
**在售侧的价位分布**（现在只有总数没有分布）、**真实成交量**（无数据源）。
两个都不是调参能解决的。

**⚠️ 但不要顺手把求购深度整个丢掉**：它在**检测**任务上仍然是有效特征（第四节那张表），
而 §2.5 里"外部事件 vs 操盘可分"那条——**三类 `bidRatio` 中位数 操盘 1.0824 > 平时 0.9802 >
外部事件 0.8022，外部事件期间求购盘反而更薄**——是这个项目**唯一**一条直接打在
C1/C2 核心死结上的线索。**那条仍然活着，而且优先级最高。**
它卡的是外部事件样本只有 48 条，不是特征无效。

---

## 七、复现

```
node scripts/build-t7-bidding-baseline.mjs <库文件>
node scripts/build-t7-bidding-baseline.mjs <库文件> --hour-pick noon
node scripts/build-t7-bidding-baseline.mjs <库文件> --since 2026-07-28
node scripts/analyze-bidding-depth-features.mjs <库文件>   # 标注口径那一半
```

特征公式只有一份，在 `scripts/bidding-features.mjs`，两个脚本共用
（HANDOFF 第四节 0.5：双份实现的危害是**单边修改**，让第二份直接 import 第一份）。
