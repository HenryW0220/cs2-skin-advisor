# 项目计划书：CS2 操盘检测与预测

> 目标读者：项目所有者 + 未来的开发会话。现状见 HANDOFF.md，本文只谈"接下来怎么走"。
> v2：已并入项目所有者对全部开放问题的答复（2026-07-19）。
> 核心决策已定：**交易永远由用户手动操作，系统只做数据、信号与记录**；最终产品形态是 App（详见阶段 D）。

---

## 0. 终极目标与阶段拆解

**终极目标**：系统能在操盘的不同阶段给出可操作的决策——
- 拉盘**开始前/早期**提示买入（吃到主升段）；
- 拉盘**进行中**识别处于剧本哪个阶段（拿得住、不被洗下车）；
- **出货期**提示卖出（逃顶,对已持仓的人最值钱）。

| 阶段 | 名称 | 一句话目标 | 状态 |
|---|---|---|---|
| A | 数据与标注完备 | 标注可信、数据不断档 | **进行中** |
| B | 检测强化 | 规则/统计层把"正在操盘"看准 | 部分完成 |
| C | 预测模型 | 从"识别现在"到"预判将来" | 未开始 |
| D | 决策闭环与产品化 | 信号→建议→流水复盘→App | 未开始 |
| E | 多用户与商业化 | 远期,不急 | 仅记录 |

**原则（每个阶段都适用）**
1. 样本不够就不上 ML,规则+统计顶着;上 ML 也从小模型开始(逻辑回归/GBDT),不碰深度学习。
2. 每个特征/阈值都要有数据依据(重跑 `scripts/analyze-manipulation-features.mjs` 说话),不拍脑袋。
3. 外部事件(版本更新等)是最危险的混淆源,标注时必须写明事由,建模时单独作为一类对照。
4. 时间序列任务一切按时间切分训练/验证,严禁未来数据泄漏。
5. **系统不碰交易执行**,只出信号和记录,买卖永远用户手动。
6. **T+7 约束（2026-07-15 交易保护新规后的市场现实）**:买入的饰品锁定 7 天才能再交易,所以"预测会不会被拉"不够,能变成钱的预测必须是"**买入锁 7 天后卖出仍有净收益**"。用 170 个历史窗口模拟过(`scripts/analyze-t7-forced-hold.mjs`):就算每次都精准买在窗口开始当天,60% 的盘 7 天内就过峰,第 7 天卖只有 50% 还净赚、只有 21% 保得住峰值收益的一半——**快盘就算预测对了也吃不到,买入侧信号只对慢盘有意义**。且 T+7 是不对称的:庄家吸货是中长线,拉升时手里的货早过了锁定期,出一小部分就回本、之后卖什么价都是赚,真正被锁死的是拉升中段跟进的人——所以**不能指望新规逼庄家放慢节奏,快盘会继续存在**,识别"这是不是慢盘"是买入侧预测绕不开的一环。卖出侧不受影响:持有超 7 天的仓位随时能卖,逃顶信号(D1)价值不降反升。

---

## 阶段 A：数据与标注完备

**A1. 清完待审核标注**（人工,项目所有者）—— **范围已重新定义,实质完成**
- 2026-07-20 项目所有者判断:buy_price=0(开箱所得,购入渠道未知)的饰品没内幕消息没法判断是不是操盘,不用审。已把这类持仓整体移出追踪范围(`lib/tracked-items.ts`),/anomalies 默认也不展示这类饰品的待审核事件——不是清空数据库,是从"要不要审"这件事上彻底排除。
- 排除之后,可审核(buy_price>0)的待审核队列已经是 0。之后的常态就是"每小时扫描 56 个 buy_price>0 持仓 + 观察池,新增异常照旧走 /anomalies 审"。
- 完成标准:pending(可见部分)清零 ✅,之后保持"每周过一遍新增异常"的节奏。

**A2. 常驻数据采集**（开发,最高优先级）—— **已部署到云端（2026-07-21），彻底解决"电脑不开就断档"**
- 现在价格同步挂在 dev server 里,电脑不开/服务不跑就断数据,断档直接毁时序特征。
- **方案演进**:一开始是 Windows 原生生产构建 + 登录自启（`scripts/start-collector.cmd`），独立于开发环境但仍然绑定"本机开不开机"这个前提，2026-07-19~20 用这个方案但断过至少一次（HANDOFF 记录过）。**2026-07-21 迁到 Oracle Cloud Always Free 的 Ubuntu VM，用回了最初设计的 Docker 方案**（`Dockerfile`/`docker-compose.yml`，`restart: unless-stopped` + Docker daemon 开机自启），本机 Windows 采集器已停用降级为纯开发环境。之前放弃 Docker 是因为 Windows bind mount 下 SQLite 多进程写锁不可靠，这个限制只存在于 Windows 本机，Linux 服务器上不存在，所以 Docker 方案在云端是对的选择。详细部署方式和踩坑见 HANDOFF.md"运行架构"一节。
- 顺路调研更多数据渠道(成交量、挂单深度、成交笔数):SteamDT/C5 的未用接口、其他公开源;找到就加进采集器,找不到就靠现有接口高频抓在售量自己攒。
- **2026-07-20 有进展**:SteamDT 价格接口本来就有返回 `biddingPrice`/`biddingCount`(求购价/求购数),之前同步代码只存了在售侧、求购侧被丢弃——已补上(迁移 `013_add_bidding_depth.sql`),不算新数据源,是把已经在拉的字段存下来。从这天起开始积累,历史数据补不了;C5 直连价格和 K 线回填都没有这两个字段。**仍然缺的**:在售侧的挂单深度(不同价位挂了多少单,不是总在售数)、成交笔数、C5 平台自己的挂单簿数据。
- 完成标准:采集器连续 30 天无断档;"量"类数据至少有一路稳定入库(✅ 求购数据这一路已达成,继续找在售深度/成交笔数)。

**A3. 数据面扩容 = 观察池**（开发一次性改造 + 项目所有者日常使用）
- 已定:不做单独的"市场面板",**观察池就是扩容入口**——项目所有者会持续把关注的单品加进观察池。
- 开发侧配套:把 K 线回填、异常扫描、嫌疑分的覆盖范围从"仅持仓"扩到"持仓+观察池"(加入观察池时自动回填 90 天历史),让新加的品立刻有完整数据。
- 对照组问题(防"模型学成识别你会买什么"):鼓励往观察池加一部分"确定没庄的正常品"做对照。

## 阶段 B：检测强化（把"正在操盘"看准）

**B1. 嫌疑分校准闭环**
- A1 标完后重跑特征分析,更新 manipulation-score 阈值/权重;以后每次大批量标注后都重跑。
- ~~重新验证联动特征(coMove)~~ **2026-08-03 已验证,结论是有效**:此前四次复跑记录的 AUC 0.500~0.520 是**池化统计的假象**——67 个有标记的饰品里 34 个(印花/探员)没有收藏品信息、该特征恒为 0,AUC 机械等于 0.5,把命中率稀释成抛硬币。按饰品检验 + 剔除退化样本后:coMove **32/32 同方向**、中位 AUC 0.613;coMove24h **32/32**、中位 **0.749**(跟 maDev 同级),符号检验 p≈0。瓶颈不是配对案例数量而是**分组覆盖率**;印花按赛事胶囊/探员按组织分组也已验证成立(25/26,中位 0.651,`scripts/analyze-group-comove.mjs`)。要不要写进生产 `item_metadata.collection` 见 HANDOFF 第四节第 5 条。

**B2. 操盘剧本验证（本阶段核心,直通预测和逃顶）—— 第一版已完成,见 `REPORT-manipulation-playbook-stages.md`**
- 项目所有者给出的做盘剧本(待数据证实/修正):
  1. **低位横盘**:单品处于价格较低阶段; ✅ 已验证,是全程占比最大的状态
  2. **吸货期**:庄家长时间吸货,价格小幅上涨; ⚠️ 价格形态上和纯横盘分不开,需要量/挂单数据才能验证
  3. **会员进场**:庄家把消息散给会员,会员拿货,价格再抬一级; ❌ 现有数据看不出这个子阶段
  4. **洗盘/砸盘**:为压住拿货成本,拉高一小段后砸盘压价,诱更多小散入场; ✅ 已验证,指纹清晰(深回撤>15%接急拉)
  5. **主拉升**:正式拉盘; ✅ 已验证,指纹清晰(急拉簇,涨速中位数12.69%/天)
  6. **出货**:抛售离场。⚠️ 价格形态和普通回调区分不开,需要量数据佐证
- 方法(`scripts/analyze-playbook-stages.mjs`):138 个窗口按同饰品间隔合并成 115 个可分析 episode,对窗口前14天~后5天做分段线性拟合+BIC 无监督变点检测,再用 k-means 对涨速/波动率/回撤聚类,全程不用剧本先验,聚类完才对号入座防循环论证。
- 结论:洗盘和主拉两个指纹已经可靠,**已实现上线**（`lib/signals/washout.ts` + `lib/anomaly-scan.ts` 的 `washout_signal`）;吸货期、会员进场、出货期三个都卡在缺量/挂单深度数据——这把 A2 遗留的"调研成交量/挂单深度数据源"提级为阻塞项。**2026-07-20 有进展**:发现 SteamDT 接口本来就有求购价/求购数（`biddingPrice`/`biddingCount`），之前被同步代码丢弃，已补上开始入库（见 A2）。这不是完整方案（只是求购侧，没有在售深度/成交笔数），但足够开始重新验证吸货/出货指纹了——只是数据要从 2026-07-20 起重新攒，等几周有积累量再重跑。
- 后续:剩余 212 条待审核标完 + 求购数据积累几周后,重跑本脚本(带上 bidding 特征)让三个已验证簇的边界更稳,同时再看能否补出吸货/出货指纹。

**B3. 联动预警上线**
- 规则:某收藏品上级确认异动/高嫌疑 → 自动对同收藏品下级(炼金料)生成"可能跟涨"提示,进异常提醒页。

**B4. 嫌疑分接入自动扫描**
- 每小时给全部跟踪品打嫌疑分,高分(≥60)自动落提醒;异常页支持按嫌疑分排序。
- 低价品(<¥0.5)分数打折,治最小报价单位造成的虚高。

## 阶段 C：预测模型

**C1. 数据集构建 + C2. 基线模型与评估 —— 第一版已完成,见 `REPORT-prediction-baseline.md`（结论:不满足上线门槛）**
- 方法(`scripts/build-prediction-baseline.mjs`):样本=(饰品,小时)时点,标签=未来 N 天(1/3/7)内是否进入操盘窗口;特征=B1 验证过的 8 个特征 + 洗盘信号 2 个特征(联动特征/品类静态特征受限于当前标注结构,还没能力度加进去);逻辑回归(手写梯度下降,没装新依赖);按 2026-06-29 切分训练/测试(约78/22)。
- 名义结果:6 组(3 个 N × 2 套候选池)全部"打赢"随机基准和 coMove 规则基准。**但不建议上线**,三个问题:
  1. 候选池只用 53/131 个已标注/跟踪饰品自己的历史,不是全市场随机样本,数字比真实上线场景乐观(报告里用 narrow vs full 两套池对比直接量化了这个偏差有多大,PLAN 风险4 说中了)。
  2. coMove 规则基准本身是废的(当前标注集里 AUC 只有 0.50,且真正有收藏品结构的候选——千瓦/手套——因为 A1 范围调整永久失去了标注机会),打赢它没有验证意义。**2026-08-03 修正**:那个 0.50 是池化+退化样本造成的假象,coMove 按饰品检验是 32/32、中位 AUC 0.613(coMove24h 0.749)。所以"打赢的是个废基准"这个说法不成立,**但结论方向反而更不利**——真实的 coMove 基准比当初以为的强得多,模型"打赢"它的那点优势要重新评估,C1/C2 重跑时必须用修正后的基准。
  3. **最该关注**:模型给外部事件(版本更新等)打的分几乎跟真实操盘一样高,明显分不清"操盘"和"异动"——这正是 PLAN 原则3 警惕的混淆源,当前特征集测的是"异动强度",测不出"是不是有人在操盘"。
- 下一步:等全市场候选池、求购深度数据积累、能分开操盘/外部事件的特征,再重跑;C3(回测)在这些问题解决前没有意义,暂不做。
- **T+7 可行动标签第一版已跑完，见 `REPORT-t7-actionable-labels.md`（2026-07-21，`scripts/build-t7-actionable-baseline.mjs`）**：标签换成"此刻买入、锁7天、第7天卖出净收益是否>2%"。跑出来模型精度39.5%/平均净收益+41.6%，明显打赢随机基准(-0.5%)和新增的动量(追涨)基准(-33.1%)——但排查发现这个数字被极少数极端赢家主导(去掉贡献最大的5个饰品,平均收益掉到+7.67%),"打赢基准"的方向可信,具体打赢多少现在样本量下没有统计意义,不能拿来估算实盘收益。**过程中抓到并修复了一个真实bug**：每日top-5评估如果不按饰品去重，同一饰品同一天的多个小时级样本会把"5次独立判断"算成"1个饰品的孤例被计数5次"，第一版数字(+50.5%)全是灌水的假象。**目前最可信的两个结论**：①别追涨——动量基准两次测试都是深度负收益(-28.8%~-33.1%)，效应量级远大于模型的正向优势，且不需要任何模型；②drawdown48h(洗盘回撤)在这版和C1/C2最新一版里都是最大正权重特征，跟REPORT-manipulation-playbook-stages.md独立验证的洗盘指纹三次互相印证，是目前唯一稳定复现的信号。下一步：测试期(43天)受候选池90天K线窗口限制拉不长，需要更多真实标注窗口或更长历史才能把"打赢多少"估准；"别追涨"这条不需要模型验证，可以考虑直接做成异常扫描里的谨慎提示。

<details><summary>原始设计(供参考,已被上面的实际结果取代)</summary>

- 样本定义:每个(饰品,小时)为一个时点;标签=未来 N 天内是否进入操盘窗口(N 取 1/3/7 各做一版);若 B2 阶段可辨,进一步做"当前处于剧本哪个阶段"的多分类。
- 特征:B2 筛出的阶段指纹 + 联动特征 + 品类静态特征(收藏品/品质/价位段)。
- 切分:按时间切,同一操盘窗口不得跨切分。
- 逻辑回归起步,不够再上 GBDT。
- 指标:**precision@top-K**(每天推 K 个候选里几个真的被拉)和**提前量**。召回不重要——漏掉不亏钱,推错才亏钱。
- 硬性门槛:胜过两个笨基准(①随机;②"上级动了就报下级"纯规则),赢不了不上线。

</details>

**C3. 回测**
- 模拟策略:信号买入→持有到出货信号或固定 N 天→卖出,算**扣除 C5 和悠悠有品手续费**后的净收益,对比"什么都不做"和纯规则。
- 已定:只按 C5/悠悠有品两个平台的费率算(实际交易也在这两处手动进行);低价品还要扣流动性折价(挂单深度差,名义价卖不掉)。
- **T+7(2026-07-21 定):回测必须硬编码 7 天强制持有**——买入后第 7 天之前不允许模拟卖出,出货信号在锁定期内触发也只能等锁定期结束才执行。不带这条约束的回测数字全是假的(见原则6 的模拟:60% 的历史盘 7 天内过峰)。

**C4. 上线**
- 每小时对全部跟踪品打预测分,top 候选进"机会"页,每条附触发理由。

## 阶段 D：决策闭环与产品化

**D1. 逃顶信号**
- 直接复用 B2 的"出货期指纹"做操盘结束检测;对已持仓且处于操盘中的品出"建议了结"提示。
- **优先级上调(2026-07-21,T+7 后果)**:T+7 把买入侧收益砍了一半(原则6),但卖出侧不受影响——持有超 7 天的仓位随时能卖。已持仓的品吃到拉盘后**卖在对的位置**变成了比"买在对的位置"更可靠的盈利来源,D1 从"锦上添花"升级为核心功能。当前依然卡在出货期没有价格指纹(REPORT-manipulation-playbook-stages.md),继续依赖求购深度数据积累,但数据够了之后 D1 应排在 C 阶段重跑之前做。
- **2026-08-03 有实质进展,但走的不是原计划那条路**:原计划是"复用出货期指纹",而出货期指纹至今没验证出来。实际做出来的第一版卖出信号(`lib/rules/sell-rule-v2.ts`)绕开了它,改用**已验证的追涨风险 + 洗盘否决**,阈值全部从回测反推(`scripts/build-sell-rule-baseline.mjs`,714 个饰品/99 天,扣掉大盘 beta 的超额口径):24h涨幅 >30% 超额中位数 -18.69%、71.4% 为负;15~30% 为 -4.35%~-5.89%;5~15% 只有 -3% 上下、扣手续费后不值得换手所以不触发;深回撤且低涨幅时超额反而为正、明确不卖。也就是说 D1 不必等出货期指纹——**追涨风险这个已验证的信号本身就够撑起第一版逃顶提示**。
- **2026-08-03 之二,状态从"只影子并行"推进到"影子并行 + 模拟盘平仓判定"**:v2 现在是模拟盘平仓的**唯一**判据(30 天超时强平保留作兜底),`close_reason` 按档位分成 `sell_rule_v2_strong`/`sell_rule_v2`。**给人看的真实建议仍然完全由 v1 出,`/positions` 一个字没动**——这一步只换模拟账本。理由是 v1 卖出侧结构上永远触发不了,230 笔仓位注定全部走 `timeout`,而 timeout 的含义是"持满 30 天按当时价卖掉",对"卖出规则准不准"零信息量;模拟盘存在的意义就是验证卖出规则,拿一个永不触发的规则去验证等于什么都没验。影子表继续全量记录(两边用同一次判定,数字可互相印证)。评估脚本两个:`scripts/report-shadow-sell-signals.mjs`(影子侧触发次数/命中率)、`scripts/report-paper-trades.mjs`(平仓侧超额收益 + "如果持到第30天"的反事实对照)。
- **接进真实建议之前要看到什么**:两个卖出档的超额中位数都为负且强档明显更负、反事实对照里"提前卖 − 持到30天"为正、且结论在多个开仓日 cohort 上一致。样本判据是可评估平仓 ≥30 笔且开仓日 ≥5 天——**回测里 15~20% 档的为负占比也才 61%,几十条样本的随机波动完全盖得过这个幅度**。按回测里 ≥15% 三档合计占全部饰品-小时 2.7% 估算,攒到三位数要几周。验证通过后要考虑的两件事:接进 `/positions` 的建议列,以及要不要接推送(D3 里 D1 本来就列在待接入清单)。

- **2026-08-03 之三:v1 规则引擎已被逐项回算,结论是它没有一项站得住**(`scripts/build-rsi-trend-baseline.mjs`,只读,口径与 `build-sell-rule-baseline.mjs` 完全一致)。成交量项此前已查明是死信号并删除,剩下的两项现在也有了数字:
  - **RSI 方向对但幅度不够**:≥70 超买 −0.94%(小时)/−3.57%(日);<30 超卖 +0.60%/+0.42%。
  - **均线趋势方向是反的**:走弱 +0.34%/+0.37% 却给 −25,走强 −0.83%/−1.54% 却给 +15。主要依据是**同一饰品内部的配对检验**(消掉饰品自身漂移):小时 578/713、日 555/649,差距中位 +1.66%/+5.30%,p=0.0000。这跟已验证的短期反转一致——涨过头会回落。
  - **两项对交易成本都不够**:一次往返 6.7%~12%(价差中位 5.72% + 手续费 1%;12% 是项目所有者口径),而十个 RSI 档位没有一个够得着。对比 v2 的 >30% 档 −18.69% 对两条成本线都够。
  - **对 v1 买入侧的直接含义**:买入侧走的就是 RSI<30 → +30 → 达到 `ENTRY_MIN_SCORE` 开仓,而那一档超额只有 +0.42%~+0.60%,**赚不回换手成本**。模拟盘 136/153 笔开仓是 score=30,等于全靠这一个信号。
  - **删除趋势项的影响已出只读清单**(`scripts/preview-trend-removal-diff.mjs`):42 个饰品 `TRIM`→`HOLD`(12.9%,全部来自"只有走弱"那一档,属于纠错);无饰品变 `SELL`;真正会新增开仓 9 个。**但删趋势项在买入侧是放开闸门**(110 个饰品 `+5`→`+30` 跨过门槛),而买入侧本来就赚不回成本——**所以"删趋势项"和"动 `ENTRY_MIN_SCORE`"必须一起决定,不能只做一半**。
  - ~~**状态:全部只读,尚未改任何生产代码**~~ **2026-08-13 项目所有者已决定并落地**,见下条。

- **2026-08-13:趋势项已删除,`ENTRY_MIN_SCORE` 维持 30,另加一条成本线标注**。三件事一起做的,缺一件都会留下反效果:
  - **删趋势项**。理由不只是"方向反了不能翻"(翻方向是数据窥探的标准产物),更彻底的一条是**就算翻过来也没用**:走弱那档超额只有 +0.32%~+0.35%,翻成正向信号照样离 6.7%~12% 的往返成本差一个数量级。一个正着用反着用都不可行动的项,留着只会污染 score。实际收益是 **42 个饰品 `TRIM`→`HOLD`**——停止基于一个反向信号建议减仓 12.9% 的持仓。删完可达 score 塌缩成 `{−30, 0, +30}`,`SELL` 结构上不可达(这是有意的,真实平仓已交给有回测依据的 v2)。
  - **`ENTRY_MIN_SCORE` 维持 30**。原来担心的"放开闸门"不成立:预览显示 110 个饰品跨过门槛但**真正新增开仓只有 9 个**,因为 269 笔 open 对应 269 个不同饰品(一品一笔)、跟踪总量 325,位置已接近饱和。更重要的是**闸门管的是模拟盘不是真金白银**:`ENTRY_MIN_SCORE` 只作用于 `lib/paper-trading.ts`,它开的仓是已知负 EV 的(RSI<30 那档 +0.37%~+0.61% vs 成本 6.7%~12%),但**代价是模拟的、收益(卖出侧样本)是真的**;提到 40 等于永不开仓(删完趋势项后最高分就是 +30),存量仓位平完之后卖出侧验证直接断粮。
  - **所以写死一句话在 `lib/paper-trading.ts` 和这里:模拟盘的开仓阈值是采样率参数,不是策略参数,不代表任何买入建议。** 证据:现有仓位 `buy_score` 只有 30 分 262 笔、40 分 17 笔,全靠 RSI 单因子。没有这句,三个月后有人看到 `ENTRY_MIN_SCORE=30` 会以为我们认可 RSI 抄底。
  - **面向真金白银的那一侧补了成本线**(`lib/rules/cost-line.ts`):`ROUND_TRIP_COST_MIN=6.7%`/`ROUND_TRIP_COST_TARGET=12%` 做成常量、不再散落在脚本注释里;每一档的回测超额跟着信号一起走,`/positions`、`/watchlist`、饰品详情页把没过成本线的档**显式标成"不可行动"并显示该档历史超额**;LLM 提示词强制带上这个标注,不然 `+30` 喂进去出来的中文必然是"超卖,存在反弹机会"——那是在用自然语言把一个证伪过的信号讲回机会。
  - **`paper_trades` 加了 `entry_rule_version`**(迁移 022,旧仓位回填 `v1`、新仓位写 `v2`):8-13 之后的平仓样本否则是两套买入规则的混合,算卖出侧胜率时拆不开。
  - **数字用今天的库复核过**(714 个饰品/109 天,比 8-03 那次多 10 天):RSI<30 超卖 **+0.61%**(小时,698 个饰品,p=0.0000)/**+0.37%**(日,513 个),趋势配对检验小时 **587/713**、差距中位 +1.71%,日 **554/652**、+5.11%,p 全 0.0000。结论与 8-03 完全一致。

**D2. 交易流水与月度盈利**（已定需求,可与 B 并行开发）
- 库存同步发现商品减少(卖出)时:先尝试从 C5 卖家订单接口拉真实成交价(接口已接过,`lib/api/c5` 有卖单类型);拉不到就弹窗问用户卖价。
- 记录完整流水:每笔买入价、卖出价、时间、平台;按月汇总买入额/卖出额/已实现盈利。
- 流水同时是复盘素材:信号的实盘胜率、平均收益,定期对账反哺标注和阈值。
- 配套改造:现在"卖出=直接删行"要改成"标记已售+进流水",不然卖出历史留不下来。

**D3. 产品形态:App**
- 已定目标:**最终做成电脑端+手机端 App,最低限度是网页版**。
- 演进路径(不推倒重来,沿现有 Next.js 走):
  1. 现在:本机网页(已有);
  2. 部署成常驻服务(配合 A2 的常驻采集器) —— **已完成（2026-07-21）**：Oracle Cloud VM，公网 IP 可访问，手机浏览器随时能看，加了 Basic Auth 最低限度防护；
  3. PWA 化:手机"添加到主屏幕"即近似原生 App,并解锁 **Web Push 通知**——高优先级信号(联动预警、逃顶、预测高分)推到手机;**已完成第一版(2026-07-20)**:manifest+sw.js+VAPID 推送,目前接了嫌疑分预警(≥60)和同收藏品联动预警两类;逃顶信号(D1)、预测高分(C3)还没做,做出来后要补进 `lib/anomaly-scan.ts` 的 `pushNotifications` 列表。设置页 `/settings` 订阅/取消订阅/测试推送。
  4. 需要更原生再套桌面/移动壳(Tauri/Capacitor),Web 代码复用。
- 通知渠道即 App 推送,不再单独接微信/邮箱。

## 阶段 E：多用户与商业化（远期,仅记录,不急着做）

**E1. 多用户基础**
- 若开放给更多人用:用户登录体系、每用户独立的持仓/观察池/标注、数据隔离;订阅收费。
- 到时要重新审视:SQLite→服务端数据库、API 配额按用户扩容、以及**信号分发的利益冲突**(你的会员拿到信号本身会影响盘面——这正是庄家剧本里的"散消息"环节,角色会反转,需要想清楚再做)。
- **实现方案（2026-07-25 设计定稿,只记录不实施）**——总原则:**数据分共享层和用户层**。共享层=市场数据+研究资产(price_snapshots/K线/item_catalog/anomaly_events/manipulation_tags/market_candidate_pool),全局唯一,采集一次全员用,标注审核保持 admin-only(站长研究资产);用户层=inventory/watchlist/ledger/push_subscriptions/paper_trades,加 `user_id` 列。这个切分是整个多用户成本结构的核心:用户数增长时,共享层的采集/存储/LLM成本几乎不动,只有用户层线性增长。
  1. **登录:首选 Steam OpenID**。目标用户 100% 有 Steam 账号,OpenID 免审核免费,登录即拿到 SteamID64——现在 `.env.local` 里的 `STEAM_USER_ID` 变成每用户字段,库存导入直接打通。微信登录要企业资质,不做;可留邮箱登录兜底。
  2. **数据库迁移路径(不推倒重来,每步可独立验证)**:①先引入 Drizzle ORM 重写 `lib/db/*`(同时支持 SQLite/Postgres,本地开发继续 SQLite,这步随时可做且不破坏单用户);②`users` 表+`user_id` 列先在 SQLite 上落地,种子用户=站长,行为不变;③开放前切托管 Postgres(Neon/Supabase 免费层起步),一次性脚本搬数据(90万行 price_snapshots 对 Postgres 不是问题)。
  3. **外部 API 配额:铁律是用户永远不触发外部 API 调用,只读库**。采集器保持全局单例,同步集合=所有用户持仓+观察池的去重并集(热门品重叠度高,增长亚线性)。容量账:批量价格接口 100 名/次、请求间隔约 60s(HANDOFF 踩坑 6c)→ 每小时硬上限约 6000 个去重饰品,到顶之前不用换方案;超了做分层同步——多人跟踪的热门品每小时,长尾品降频到 4-6 小时或改走 kline 接口(限流宽松,实测 250ms 连打 490 次没事)。Steam 库存导入是按用户调 Steam 的,要排队+限速,不能并发导。**ToS 待办:开放前跟 SteamDT/C5 确认聚合数据展示给终端用户/商用是否允许,必要时谈授权——被封 key 是致命的**。
  4. **LLM**:`reason_cache` 按 饰品+action+score+日期 做 key,天然全员共享,用户数增长不会线性增加调用量,这块架构不用改。要加的是**按用户限流**(每人每天 N 次未命中缓存的生成,防止刷爆额度);可选把 top 信号的理由挪到同步任务里预生成,用户到页面全是缓存命中。**供应商:NVIDIA NIM 免费层别指望商用**,切换首选 DeepSeek 官方 API——本来用的就是 deepseek 模型,OpenAI 兼容格式,`lib/api/nvidia-llm.ts` 只改 env 的 base URL/key/model 就能切,价格 ¥1/百万 token 量级;保持封装隔离,调用方不感知供应商。
  5. **部署**:1核1GB 撑不住多用户。①先把采集器和 Web 拆成两个容器(现在同步挂在 Next 进程里,重同步会抢 Web 的 CPU);②VPS 升到 2核4G 起步;③受众是国内玩家的话美西节点延迟差,考虑香港节点(免备案,国内节点要域名备案);④注意 `lib/signal-cache.ts` 是进程内缓存,多实例部署时失效逻辑会破,单实例没问题,真要横向扩容时换 Redis 或接受 TTL(2026-07-30 起这层只剩"各平台最新一条",每饰品几行,价格历史那份因为在批处理循环里撑爆内存已经删掉——横向扩容时要迁移的数据量比原来小得多)。⑤**内存是先于 CPU 撞墙的那一项**:2026-07-27 就是因为异常扫描把全部追踪饰品的完整历史同时读进内存,把 1GB 机器的 Node 堆撑爆、连崩三天(详见 HANDOFF 踩坑 28)。已经用 `SIGNAL_HISTORY_WINDOW_DAYS`(21天读取窗口)在结构上封顶,内存不再随饰品数×历史长度增长;**但拆容器/上多用户时要重新验一遍这条**——多用户的同步集合是所有人持仓+观察池的并集,批处理循环的规模会再上一个量级。
  6. **合规最低配**:一句免责声明("建议仅供参考,不构成投资建议"——决策类产品必须有)+ 最简用户协议覆盖"平台存储你的持仓/流水数据"。
  - **实施顺序**:Drizzle 改造(随时可做)→ Steam OpenID+users 表 → user_id 铺开 → Postgres 切换 → 分层同步(用户量逼近配额再做)。前两步不等开放决定也可以先做,是纯架构健康度改进。

**E2. 观察池聪明钱信号（2026-07-24 记录,依赖 E1）**
- 出发点:开放后会有职业饰品商人把"提前知道要涨的东西"加进自己的观察池——他们不用明说,**加观察池这个行为本身就是信号**。项目所有者从后台观察这些行为,作为自己的买入参考。这和现有的"buy_price>0=凭操盘消息买入"是同构思路:用行为标记聪明钱,从标记饰品升级到标记人。
- **核心设计:事件流而非状态表**。建 `watchlist_events` 表 `(user_id, market_hash_name, event_type[add/remove], created_at)`——"什么时候加的"是信号核心,**remove 也是信号**(消息灵通的人撤掉≈行情走完,对应卖出时机),状态表留不住这些。
- **用户准确率评分**:散户随手加的是噪音,要区分"谁的观察池值得跟"。定时任务(复用 sync 机制)回看每个 add 事件,用 `price_snapshots` 对比加入时价格 vs 7天/30天后价格,算每用户命中率/平均收益存 `user_signal_stats`。跑一两个月后"加什么涨什么"的账号自然浮现,其新动作权重调高。
- **聚集性检测**:多个互不相关的高分用户在短时间窗口加同一个饰品 = 最强信号(一个人可能是巧合,几个职业商人同一晚加同一个东西基本是拉盘前兆)。
- **后台与提醒**:admin-only 页面(`users` 表加 `role` 列,proxy.ts 拦截),展示最近事件流/按用户准确率排序/按饰品聚合;高分用户新 add 或聚集性触发时复用 D3 Web Push 推送——这类信号时效可能只有几小时,不能靠日报。
- **风险,做之前必须想清楚**:①用户观察池是用户数据,拿来做站长个人交易决策需要用户协议/隐私政策覆盖("平台可对使用数据做分析"类条款);②信任风险更致命——饰品圈子小,职业商人警觉,一旦发现"站长在抄观察池作业",最有价值的这批用户会跑光,甚至反向喂假信号钓鱼。
- **更稳的变体(优先考虑)**:把聚合信号做成**公开产品功能**——"观察热度榜/聪明钱指数"(匿名、聚合、延迟发布),站长后台看实时、精确到人的版本。信息优势从"独占偷看"变成"更早更细",产品还多一个吸引用户的功能,被发现时解释空间完全不同。
- 实施顺序:用户认证 → watchlist 挂 user_id + 事件表 → admin 后台事件流 → 准确率评分定时任务 → 聚集性检测 + Web Push 提醒。前两步是 E1 本来要做的,后面纯增量,不动现有单用户逻辑。

---

## 里程碑

| 里程碑 | 判定标准 | 预估节奏 |
|---|---|---|
| M1 标注清零 | pending=0,窗口标记 ≥150 个 | 本周 |
| M2 采集器常驻（云端 Docker，Oracle Cloud） | 连续 30 天无断档,量数据入库 | 2026-07-21 迁到云端后重新起跑，验证中（此前 Windows 本机方案断过至少一次） |
| M3 剧本验证报告 | B2 产出六阶段画像(带数据证实/修正结论) | M1 后 1-2 周 |
| M4 联动预警+流水上线 | B3 规则进扫描;D2 流水记录跑通 | 与 M3 并行 |
| M5 预测基线 | C2 胜过两个笨基准 | M2+M3 之后 |
| M6 PWA+推送 | 手机能装、能收到信号推送 | M4 后 |
| M7 首次实盘复盘 | 依信号完成 ≥10 笔操作并用流水复盘 | M5 后 1 个月 |

## 主要风险

1. **样本量**:138 个窗口对分类器仍偏少,且集中在 2026 年 4-7 月单一市场环境——大更新(如 5-22)一来规律可能整体改写。缓解:持续标注 + 外部事件单列 + 定期重训。**这个风险已经部分兑现:2026-07-15 交易保护新规(T+7)是结构性变化,全部历史标注都来自没有锁定期的旧市场**;好在求购深度数据(07-20 起)全是新规时代的,新旧市场的形态差异以后可以对比验证。
2. **数据断档**(A2 不做,后面全部白搭)。
3. **API 依赖**:SteamDT/C5 改接口、收紧配额都会伤数据管道;kline 等实测行为与文档不符,升级要重新实测。
4. **自我实现偏差**:标注大量来自"已购=操盘"规则,模型可能学成"识别你会买什么"。缓解:观察池里放一批与买入决策无关的对照品(A3)。
5. **剧本先验偏差**:B2 验证时避免"按剧本切数据再用数据证明剧本"的循环论证——分段先用无监督变点检测切,再对照剧本命名。
