# 服务器/NAS 部署用：docker compose up -d --build
# .env.local（各平台 API key）不进镜像，用 env_file 注入。
services:
  app:
    build: .
    ports:
      - "3210:3000"
    env_file:
      - .env.local
    volumes:
      - ./data:/app/data
    restart: unless-stopped
