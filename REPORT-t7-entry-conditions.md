# 买入侧：可观测入场条件的 T+7 分档表

> 脚本：`scripts/build-t7-entry-baseline.mjs`（只读）。日期：2026-08-15。
> 基准口径版本 `b03672dc0`（874 行 / 8 个窗口 / 2026-04-18 ~ 2026-08-08）。
>
> **这份报告取代 `REPORT-t7-actionable-labels.md` 作为买入侧的现行结论。**那一份不是复用而是
> 重做——它的方法有已知缺陷（见第六节），数字不要再引用。

---

## 0. 一句话结论

**没有。在可采信的样本窗口里，35 个候选档位没有一个的 T+7 绝对收益中位数够得着 6.7% 的
往返成本下界。**最强的一档（偏离 7 日均线 < −20%）是 **+4.57%**，差 2.1 个百分点。

这是一个**有效结论，不是"这次没跑出东西"**。它说明：现有可观测的价格类特征撑不起买入侧，
下一步该去找**新数据源**，而不是换模型、换窗口、或者再加特征组合。

顺带跑出一个**卖出侧**的意外发现，见第四节——那一条是够得着成本线的。

---

## 1. 为什么换标签（背景）

C1/C2 那条线的终点结论是模型分不清"操盘"和"外部事件"（`REPORT-prediction-baseline.md`
问题 3）。这不是特征不够好，是**标签定义不良**：操盘不可观测，只能用价格特征去识别一个
只能用价格特征识别的东西，循环里没有新信息进来。而唯一通过经济显著性门槛的结论
（v2 的 >30% 档，超额 −18.69%）是从 714 饰品的**价格数据**反推的，一条人工标注都没用到。

所以买入侧改用**自动生成的标签**：此刻买入、锁 7 天、第 7 天卖出的收益。方法论跟卖出侧 v2
同源（分档 + 超额 + 成本线 + 按饰品符号检验）。人工标注从训练输入降级为事后解释性检查。

T+7 不是可调窗口：新规后买入锁 7 天才能卖（PLAN.md 原则 6），"第 7 天的收益"就是买入侧
唯一能变成钱的量。**不要因为不够成本线就去试 14/30 天窗口**——理由见 HANDOFF ② 的预先封口。

---

## 2. 口径（六条，全部预先钉死）

| # | 口径 | 落实 |
|---|---|---|
| 1 | **按饰品去重**：同一饰品同一天只取一个样本 | 取当天**最早**的合格小时桶。取哪个小时与结果无关——按"回撤最深"或"收益最好"去重就是每天挑一次最优点，那正是上一版的病根 |
| 2 | **只报中位数，不报均值** | 表里没有任何均值 |
| 3 | **每档同时给**可评估数、饰品数、中位为正/为负的饰品数、符号检验 p | 表结构与 `build-sell-rule-baseline.mjs` 对齐 |
| 4 | **AUC 只淘汰不录取** | 下限 0.50，推导见第五节 |
| 5 | **régime 必须单一** | 主口径 `--since 2026-07-01`，见第三节 |
| 6 | **绝对收益和超额都要报** | 这一条是写脚本时补的，理由见下 |

### 关于第 6 条：成本线是用现金付的，超额不是现金

> **⚠️ 这条判据只属于买入侧，不要套到卖出侧去**（2026-08-15 项目所有者确认）。
> `build-sell-rule-baseline.mjs` 整张表是**纯超额**口径，v2 的全部阈值都是那么反推的，
> **那是对的，不用重算**。**两侧的正确口径本来就不同，这不是不一致，是标的不同**：
> 卖出侧决定"换不换手"，两个选项**持有同一份资产**，大盘涨跌对双方一样、在差额上抵消；
> 买入侧决定"要不要动用现金"，对照物是**现金**不是市场。
> **判之前先问一句：这个决策的对照物是同一份资产，还是现金？**

原始要求写的是"**超额**收益的中位数能覆盖 6.7%~12% 的往返成本"。**但这两个量不同质。**
大盘跌 10% 而某饰品只跌 2% 时，超额是 +8%、现金是 −2% —— 拿不出钱付手续费。反过来只看
绝对收益又会把 beta 当成信号（踩坑 44 ②，卖出侧正是为此才改的超额口径）。

**所以录取判据写死成：绝对收益中位数 ≥ 成本线，且超额中位数 > 0。**

这不是吹毛求疵——**它恰好是这次的主要结果形态**。下面大量档位落在"超额为正、绝对为零"
（表里标成"只赢大盘（现金亏）"）：市场在跌，这些条件挑出来的饰品只是**跌得比大盘少**。
只按超额判的话，会有十几个档位被误读成买点。

---

## 3. 样本与 régime

`report-regime-boundaries.mjs` 实测：**2026-04~06 有一批至今无法解释的台阶**（平台数在
1↔5 之间反复跳：05-27、06-13、06-22、06-29……；饰品数 113→486→710→531→711 反复跳），
另外 **07-19（平台数 5→2）和 07-28（5→3）也没有对应的已知配置变更**。

按规范四点五 #4（"没查清之前，任何跨越 2026-07 之前的聚合都不要用"）：

- **主口径 = `--since 2026-07-01`**：714 个饰品 / **39 天**（2026-07-01 ~ 2026-08-08）/
  去重后 **17993 个饰品-天**（去重前 390475 条小时样本）。这个区间内的 7 月边界都是
  **已解释**的变更（候选池回填结束、迁云端、C5 提频、OOM 崩溃循环），没有一条改变单个
  饰品的价格语义。区间内仍含 07-19 / 07-28 两处未解释的平台数抖动，见下。
- **全区间跑法（04-25 ~ 08-08，48316 个饰品-天）只作参照**，**幅度和 p 值不作数**。

> **平台数抖动对本脚本的影响比对别的脚本小**，这是修回看方式的副产品：参考平台按**全历史**
> 快照数固定选一次，某天只采到别的平台时，这个饰品当天就是**缺格**，而缺格的样本会被
> 直接跳过（不做就近取值）。所以平台抖动在这里表现为**功效损失**，不是静默的语义漂移。

**两项预先声明的稳健性检查**（是预先声明的一对，不是跑完挑好看的那个）：

| 检查 | 做法 | 结果 |
|---|---|---|
| 去重规则 | `--hour-pick noon`（改取最接近 12:00 UTC 的小时） | **方向全部一致**，幅度小幅变动。结论不由"取哪个小时"决定 |
| régime | 不带 `--since` 跑全区间 | **方向全部一致**；唯一的幅度差异见第四节 |

---

## 4. 结果

### 4.1 买入侧：全军覆没

主口径（`--since 2026-07-01`）下最强的四档，**没有一个够得着 6.7%**：

| 条件档位 | 可评估 | 饰品数 | 计票 | 中位为正/为负 | **绝对中位** | 超额中位 | 符号 p | 判定 |
|---|---|---|---|---|---|---|---|---|
| 偏离 7d 均线 **< −20%** | 119 | 36 | 24 | 17 / 7 | **+4.57%** | +6.67% | 0.0320 | ✗ 差 2.1pp |
| 24h 涨跌 **−30~−20%** | 47 | 36 | **9** | 8 / 1 | **+5.06%** | +6.83% | 0.0195 | ✗ 只有 9 票 |
| 48h 回撤 **20~30%** | 137 | 68 | 38 | 26 / 12 | **+0.43%** | +2.23% | 0.0168 | ✗ |
| 组合：回撤>15% 且 24h<5% | 394 | 123 | 88 | 65 / 23 | **0.00%** | +2.04% | 0.0000\* | ✗ 现金持平 |

（\* = 过 Bonferroni α=0.00143。35 个档位的分母是**跑之前**声明的。）

三件事要一起读：

1. **"洗盘深回撤"这条最有先验依据的假设，方向对、样本厚、p 极小，但幅度是 0。**
   回撤>15% 且低涨幅：123 个饰品、88 票、65 正对 23 负、p=0.0000——这是本表里方向最扎实的
   一档，而它的**绝对收益中位数是 0.00%**（超额 +2.04%）。也就是说：洗盘态之后确实跑赢大盘
   两个点，但**现金上就是不赚不赔**，换手成本一扣是净亏。
   **这是 drawdown48h 第一次被放在成本线下面量**——此前三份报告说它是"最大正权重特征"，
   那是**排序能力**，不是**幅度**。两者不是一回事，这一档把差别摊开了。
2. **两个够到 5% 量级的档位都薄。** 24h 跌 20~30% 那档只有 **9 个饰品够 2 天门槛**，
   而它"各饰品中位数的中位"高达 +26.75%——池化中位只有 +5.06%，两个数差 5 倍，
   典型的少数饰品撑起来的形态。**9 票不下任何结论。**
3. **越靠近尾部越薄，这是结构性的**，不是这次运气差：39 天 × 714 个饰品的盘面里，
   24h 跌超过 20% 的饰品-天一共只有 97 个。

### 4.2 卖出侧：意外发现一条够得着成本线的

买入侧的分档表在另一端摔出了一条**比 v2 现有信号更强**的卖出条件：

| 条件 | 窗口 | 可评估 | 饰品数 | 计票 | 正/负 | **绝对中位** | 超额中位 | 符号 p |
|---|---|---|---|---|---|---|---|---|
| **偏离 7d 均线 > +20%** | 主口径 | 178 | 81 | 45 | 8 / 37 | **−19.04%** | −17.30% | 0.0000\* |
| 同上 | noon 去重 | 174 | 79 | 44 | 6 / 38 | −18.92% | −17.03% | 0.0000\* |
| 同上 | 全区间（参照） | 1040 | 283 | 193 | 47 / 146 | −13.37% | −12.29% | 0.0000\* |
| 对照：24h 涨幅 > 15% | 主口径 | 235 | 132 | 62 | 10 / 52 | −12.85% | −10.87% | 0.0000\* |

**三个跑法全部过 12% 线，幅度稳定在 −13% ~ −19%，饰品数 79~283，p 全部过 Bonferroni。**
量级跟 v2 的王牌档（24h 涨幅 >30%，超额 −18.69%）相当，**而 v2 目前只用 24h 涨幅、
根本没看均线偏离**。同一张表里，maDev>20% 比 return24h>15% 还狠 6 个百分点。

### 4.2b 独立性检验（预先声明后执行，结论：**不加进 v2**）

项目所有者声明了**一次检验、不是一组**：在 `return24h < 15%` 的样本里单独看 `maDev > +20%`，
只此一种切法，跑完不换边界、不换分层变量。主口径窗口，纯超额判读。

| 切法 | 可评估 | 饰品数 | **计票** | 正/负 | 绝对中位 | **超额中位** | 符号 p |
|---|---|---|---|---|---|---|---|
| `maDev>+20%`（无条件，即 4.2 那行） | 178 | 81 | 45 | 8 / 37 | −19.04% | **−17.30%** | **0.0000\*** |
| `maDev>+20%` **且 `return24h<15%`** | 78 | 44 | **20** | 7 / 13 | −13.99% | **−11.58%** | **0.1316** |

**结果不是干净的二选一，如实记：幅度台阶还在，统计支持塌了。**

- **幅度这一半通过了**：剥掉追涨样本之后超额仍有 **−11.58%**，保住了原幅度的 67%，
  仍然够得着 6.7% 的成本线下界。
- **样本这一半没通过**：可评估从 178 掉到 **78** —— 也就是说 **56% 的 `maDev>20%` 样本
  本来就伴随 24h 涨幅 ≥15%**。**这个重叠度本身就是答案的一半：它跟追涨确实高度共生。**
  剩下的 78 条只摊到 44 个饰品、**20 个够计票门槛**，13 负对 7 正，**p=0.1316，
  连名义 0.05 都够不着**。

**按预先声明的判据，"饰品数够"这一半没满足 ⇒ 不作为独立候选，不进影子并行，不改任何现有阈值。**

**但要说准**：这**不等于**证明了 maDev 没有独立性，只是**用现有数据立不住**。
点估计仍然很大而样本撑不住 —— 这正是"再切一刀就能得到想要的结论"的经典位置，
**所以到此为止，不再换切法**。样本厚了要重开，得重新预先声明。

**这一节存在的意义**：以后谁再看到 §4.2 那个 −19.04%，会同时看到"其中 56% 跟追涨重合、
剥离之后 p=0.13"，**不会重新兴奋一次**。

### 4.3 AUC：如设计的那样，什么都没说明

| 特征 | 主口径 AUC 中位数 | 全区间 | 判定 |
|---|---|---|---|
| 48h 回撤 | 0.621 | 0.564 | 过下限 |
| 24h 涨跌 | 0.601 | 0.566 | 过下限 |
| 7d 涨跌 | 0.644 | 0.602 | 过下限 |
| 偏离 7d 均线 | 0.678 | 0.611 | 过下限 |

**四个全过下限，一个都没被淘汰，而四个的买入侧全部不过成本线。**这正是踩坑 46 说的：
AUC 高低跟"能不能赚钱"是两件事。偏离均线的 AUC 最高（0.678），它的买入侧一样是 +4.57%。

下限取 **0.50** 而不是 0.60，理由写在脚本常量里：阴性对照 `volumeRatio`（已证实的死信号）
是 0.470，而**已上线的 return24h（0.567）比被否掉的 RSI（0.578）还低**——任何高于 0.5 的
下限都会同时淘汰掉唯一赚得到钱的那个。

---

## 5. 过程中查出来的两个既有缺陷

写脚本对齐口径时查出来的，**都不影响已有结论，但都要记**：

1. **`build-sell-rule-baseline.mjs` 的回看是按数组下标数的**（`series[i - 24]`），
   而那个序列是**按小时去重后的稀疏数组**。采集有缺口时（7-27~7-30 那轮 OOM 崩溃循环、
   偶尔错过的整点同步），往回数 24 格可能落到 30 小时之前，于是"24h 涨跌"的实际窗口
   随缺口悄悄变长，**不报错**。更别扭的是前瞻那一侧本来就是按时间戳精确查的
   （`ts + 7*DAY_MS`），两侧口径不一致。
   **本脚本两侧都按时间戳查，缺格直接跳过并计数**：主口径下 390475 条里有 **13847 条
   （3.4%）** 命中缺格——也就是说老写法里有 3.4% 的样本窗口是被悄悄拉长过的。
   量级不足以推翻 v2 的任何结论（那些档位的幅度是 4~19 个百分点），**但下次重跑
   `build-sell-rule-baseline.mjs` 时应当一并改掉**。
2. **"洗盘回撤"在两处的定义不一样。** 生产 `lib/signals/washout.ts` 取的是窗口内**任意
   时点的最大回撤**（只要高点不是当前点就算数），而 `build-sell-rule-baseline.mjs` 取的是
   **当前价距 48h 高点的回撤**。差别是：中途深跌但已经涨回高点附近的形态，生产判成洗盘态、
   回测那份不判。**生产的定义更松。**
   本脚本用的是**当前回撤**——买入侧问的是"现在还在不在坑里"，已经涨回去的不是买点。
   这个差异是查出来的不是笔误。HANDOFF ⑭ 的结论（洗盘否决只改 reason、改不了 action）
   不受影响，但"回测那一列"和"生产那个分支"此前被当成同一个东西引用过。

---

## 6. 与上一版（`REPORT-t7-actionable-labels.md`）的关系

**不是复用，是重做。**那一版的数字（模型 +41.6%、去掉 top-5 后 +7.67%）不要再引用，
三个原因：

1. 它按"每日 top-5"评估，去重只做在选 top-5 那一步，仍然是**挑最优点**的形态；
2. 它的 `prices[i + 168]` 是往后数 168 **行**不是 168 **小时**——2026-07-26 C5 提频到
   10 分钟一轮之后，168 行大约只有 28 小时。**那份脚本今天重跑会直接给出错误的持有期**，
   它当初的数字（7-21 跑的，那时还是每小时一行）不受影响，但它现在是坏的；
3. 判据是"打赢随机/动量基准"，而不是成本线。打赢一个负收益基准不代表能赚钱。

**唯一被这次证实的旧结论是"别追涨"**：24h >15% 档绝对 −12.85%、7d >15% 档 −10.57%，
三个跑法一致，方向和量级都对得上。**而"drawdown48h 是最稳定的正权重特征"这条要改口**——
它的排序能力是真的（AUC 0.621），但**幅度撑不起一次换手**。

---

## 7. 下一步（按优先级）

1. **买入侧暂停，别再在价格特征上找。** 35 个档位、4 个特征、2 个组合、3 个跑法全部不过
   成本线，且最强的那档还是**统计上最薄的**。这不是调参能解决的形状。
2. **要重开必须靠新数据源，不是新窗口/新模型。** 现成的路只有一条：**求购深度**
   （`bidding_count`/`bidding_price`，2026-07-20 起入库）。HYPOTHESES §2.3 里
   `bidRatio` 已经有 **15/16、p=0.0003、AUC 中位 0.747** 的初步证据，卡在只有 16 个饰品。
   它是目前唯一有正面证据、又不在这次 35 个档位里的候选。
3. ~~**maDev > +20% 的独立性检验**~~ **已跑完，结论是不加进 v2**（见 §4.2b）。
   幅度保住 67%（超额 −11.58%）但计票只剩 20 个饰品、p=0.1316，"饰品数够"这一半没满足。
   **不再换切法。**样本厚了要重开必须重新预先声明。
4. **`build-sell-rule-baseline.mjs` 改成按时间戳回看**（第 5 节第 1 条），重跑一次看
   v2 的阈值动不动。预期不动（3.4% 的样本、4~19pp 的幅度），但要实测。
